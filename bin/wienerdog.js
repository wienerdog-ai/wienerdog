#!/usr/bin/env node
// Wienerdog CLI entry point. Subcommand dispatch is implemented in WP-003.
console.error('wienerdog: pre-release — the CLI is not implemented yet. See https://github.com/wienerdog-ai/wienerdog');
process.exit(1);
