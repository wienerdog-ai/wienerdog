{
  "suite": "tests/unit/run-evidence.test.js",
  "proofs": [
    {
      "id": "run-evidence-redact-before-truncate-argv",
      "wp": "WP-secret-sink-wiring-probes",
      "criterion": "AC4",
      "why": "P4 pins Table S row S2's truncate-then-redact order in sanitizeArgv. Its pass is also what you would see if the site did no redaction at all, and only the paired CORRECT probe P3 rules that out — an inference, which is what ADR-0042 exists to replace. Swapping the cut and the scan makes the whole argv element reach the detector, so PROBE_HEAD no longer survives and MARKER appears: exactly the condition P4 asserts is absent. testNamePattern scopes the phase to P4, because the mutation also changes what every other argv element in this suite is scanned as, and none of those is this criterion's concern.",
      "file": "src/core/run-evidence.js",
      "find": "    out.push(redactOnly(a.slice(0, 2000)));",
      "replace": "    out.push(redactOnly(a).slice(0, 2000)); // RP_MUT_RUNEV_REDACT_BEFORE_TRUNCATE_ARGV",
      "marker": "RP_MUT_RUNEV_REDACT_BEFORE_TRUNCATE_ARGV",
      "occurrences": 1,
      "testNamePattern": "straddling the argv cap",
      "expectRed": [
        {
          "test": [
            "sink-probe: run-evidence — a labelled secret straddling the argv cap is NOT redacted in run-evidence.jsonl (KNOWN DEFECT WD-SINK-TRUNC-RUNEV-ARGV)"
          ],
          "signal": "WD-SINK-TRUNC-RUNEV-ARGV: this probe pins a KNOWN-OPEN defect"
        }
      ]
    },
    {
      "id": "run-evidence-redact-before-truncate-field",
      "wp": "WP-secret-sink-wiring-probes",
      "criterion": "AC4",
      "why": "P6 pins Table S row S3's truncate-then-redact order in sanitizeRecord's scrub — a SEPARATE call site from S2's sanitizeArgv, fixing S2 alone leaves this open. Its pass is also what you would see if the site did no redaction at all, and only the paired CORRECT probe P5 rules that out — an inference, which is what ADR-0042 exists to replace. Swapping the cut and the scan makes the whole scalar field reach the detector, so PROBE_HEAD no longer survives and MARKER appears: exactly the condition P6 asserts is absent. testNamePattern scopes the phase to P6, because the mutation also changes what every other scalar field in this suite is scanned as, and none of those is this criterion's concern.",
      "file": "src/core/run-evidence.js",
      "find": "  const scrub = (v) => redactOnly(String(v == null ? '' : v).slice(0, 2000));",
      "replace": "  const scrub = (v) => redactOnly(String(v == null ? '' : v)).slice(0, 2000); // RP_MUT_RUNEV_REDACT_BEFORE_TRUNCATE_FIELD",
      "marker": "RP_MUT_RUNEV_REDACT_BEFORE_TRUNCATE_FIELD",
      "occurrences": 1,
      "testNamePattern": "straddling the scalar-field cap",
      "expectRed": [
        {
          "test": [
            "sink-probe: run-evidence — a labelled secret straddling the scalar-field cap is NOT redacted in run-evidence.jsonl (KNOWN DEFECT WD-SINK-TRUNC-RUNEV-FIELD)"
          ],
          "signal": "WD-SINK-TRUNC-RUNEV-FIELD: this probe pins a KNOWN-OPEN defect"
        }
      ]
    }
  ]
}
