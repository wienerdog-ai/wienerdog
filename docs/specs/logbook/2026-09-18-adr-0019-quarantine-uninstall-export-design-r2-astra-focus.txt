Adversarial DESIGN review, round 2, of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Round-1 raw and dispositions: docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r1-astra-raw.json and -design-review.md. Round 1 found the deleter raced a concurrent dream (recursive rm after an emptiness snapshot) and that K2 counted the shelf roots themselves (uninstall refused forever after emptying). Fixes: X1 is snapshot-free — enumerate state/'s direct children; every child NOT named `quarantine` → rmSync recursive (machine mechanics); no shelf FILE is ever deleted on any path; bottom-up non-recursive `rmdirSync(<state>/quarantine/redacted)` → `rmdirSync(<state>/quarantine)` → `rmdirSync(<state>)`, ENOENT/ENOTDIR = already gone, ENOTEMPTY/EEXIST or any other code = preserve, push to preservedQuarantine, stop climbing; rmdirSync makes emptiness-test and deletion one syscall so the window is closed, not narrowed; X2 snapshot-free; criterion 9 an interleaving test seamed via fs for both live sweeps; K2 excludes the two shelf roots (zero entries when each is a real directory), conservative counting elsewhere and for a symlink/file occupying either root.

Execute with mocked I/O (temp dirs only; never ~/.wienerdog; never uninstall outside a temp HOME): (1) X1's rmdirSync argument — on macOS/Linux rmdir on a non-empty dir → ENOTEMPTY (Linux may return EEXIST on some filesystems — listed); on Windows Node's rmdirSync of a non-empty directory → ENOTEMPTY; a directory containing only a `.DS_Store` or another hidden file → non-empty → preserved: is that stated (a user who "emptied" the shelf in Finder leaves .DS_Store, uninstall refuses — is the refusal message going to name that file so the user can act, given K2's conservative counting shows it as 1 entry)? (2) the order: rmdirSync of `quarantine/redacted` before `quarantine` — what if `quarantine/` holds a withheld file but `redacted/` is empty: redacted/ removed, quarantine/ preserved with only the withheld file — fine; what if `redacted/` is a symlink (K2's "symlink occupying either root" counts as an entry — but rmdirSync on a symlink-to-dir: ENOTDIR on POSIX — bucket?); (3) the "no shelf file is ever deleted" invariant vs step 2's recursive rmSync of OTHER children: can a shelf path live under another child (a symlink from `state/foo/` into `quarantine/` — rmSync recursive follows? Node's rmSync does NOT follow symlinks into directories (it unlinks the link) — confirm; and a hardlink to a shelf file under another child — rmSync deletes the link, the shelf inode survives — fine, stated?); (4) the interleaving criterion 9 — seamed via fs: does the seam actually place the copy between enumeration and the specific rmdirSync (which syscall is intercepted), and is it non-vacuous on both sweeps; (5) is the refuse-and-report contract (W1/W2, --yes does not proceed) unchanged and still consistent with the closed window (the refusal fires from the same rmdirSync ENOTEMPTY, so the plan-phase count and the act-phase refusal can disagree — a shelf that fills DURING the prompt: plan said EMPTY, act refuses — is that outcome stated and is the message the same?); (6) RED declarations (quse-carve-out-dropped now restores the recursive rm — anchor unique?), ACs, checklist, Security; owner items unchanged, no approval claims; cites at both ends. Report findings with file:line, band, LIGHT/HEAVY; verdict: approve / request-changes.
