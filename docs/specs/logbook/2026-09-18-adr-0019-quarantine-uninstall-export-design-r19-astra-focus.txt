Adversarial DESIGN review, round 19 (confirming, final), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-18 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..18}-astra-raw.json and -design-review.md. Round 18: an absent-shelf alias into a swept directory could be unlinked before a late copy landed through it; X17 carried a stale clause. Fixes: X19 clause (b) — retain a <state> alias when its resolved target lies inside any directory this uninstall sweeps (enumerated: <core>/logs, /schedules, /secrets, every child of <state> step 2 removes, every Table V replay target, and <core> itself); an alias outside every swept directory with no existing shelf is unlinked as at 5b77865f (sufficient by construction); AC 9 before-the-unlink + full-retry arm; AC 14's fixture target outside every swept directory; RED quse-alias-into-swept-dir-unlinked; X17's stale "class (ii) only" clause replaced; X10 and the security bullet updated; twenty-three declarations over thirteen criteria; nothing inside the three residuals for four rounds.

Final confirming round. Apply the three named residuals first — anything inside them is CLOSED. Then, read-only: (1) X19 (b)'s swept-set enumeration — is it complete? list every directory the uninstall path recursively removes or replays into (from uninstall.js and reverse()'s kinds, including <claude>/<codex> dirs where skills/hooks are removed — are those swept, and can a state alias target them?) and confirm each is in the enumeration or provably not a possible alias target; (2) the pair X19(a)+(b) vs AC 14: the fixture's alias target outside every swept directory — with `<core>` itself in the swept set, the only such targets are outside <core>, <claude>, <codex> entirely (e.g. /data/personal) — confirm AC 14's fixture says so and that the unlink branch is reachable; (3) twenty-three declarations: anchors unique; no cross-reddening; (4) whole-cell re-read of every X row and every W row for intra-cell drift (the architect's own process note); Tables K/X/V/W/Y/B, ACs 1–19, both mirror classes, Platform scope, owner items 1–4 (standing form, no approval claims), cites at both ends; (5) size verdict. If nothing about the product is wrong outside the residuals, say approve.
