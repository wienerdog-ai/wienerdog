DESIGN REVIEW (confirming round, fresh reviewer) of a new work-package spec; no product code has been written yet. Review ONLY docs/specs/WP-dream-lock-stale-owner-loud.md and the 2026-09-17 dream-lock-stale-owner / dream-lock-silent-stall logbook entries under docs/specs/logbook/. Exclude docs/marketing/, memory/research/ and userreports/ entirely. Four earlier rounds by a different model are preserved beside them as -design-r1..r4-raw.json with dispositions in the design-review entry; do not assume they were right. Background: src/core/dream/lock.js acquireLock keeps an EXPIRED single-run dream lock when its recorded local PID still exists (signal 0 success or EPERM returns busy; only ESRCH permits takeover), and src/cli/dream.js prints one line and exits 0 on busy, so the supervisor src/cli/run-job.js records success. A lock left by an uncleanly-killed dream whose PID is later held by an unrelated long-lived process therefore stops memory consolidation indefinitely with no alert. The spec does exactly three things: (a) a busy decline more than 6 hours past the lock's deadline throws (exit 1 - the existing alert and email path) with a byte-exact plain-language message; (b) a finite deadline more than an absolute 24 hours ahead of now is classified as the existing owner-unknown; (c) an exit-code doc comment is corrected. No takeover decision changes. Challenge: (1) can the loud gate fire on a legitimately live dream, or fail to fire when it should; (2) is the byte-exact message safe and actionable for a non-developer - it says restart first (the next run normally takes the lock over by itself), and only if the same message reappears, first make sure no dream is running at that moment and then delete the named file; the spec prices as a residual that this rests on a fallible human check - is that residual stated truthfully and is there a cheaper safer instruction; (3) does the message interpolate anything read from the lock file; (4) the spec claims NO hard wall-clock maximum for silence, only a sampling rule with two worked cases (54 h, and 55 h across a daylight-saving fall-back) - check the arithmetic against run-job's last_success and catchUp logic; (5) does the proposed ADR-0012 amendment block contradict Table L of docs/specs/done/WP-dream-live-owner-lock.md or the 2026-09-15 amendment it amends; (6) can each acceptance criterion and RED-proof declaration discriminate a correct implementation from a plausible wrong one, and is every consumer of acquireLock's result listed; (7) do the file:line citations resolve on this tree. Report findings about PRODUCT behaviour in the main list; list verification-machinery findings separately, marked as machinery. A risk the spec already names and prices as a residual or owner item is not a finding unless its statement is untrue. Owner items are recommendations with overrule costs by design. State what you executed versus what you only read.
