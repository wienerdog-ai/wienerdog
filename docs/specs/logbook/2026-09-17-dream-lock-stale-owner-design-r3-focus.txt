DESIGN REVIEW ROUND 3 of a work-package spec; no product code has been written yet. Review ONLY: docs/specs/WP-dream-lock-stale-owner-loud.md and docs/specs/logbook/2026-09-17-dream-lock-stale-owner-design-review.md (prior raws for reference: docs/specs/logbook/2026-09-17-dream-lock-stale-owner-design-r1-raw.json and -r2-raw.json). Exclude docs/marketing/, memory/research/ and userreports/ entirely. Background: src/core/dream/lock.js keeps an EXPIRED single-run dream lock when its recorded local PID still exists, and src/cli/dream.js exits 0 on that busy decline, so a lock left by an uncleanly-killed dream whose PID is later reused stops memory consolidation forever with no alert. The spec now does exactly three things: a busy decline more than 6 h past the lock's deadline throws (exit 1, existing alert path) with a byte-exact plain-language message; a finite deadline more than an ABSOLUTE 24 h ahead of now is classified as the existing owner-unknown; and the exit-code doc comment is corrected. No takeover decision changes. PART ONE - verify each round-2 finding is GENUINELY fixed in substance, not re-worded: (R2-A1) "Restart-then-delete can delete a newly acquired live lock" - the message now says restart first, Wienerdog normally clears the lock by itself at its next run, and ONLY if the same message appears again after a restart delete the named file at a time when no dream is about to start; the remaining race is named as a residual and priced in an owner item. Is the reasoning sound that a lock which still produces this message after a restart is necessarily still the dead owner's record, and is the residual stated truthfully? (R2-B1) "The deadline cap still inherits the unbounded timeout" - the cap is now absolute, no multiplication; check for any remaining config dependence or overflow, and check the stated worst-case silence bound (about 30 h, at most two lost nights). (R2-B2) "The new busy shape breaks a test outside the deliverables boundary" - tests/unit/dream-pipeline.test.js is back in Deliverables and the consumer inventory is a full grep; check it is complete. PART TWO - this is a closure round: report only findings about PRODUCT behaviour a user or a consuming process would observe (wrong alert, missed alert, unsafe instruction, data loss, a legitimate run refused). Findings about the spec's own verification machinery should be listed separately and marked as machinery. Do all file:line citations resolve on this tree. Owner items are recommendations with overrule costs by design; disagreeing with one is a welcome finding, their being unratified is not. Report what you executed versus what you only read.
