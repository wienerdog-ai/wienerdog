Adversarial DESIGN review of two chained work-package specs, not of code: docs/specs/WP-ep2-n2-rehome.md (S; re-homes Table N row N2 / B10 / M-48 of docs/specs/done/WP-secret-fence-ep2-redact-arm.md to the real locus, plus a JSDoc comment fix in src/core/dream/validate.js:1159-1173) and docs/specs/WP-ep2-prune-once-per-run-test.md (S, depends on the first; one test in tests/unit/dream-pipeline.test.js asserting the retention prune runs exactly once per run and only after a completed redaction, with one ADR-0042 RED declaration whose mutation duplicates `gates.pruneRedacted();` at src/cli/dream.js:1102). Background: the July package they replace was found stale (docs/specs/logbook/2026-09-18-ep2-retention-prune-timing-test-stale.md).

Reason over the code and execute with mocked I/O where useful. Verify: (1) the seam (Table P row P-6): observing `fs.rmSync` calls under <state>/quarantine/redacted/ whose basename is one of the fixture's seeded names, recorded AND neutralized — is neutralizing sound (the spec says a performed first prune leaves the dir at the cap so a second invocation is invisible: check pruneRedactedOriginals at validate.js:1174-1190), can the neutralized rm leave a state that makes the assertion vacuous or flaky, and does scoping to seeded basenames really exclude quarantinePreserve's temp cleanup? (2) Does the test discriminate the property the contract states — "exactly once per run AND only after a completed redaction" — or only cardinality? Check the zero-redaction leg. (3) The rejected seams in Table S (S-1 wrap makeGates impossible because dream.js:33-38 destructures at require time; S-2 readdirSync count contaminated 3 reads) — are the rejections correct, and is there a cheaper seam the spec missed (e.g. an existing dependency-injection opt on dream.run)? (4) The rehome WP: is every mirror of the contract enumerated (Table M, 16 surfaces) — grep the Done spec and src for the old locus wording and the routed slug and find any the table misses; is editing a Done spec's canonical table (row N2) legitimate under ADR-0031, and does the spec bound the validate.js edit to comment text only in a way boundary-check.js and a reviewer can verify? (5) Owner item O-1 (one slug in ADR-0036 row A3, an OWNER-SIGNED ADR): is the standing form respected and nothing claims approval? Check cites at both ends, the Mirrored Surface Checklists, Security checklists. Report each finding with file:line, what the spec says, what the code says, band A/B/C and LIGHT/HEAVY. End with a verdict per spec: approve / request-changes.
