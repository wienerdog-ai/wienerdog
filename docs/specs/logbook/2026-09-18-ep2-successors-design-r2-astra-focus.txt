Adversarial DESIGN review, round 2 (confirming), of docs/specs/WP-ep2-n2-rehome.md and docs/specs/WP-ep2-prune-once-per-run-test.md (Draft). Round-1 raw and dispositions: docs/specs/logbook/2026-09-18-ep2-successors-design-r1-astra-raw.json and 2026-09-18-ep2-successors-design-review.md. Round 1's finding: with 49 seeded files the detector could not distinguish per-redaction pruning from once-per-run pruning. Fix: seed 50 (directory ends two over the cap), expected delete counts — correct once-per-run 2, duplicate-at-end mutation 4, per-redaction-after-each-completed-redaction with the end call kept 5, true per-redaction move 3 — measured by hand on the whole suite (only the new test reddens in each mutated run); the declaration now carries two proofs (ep2-prune-runs-once-per-run, ep2-prune-not-once-per-redaction); Table P rows P-4..P-10 rewritten; the seed-count↔expected-count pairing registered in the Mirrored Surface Checklist.

Execute against src/core/dream/validate.js pruneRedactedOriginals (~:1174-1190, cap REDACTED_RETENTION_CAP = 50 at tests/unit/dream-validate.test.js:663 / the production constant) with simulated directory entries: (1) confirm the four schedules yield exactly 2 / 4 / 5 / 3 intercepted deletes under the spec's seam (fs.rmSync under <state>/quarantine/redacted/ scoped to seeded basenames, recorded and neutralized); find any schedule that violates N2 yet yields 2 (e.g. prune before the first redaction, prune on a non-redaction path, a prune that deletes two at once); (2) does the second proof's mutation (insert a call after completedRedactions += 1 at validate.js:1418 with the end call kept) correspond to a plausible regression — and since the "true move" (3 deletes) spans two files and cannot be a RED declaration, is the residual named honestly? (3) neutralized rm: any way the recorded-not-performed delete makes the run's later steps diverge (the redacted dir stays over cap — does anything else read it and change behaviour: scanPrivateModes, warnings, the banner)? (4) the rehome WP unchanged since round 1 except cite fixes — confirm nothing regressed, and that owner item O-1 (one slug in the OWNER-SIGNED ADR-0036 row A3) is in the standing form with no approval claim. Cites at both ends. Report findings with file:line, band, LIGHT/HEAVY; end with a verdict per spec: approve / request-changes.
