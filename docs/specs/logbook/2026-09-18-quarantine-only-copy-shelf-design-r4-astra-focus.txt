Adversarial DESIGN review, round 4 (confirming), of docs/specs/WP-quarantine-only-copy-shelf.md (Draft, S). Rounds 1-3 raws and dispositions in docs/specs/logbook/2026-09-18-quarantine-only-copy-shelf-design-r{1,2,3}-astra-raw.json and -design-review.md. Round 3 found C1's lossless claim time-dependent. Fix: C1 is "a duplicate at creation time only" (the twin is not pinned — promote.js:600-603 and docs/runbooks/secret-incident.md:40-48 tell the owner to delete it); row O2 now leads with the general form: at prune time NO class is provably lossless because pruneRedactedOriginals (validate.js:1176) performs no identity check — readdirSync, date regex, statSync, rmSync — so every identity fact is creation-time and the prune runs in a later process; O8/owner item 1 reworded: the lossless case is a condition the prune never checks, not a class; [OC-4] added (equal buffers → identical === true, rmSync forced to throw for the redacted path, both copies present, shelf copy absent from the preservation record, twin deleted as the runbook instructs, prune fires, whole-tree walk finds the bytes nowhere — asserts the loss, does not prevent it). The surface is declared frozen: four tests, one RED declaration, two doc clauses, no src/; the logbook carries a convergence note.

This is a confirming round: review ONLY whether the round-3 fix is correct and complete and whether anything regressed. Execute with mocked I/O (temp state dir): (1) O2's general form — confirm against validate.js:1176-1195 that the prune opens no file and consults no record, so the "no class is provably lossless at prune time" statement is exactly true; (2) [OC-4] non-vacuity — with rmSync forced to throw only for the redacted path, does the gate actually reach identical === true and the else-branch that skips the record (:1498-1504), and is "absent from the preservation record" observable (where is the record; is it written before or after the delete attempt); (3) the frozen surface: does the RED declaration still redden [OC-1] alone with [OC-2]/[OC-3]/[OC-4] present (their assertions don't touch the pruned bytes' absence — verify); (4) the two doc clauses' literal text: true under all four classes, and the runbook's instruction to delete the twin now sits next to a clause saying the shelf copy may be the only copy — is the pair coherent for a reader, or does the runbook need one more sentence (that would be a third clause — if so, is it a named residual or in-surface)? (5) owner items in the standing form, no approval claims; cites at both ends; all template sections and Table O intact. If nothing about the product is wrong, say approve; findings with file:line, band, LIGHT/HEAVY.
