DESIGN REVIEW ROUND 3 of a work-package spec; no product code has been written yet. Review ONLY: docs/specs/WP-dream-digest-omits-own-job-alerts.md and docs/specs/logbook/2026-09-17-dream-digest-omits-design-review.md (prior raws for reference: docs/specs/logbook/2026-09-17-dream-digest-omits-design-r1-raw.json and -r2-raw.json). Exclude docs/marketing/, memory/research/ and userreports/ entirely. The spec proposes that the nightly dream job, when it re-renders state/digest.md at the END of a successful run (one call site only), omits the "job has failed" alert records for its own job, identified by WIENERDOG_JOB resolving via findJob to run: builtin:dream AND a valid supervisor-minted WIENERDOG_DREAM_RUN_TOKEN; every other render is unfiltered; an in-process failure after the filtered render triggers exactly one unfiltered re-render with the original error winning. PART ONE - verify each round-2 finding is GENUINELY fixed in substance, not re-worded: (R2-1) "The B1/B2-only narrowing omits ordinary job failures and has no finite bound" - the revision withdraws the sole-narrowing and finite-bound claims and instead states a pre-existing product property: a failed dream never reaches the final render, run-job appends the failure record only after the child exits, so a dream-job failure callout reaches digest.md only via an attended sync render or a later dream's conditional early render; doctor does not read alerts at all; the timely channels are the fail-loud email and alerts.jsonl / `wienerdog alerts`; whether that is acceptable is an unresolved owner item. Check that statement against src/cli/run-job.js, src/cli/dream.js, src/cli/sync.js, src/cli/doctor.js and src/cli/alerts.js: is it TRUE, is it complete, and does the new end-to-end criterion AC6e pin it? (R2-2) "A failed recovery render is swallowed and leaves no diagnostic" - now one fixed-text non-interpolating stderr line, original error still wins, AC7 split into AC7a/AC7b. Check it. PART TWO - this is a closure round: report only findings about PRODUCT behaviour a user or a consuming process would observe (a genuine alert hidden that the spec does not name, a wrong job's alert dropped, a corrupted or double render, a masked error). Findings about the spec's own verification machinery should be listed separately and marked as machinery. Do all file:line citations resolve on this tree. Owner items are recommendations with overrule costs by design; disagreeing with one is a welcome finding, their being unratified is not. Report what you executed versus what you only read.
