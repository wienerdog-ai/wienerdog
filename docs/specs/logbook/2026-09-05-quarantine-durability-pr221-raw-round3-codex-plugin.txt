# Raw gate output — Codex plugin native review (codex-companion.mjs review --wait --base main), PR #221 round 3, wp/quarantine-preserve-durability at 85514b9a (base main = bb58e398). Committed BEFORE adjudication. git status --porcelain identical before and after.

# Codex Review

Target: branch diff against main

The durability branch causes preservation to fail systematically on an included non-Windows platform whose fsync requirements are incompatible with the directory descriptors used.

Review comment:

- [P2] Do not enable read-only directory fsync on AIX — /Users/gyulafeher/Documents/Claude_Projects/wienerdog-wt/quarantine-preserve-durability/src/core/dream/validate.js:647-647
  On AIX, this predicate enables durability, but `DIR_OPEN_FLAGS` opens every directory `O_RDONLY`; the work-package contract itself notes that AIX requires a write-open descriptor for `fsync` ([Table F row F5](docs/specs/WP-quarantine-preserve-durability.md#L694)). Consequently the first directory flush fails, the artifact is removed, and any withhold preservation aborts the dream. Restrict this branch to platforms supporting the chosen directory-flush mechanism or provide an AIX-compatible path.
