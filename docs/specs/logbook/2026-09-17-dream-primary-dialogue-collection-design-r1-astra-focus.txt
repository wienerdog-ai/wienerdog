DESIGN REVIEW of a NEW work-package spec; no product code has been written yet. Review ONLY docs/specs/WP-dream-primary-dialogue-collection.md and docs/specs/logbook/2026-09-17-dream-primary-dialogue-split.md. (Two sibling specs changed in the same diff - WP-dream-primary-dialogue-projection.md, which this one depends on, and WP-dream-primary-dialogue-filter.md - read them only for context; a separate review covers the projection spec.) Exclude docs/marketing/, memory/research/ and userreports/ entirely. Background: Wienerdog's nightly "dream" job collects session transcripts into private scratch extracts under a byte budget X = dream_max_input_bytes (src/core/dream/scratch.js collectExtracts: newest-first, whole extracts, five disjoint exclusion arms), runs one consolidation model over them in a private workspace, then code validates and promotes the result (src/cli/dream.js, src/core/dream/validate.js, promote.js). A sibling package adds parsePrimaryWithOutcome returning {extract, gateExtract, intakeBytes, parse}. THIS package wires it in: the collector opts into the primary-dialogue projection; ADMISSION against X is measured on intakeBytes - the raw capped extract's size, byte-identical to today's quantity - so the admitted session set is UNCHANGED by construction while what is WRITTEN to scratch and shown to the model is the much smaller primary dialogue; the text-free code-only gateExtract is passed to the existing extractsBySession consumer so the skill-learning authorization gate still runs on the ORIGINAL timeline and skill-invocation geometry; the model no longer receives any metadata extracted from tool records (no skill_invocations array), and the dream skill (skills/wienerdog-dream/SKILL.md, digest-pinned in src/core/runtime-skill-digests.json) is updated to discover learnings from dialogue only; Codex subagent rollouts supply no dialogue; a dated ADR-0020 amendment is a deliverable whose Status line reads "PROPOSED - awaiting owner signature". Known and NOT fixed by design (say so only if its statement is untrue): every admitted session is marked processed whether or not the model read it. Challenge, EXECUTING the real collector, ledger and validate code with mocked I/O where you can: (1) the central claim - is the admitted set REALLY unchanged: trace every place a size is measured or memoised (the oversized memo in the ledger, the capacity stop, the individually-oversized skip, the soft preprocessing deadline) and find any where the projected size leaks into an admission decision; what does dry-run report; (2) the AUTHORIZATION gate - can filtering create an invocation, lower the gate's taint, convert Codex evidence into qualifying Claude evidence, or let a new skill learning through because its supporting tool evidence was removed from what the gate sees; is gateExtract ever written to a model-visible directory; what happens when an admitted session projects to an EMPTY messages array; (3) PROVENANCE end to end - the dream skill must set a candidate's derived_from_untrusted true if any supporting message is true or unknown and must never infer false from the mere absence of tool roles; is that enforceable by code or only by prompt, and does the spec say which; (4) the skill change and its digest: is the regenerated digest scoped to the one changed skill, and what does changing the dream prompt/skill do to the scheduler descriptor and an installed user's authorization at upgrade; (5) privacy and cleanup of scratch under the existing lock and teardown, including the new final filtered digest render that now runs as the last statement of the locked body; (6) interaction with the in-flight WP-dream-report-run-skips, which edits scratch.js, promote.js and dream.js - do its six report bullets stay TRUE under this package; (7) the proposed ADR-0020 amendment text - does it contradict ADR-0020 as it stands; (8) does each acceptance criterion and RED-proof declaration discriminate (expectRed sets are marked "derived" by design), including the offline admitted-count measurement on a disposable ledger copy; (9) do the file:line citations resolve on this tree. Report findings about PRODUCT or SECURITY behaviour in the main list; list verification-machinery findings separately, marked as machinery. A risk the spec already names and prices as a residual or owner item is not a finding unless its statement is untrue. Owner items are recommendations with overrule costs by design. State what you executed versus what you only read.
