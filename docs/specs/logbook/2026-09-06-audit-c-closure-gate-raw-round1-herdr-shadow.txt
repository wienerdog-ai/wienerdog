# raw — hermetic Codex shadow (codex exec, read-only sandbox, CODEX_HOME=~/.codex-review-home, gpt-5.6-sol high) — e2
# tip reviewed: dc419e9409cfe244a0b2182f84691e9a17ae070a (detached worktree) ; base main = eedd8783fcb3df7eabde3bd385755455c629821d ; committed pre-adjudication
# codex exec rc=0 ; porcelain before/after: identical

{"target":"dc419e9409cfe244a0b2182f84691e9a17ae070a","verdict":"needs-attention","executed":["`git status --short --branch`, `git rev-parse HEAD/main`, requested diff stat, and full scoped diff — exit 0; target/base matched","Read all four scoped files in line-bounded chunks — exit 0","Checked all four owner specs under `docs/specs/done/` for `status: Done` — exit 0","Verified cited commits/PR merges with `git cat-file`, `git show`, and `git merge-base --is-ancestor` — all exit 0","Inspected every cited code range and residual home with `nl`, `sed`, and `rg` — exit 0","Re-ran read-only equivalents of currency V3/V4, pinning V4/V5, seam V3/V4a/V5, and D3 import/consumer checks — all exit 0; observed 13996 bytes/10 placeholders, 525-byte release step, exact proof IDs, and no D3 consumer","Parsed all RED declarations read-only — exit 0; 64 proofs and all six cited roll-up memberships present","Re-ran seam V4 base guard — exit 1 as documented, with merge-base `eedd8783`","Confirmed the Done spec is byte-identical to the base after removing only appended lines 400–431 — exit 0","`npm test` — exit 1 before tests: sandbox denied `mkdtemp` with EPERM","`node scripts/red-proofs.js` — exit 1 before proofs: sandbox denied sandbox creation with EPERM","`git diff --check` — exit 0; `npm run lint` — exit 1 because markdownlint attempted unavailable registry access (ENOTFOUND); frontmatter check passed"],"summary":"Do not ship this record unchanged. The owner WPs, SHAs, residual homes, available V-steps, and retiring implementation all checked out, but the closure basis contains one semantically non-retiring citation, and O7 recreates the append-only duplication defect this same record previously corrected.","findings":[{"title":"D1(a)'s cited range does not demonstrate its stated retirement","file":"docs/specs/logbook/2026-09-06-audit-group-c-closure.md","line_start":27,"line_end":27,"materiality_band":"C","confidence":0.97,"classification":"measured-claim","body":"The row says D1(a) is retired by the promote-in inversion, but its only code citation is `promote.js:99-111`. Executing `nl -ba src/core/dream/promote.js` showed that range only declares `INSTRUCTION_BASENAMES` and begins the next JSDoc; it does not show the inversion, the Set's enforcement, or the writable-tier gate. A direct `makeAdmit` probe did confirm that root `CLAUDE.md` is currently refused, so the behavior is not disproven, but the claimed retiring citation does not retire what the row says. That conflicts with the basis file's assertion that every citation was checked at both ends.","recommendation":"Replace or supplement this citation with the actual inversion/enforcement path and a named test or read-only probe demonstrating that brain output reaches the vault only through promotion and that a vault-root instruction path cannot be promoted."},{"title":"O7 calls itself citation-only while duplicating the governing facts and reversal contract","file":"docs/specs/logbook/2026-09-05-owner-rulings-git-env-pinning-queue.md","line_start":277,"line_end":289,"materiality_band":"C","confidence":0.99,"classification":"record","body":"Lines 277–284 say O7 is citation-only and “restates none” of the governing act, then restate the four-WP green measurement, retiring-code evidence, and three accepted residuals. Lines 286–289 also reproduce the full overrule cost from the governing spec's lines 424–430. I compared these sections directly. This is the exact append-only drift mechanism that the same file's O4/O5 and O6 corrections at lines 218–269 explicitly withdrew: substantive copies require later dated corrections when the authority changes.","recommendation":"Keep O7 to adoption status and pointers to the governing act and basis file. Remove the summary of measured contents and the repeated overrule cost before this append ships."}]}