Adversarial DESIGN review, round 3, of docs/specs/WP-ledger-retry-parse-threw-on-upgrade.md (M) together with its siblings docs/specs/WP-transcript-parsers-harden-text-values.md and docs/specs/WP-doctor-recognizes-parse-threw.md (Draft). Rounds 1-2 raws and dispositions: docs/specs/logbook/2026-09-18-parse-threw-successors-design-r{1,2}-astra-raw.json and 2026-09-18-parse-threw-successors-design-review.md. Round 2's finding: deleting the parse-threw quarantine record lands a restored file with mtime ≤ baseline on skip-processed and erases its warning. Fix: CONVERT, never delete — the record becomes {fingerprint unchanged, outcome 'deferred', reason 'parse-threw', updated_at, harness}; selectState consults baseline_mtime only on the no-record path (src/core/dream/ledger.js:260-261) and `case 'deferred': return 'select'` (:255-256) already means retry; secretDeferralCount's deferred arm returns 0 for a non-secret-revert reason (:288-289); activeQuarantines (:390-399) selects outcome === 'quarantined' only, so the record leaves the banner for the span of the run and the run's outcome rewrites it; new rows A9/A10, AC1 requires a corpus file at or below the baseline, two new RED declarations (rpt-deletion-instead-of-deferral, rpt-reason-dropped-on-conversion); the one-shot upgrade gate is unchanged from round 2.

Execute against ledger.js with mocked I/O: (1) every consumer of a ledger record — selectState, secretDeferralCount, activeQuarantines, readDeferred/deferred accounting in scratch.js and the run-skip counts in promote.js (the six byte-exact bullets of WP-dream-report-run-skips), warnings.js GROUPS, doctor counts, the banner decay — with a `deferred` record whose reason is 'parse-threw' (a combination the tree has never held): does any consumer misclassify it (e.g. counted as read-deferred or as deferred-for-capacity in the report; rendered under a wrong warnings heading; counted by doctor under a wrong row), and does the run's outcome ALWAYS rewrite it (what if the run stops before reaching that file — capacity/deadline stop — is the record left as deferred/parse-threw indefinitely, and is that harmless)? (2) the one-shot gate: where it persists, crash between convert and gate-write, downgrade, dry run, a second upgrade later; (3) crash in the window where the record has left the banner: is "warnings.md lags one run" really permitted by ADR-0023 Amendment 2 — read it; (4) the two new RED declarations' mutations and whether the at-or-below-baseline corpus file makes AC1 non-vacuous; (5) owner item 1 (ADR-0023 amendment for the new transition) in the standing form, no approval claims; the transcripts and doctor specs unchanged — confirm. Cites at both ends. Report findings with file:line, band, LIGHT/HEAVY; verdict per spec: approve / request-changes.
