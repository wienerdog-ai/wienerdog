# Raw gate output — Codex plugin adversarial-review, round 2, docs/wp-dot-segment-denial at 82506ab6 (base main = 29c61d03). Committed BEFORE adjudication. Focus text reproduced at the end.

# Codex Adversarial Review

Target: branch diff against main
Verdict: needs-attention

NO-SHIP. Table E’s ten stated outcomes are correct: E1–E3 refuse, E4 admits, E5–E7 fall back, E8 is unchanged, E9 emits defaults, and E10 is unchanged. Whitespace handling is consistent: resolved paths preserve interior leading spaces and the vault-write primitive accepts them, while coerceScalar and pick trim only at their documented sites. E5 replaces the removed reader-side coverage; the retained report residual remains true but becomes dormant. RED partitioning is sound: C1 produces exactly T1–T3, C2 exactly T4–T5, T6 stays green, and adopt-e2e is outside the declared suite. However, a cheap ASCII-only predicate passed all 686 V2 cases while violating the class. Boundary check, ADR-0004, and implementation size support keeping one atomic WP rather than splitting at B1/B2-B3.

Findings:
- [high] [A] All three oracles accept an ASCII-only under-implementation (docs/specs/WP-dot-segment-denial.md:484-507)
  Every required dot-prefixed segment has only ASCII characters after U+002E: the held-out family generates ASCII letters, Table D uses ASCII names, and Table E’s Unicode cases put the non-ASCII look-alike in the first position and expect legality. I executed a candidate equivalent to `seg.startsWith('.') && /^[\x00-\x7F]+$/.test(seg)` at B1, B2, and B3. It scored `312/312 | 234/234 | 7/7 | 12/12 | 87/87 | 34/34`, exit 0, yet admits or emits `.éclair`, `.éprojects`, and any other ASCII-dot/non-ASCII-tail segment. This is a simple fourth attack, not the deliberately union-enumerated residual described by the draft.
  Recommendation: Add hand-written ASCII-dot/non-ASCII-tail cases at all three enforcement points—for example B1 `.éclair/x.md`, B2 `.éclair`, and B3 `.éprojects`—and require the ASCII-only mutant to fail V2.
- [low] [C] V3 still contradicts criterion 5’s one-line roll-up contract (docs/specs/WP-dot-segment-denial.md:922-927)
  Both declarations use the same `(wp, criterion)` pair, so `rollUp` emits one criterion line naming both IDs, exactly as criterion 5 now says. The V3 commentary still tells reviewers to expect “the two roll-up lines,” leaving the Round 1 wording fix incomplete and making correct output look deficient.
  Recommendation: Change the V3 commentary to require one roll-up line for `WP-dot-segment-denial criterion 1` naming both declaration IDs.

Next steps:
- Execution ledger: baseline `pwd`, `git status --porcelain`, `git diff --name-status main...HEAD`, `git diff --stat main...HEAD`, and `wc -l` all exited 0; the baseline status was empty. Full numbered reads of the WP (three commands) and logbook (two commands) exited 0. Targeted `rg` and numbered reads of promote.js, layout.js, layout-infer.js, frontmatter.js, vault-write.js, red-proofs.js, tests/run.js, package.json, dream-promote.test.js, adopt-e2e.test.js, spec-authoring.md, boundary-check.js, and ADR-0004 all exited 0.
- Exact extracted V2 (`sed -n '810,916p' ... | sh`) exited 1 because the read-only sandbox denied `mkdtemp` before a tally. Three preliminary in-memory-harness launch attempts exited 1 due heredoc/quoting construction and made no writes. The corrected in-memory runs exited: untouched tree 1 with the requested `admit 0/312 | reader 0/234 | producer 1/7 | over 12/12 | handoff 36/87 | boundary 16/34`; correct predicate 0 with all counts perfect; ASCII-only predicate 0 with all counts perfect.
- `node scripts/boundary-check.js ...` over all declared deliverables and changed logbook files exited 0. `git diff --check main...HEAD -- <two in-scope files>` exited 0. `npm run lint` exited 0. Final `git status --porcelain` exited 0 and remained empty, identical to baseline.
- Scope objections, excluded from the verdict: none.

---- focus text ----
SCOPE: docs/specs/WP-dot-segment-denial.md (Draft, 1014 lines) and docs/specs/logbook/2026-09-04-dot-segment-denial-design-gate-rounds.md, as changed relative to main. ROUND 2 — the closing confirmation. The WP: the class rule "no dot-prefixed segment is admitted or emitted" at three enforcement points (makeAdmit loop placed LAST; isSafeRelativePath in layout.js with the copied duplicate in layout-infer.js deleted; thereby inferLayout/adopt --yes), graded by THREE oracles — a seeded held-out family (V2), the hand-transcribed Table D (29 handoff paths), and the NEW Table E fixed boundary matrix (ten hand-written rows: refusals and legal positives) — with two RED proofs (Table C) over six named test identities partitioned at the B1 / B2-B3 seam.
ROUND 1 FINDINGS (verify each is GENUINELY fixed): (R1-A, converged) both grading families shared the `.`+letters shape, so `length > 1 && startsWith('.')` and an NFKC-normalising predicate scored a perfect V2 while admitting bare `.` and over-denying whitespace-led and U+2024/U+FF0E segments. Fix claimed: Table E in both V2 and the suite; whitespace/Unicode legality decided per trim site from the code (coerceScalar trims the whole config value, pick trims a picked directory name, the validators trim nothing; NFC does not fold look-alikes, NFKC out of scope); eight-tree measurement showing each attack caught by exactly one oracle. (R1-B, plugin) C1's expectRed omitted the Table D oracle's failures. Fix claimed: six named test identities (T1 B1 held-out, T2 B1 boundary, T3 Table D oracle → C1; T4 B2/B3 held-out, T5 B2/B3 boundary → C2; T6 no-over-denial in neither), band markers per assertion for `signal`, criterion 5 reworded to rollUp's one-line-per-criterion output.
CHALLENGE: (a) Table E — for each of the ten rows, is the expected verdict RIGHT per enforcement point under the actual code on this tree (read fold/foldedSegments, isSafeRelativePath, coerceScalar, pick, inferLayout's reports_dir special case): any row that a CORRECT implementation would fail (over-denial or under-denial encoded into the oracle), any row whose expected result differs between B1, B2 and B3 without the table saying so? (b) is the whitespace decision consistent with how the dream actually calls makeAdmit (the resolved relative path) — can a leading-space segment reach makeAdmit at all, and does a "legal" verdict there commit the implementer to anything the vault-write primitive refuses elsewhere? (c) the union-fitted residual — is it honestly bounded now, and is there a FOURTH cheap attack that passes all three oracles (e.g. a predicate keyed on segment LENGTH ranges, on the presence of a letter after the dot, on ASCII-only)? Build it in memory and score it against V2's three oracles as written if the sandbox permits, else reason and say so. (d) the six identities — read scripts/red-proofs.js evaluateRed/rollUp and judge: under C1 does EXACTLY {T1,T2,T3} fail with own-body ERR_ASSERTION and nothing else (T6? the adopt-e2e integration test is in a different suite file — is it selected?), under C2 exactly {T4,T5}; is the `signal` band-marker scheme something the runner can match; does `criterion: 1` on both proofs produce the rollUp line criterion 5 quotes? (e) the report-fallback test edit — three values removed; does Table E row E5's fallback assertion fully replace their reader-side coverage, and is the residual comment the test keeps still true after the edit? (f) V2 as written: extract from the fenced block and run on this untouched tree — expect `admit 0/312 | reader 0/234 | producer 1/7 | over 12/12 | handoff 36/87 | boundary 16/34`, rc=1 (if the sandbox denies temp writes, run it in memory and say so). (g) Deliverables/boundary/ADR-0004/size after the growth to 1014 lines — still one WP, or should it split at the B1 / B2-B3 seam the identities already draw?
OUT OF SCOPE — file as scope objections in a separate routed section, do NOT count toward the verdict: the Dispatch-precondition item (silent fallback), the 2026-08-05 ruling's class shape, the basename list, NFKC normalisation (explicitly Out of scope), the adjudicated round-1 dispositions, any code change, and every file other than the two in scope.
REPORT: state what you EXECUTED (each command with exit status), then findings with file:line and a materiality band (A: a criterion a wrong implementation passes, silent wrong behaviour, or an over-denial encoded into an oracle; B: caught downstream; C: hygiene), then the verdict. Review is READ-ONLY: git status --porcelain must be identical before and after.
