# Raw gate output — Codex plugin adversarial-review, round 1, docs/wp-instruction-basename-currency at f43f6c62 (base main = 705ae286). Committed BEFORE adjudication. Focus text reproduced at the end.

# Codex Adversarial Review

Target: branch diff against main
Verdict: needs-attention

Do not dispatch yet: the verification design permits a consistently omitted deny row to pass, and the copied inventory would overstate current dot-path protection. Executed: initial `git status --porcelain=v1 && git diff main...HEAD --stat && git rev-parse --show-toplevel` (0); V1 (1, expected missing-deliverable RED); V2 (1, expected missing-input RED); V3 (1, expected absent-obligation RED); two `nl`/`sed`/`rg` inspection batches (0 each); heredoc reachability probe (1, sandbox prohibited its temporary file); equivalent read-only Table-A reachability probe (0: 9/9 reachable, 0 dead rows); Table-B admission probe (0); final `git status --porcelain=v1` (0). Initial and final status were identical: `?? node_modules`. V4 was not run as instructed; source inspection confirms the runner requires an applied marker and own-body `ERR_ASSERTION` failures. No out-of-scope objections were counted.

Findings:
- [high] [A] A consistently omitted deny row passes every automated gate (docs/specs/WP-instruction-basename-currency.md:429-451)
  The inventory is both the test oracle and the source-count oracle. Criterion 2 only requires failure when zero rows parse, while criterion 3 compares the source literal count with that same parsed subset. V1 merely prints the DENY count and V2 has no fixed expected count or comparison with canonical Table A. Therefore an implementer can accidentally omit or mis-prefix one canonical row—e.g. `WARP.md`—and omit the matching source/fixture entry; the remaining rows pass criteria 2–3, and the RED proof still succeeds by reverting the remaining additions to the four-name baseline. Criterion 1 would be textually violated, but no verification checks its promised verbatim copy, including Tables B and C. This leaves threat-M7 behavior open while all runnable evidence is green.
  Recommendation: Add an independent acceptance oracle: compare the shipped table regions against the spec’s canonical regions with only the two permitted date substitutions, reject malformed or duplicate rows, and assert the expected nine unique DENY basenames for this WP. Ensure the RED proof targets that independently pinned test.
- [medium] [B] The verbatim Table B preamble falsely says handoff paths are already denied (docs/specs/WP-instruction-basename-currency.md:277-282)
  The copied preamble states that every HANDOFF row “is denied instead” by the dot-segment rule. That rule belongs to the subsequent WP and is not present now: the production predicate currently admits tier-local `.github/copilot-instructions.md`, `.clinerules/rule.md`, `.roo/rules/rule.md`, and `.continue/rules/rule.md`; only existing `.claude`/`.codex` segments and non-`.md` shapes are independently refused. Because this sentence is inside the mandatory verbatim copy region, the newly shipped canonical inventory would immediately overstate security coverage.
  Recommendation: Change the copied preamble to say HANDOFF rows are assigned to `WP-dot-segment-denial` and remain unprotected by that class until it lands; separately identify rows already refused by current segment or extension checks.
- [medium] [B] V3 does not discriminate the required maintenance obligation (docs/specs/WP-instruction-basename-currency.md:495-497)
  V3 only greps for the inventory pathname. A numbered step such as “Read docs/instruction-file-inventory.md” passes while omitting the release-maintainer owner, MINOR-version trigger, citation refresh, date refresh, and same-PR source/test update required by criterion 4 and Table C. That wrong implementation can silently let the deny list become stale again.
  Recommendation: Replace the pathname-only grep with a check that identifies one numbered runbook step and asserts the literal path, MINOR trigger, owner role, citation/date refresh, and same-PR code/test synchronization obligations.

Next steps:
- Close the independent-oracle gap before handing the WP to the implementer.
- Correct the copied Table B tense without implementing the out-of-scope dot-segment rule.
- Strengthen V3, then rerun the untouched-tree RED rehearsal.

---- focus text ----
SCOPE: docs/specs/WP-instruction-basename-currency.md (Draft, 548 lines) and docs/specs/logbook/2026-09-04-instruction-basename-currency-design-gate-rounds.md, as changed relative to main (run: git diff main...HEAD --stat). This is the design review of a work package that will be handed to a sonnet implementer who reads ONLY the spec and CLAUDE.md. The WP: bring the instruction-file basename deny-list in src/core/dream/promote.js (INSTRUCTION_BASENAMES, four names) current with a dated inventory (Table A: nine DENY rows with vendor-doc citations), record accepted omissions and handoffs (Table B), add a standing maintenance obligation to docs/runbooks/release.md (Table C), ship the inventory as docs/instruction-file-inventory.md, pin the set with tests plus a red-proofs declaration (Table D). Owner rulings already given: the 2026-08-05 ruling keeps this a NAME LIST with a named residual (an undocumented tool's file still passes) — an enumeration never closes and this WP must not pretend otherwise; dot-prefixed paths are a CLASS rule owned by WP-dot-segment-denial and are out of this WP.
CHALLENGE: (a) reachability — for every Table A row, can the implementer's one-line change to INSTRUCTION_BASENAMES actually deny the name through the shipped predicate (read makeAdmit, fold, foldedSegments and the Set lookup in src/core/dream/promote.js; check the "Stored spelling" column against the fold, including NFC/NFD and case) — any row that would be unreachable dead code? (b) the copy boundary — the spec copies Tables A, B, C verbatim into docs/instruction-file-inventory.md under the skeleton in Exact contracts; can the shipped document and the spec's canonical tables diverge without acceptance criterion 1 or any verification step going red? Is any sentence inside the copied region transitional or self-referential in a way that is false in the shipped home? (c) discrimination — run V1, V2, V3 on this untouched tree (they must be RED for the right reason) and reason about V4 (npm run red-proofs, ~100 s, three suite runs; do NOT run it — read scripts/red-proofs.js and the declaration format in tests/red-proofs/dream-pipeline.proofs.json and judge whether Table D's declaration would produce the required RED: the shipped four-name enumeration failing the new test as an ASSERTION failure, not an ERROR); is any acceptance criterion satisfiable by a wrong implementation? (d) the new test in tests/unit/dream-promote.test.js that derives its subject list by parsing DENY rows from the inventory — what parse shape does it assume, can a mis-formatted row silently shrink the set to zero or to a subset, and does the spec pin the failure on zero rows? (e) Table B completeness — is there a documented bare-path (non-dot-prefixed) instruction-file convention current vendor documentation establishes that Table A neither denies nor Table B records? Only name one you can cite. (f) the Deliverables table as a permission boundary — does the implementer need to touch any file it does not list (CI, package.json scripts, docs/GLOSSARY.md, tests/run.js, the runbook the obligation lands in)? (g) ADR-0004 — does anything in the obligation or the inventory watch, poll or run? (h) size S and one-line-plus-comment for promote.js — honest?
OUT OF SCOPE — file as scope objections in a separate routed section, do NOT count toward the verdict: the two owner items in ## Dispatch precondition (denying replit.md and AGENT.md; the obligation's owner being a role), the 2026-08-05 ruling's list-with-residual shape, the dot-segment class, any code change (nothing in src/ or tests/ changes on this branch — the spec describes today's code and specifies tomorrow's), and every file other than the two in scope (exclude docs/marketing/, memory/research/, userreports/, the working tree of any other checkout).
REPORT: state what you EXECUTED (each command with exit status), then findings with file:line and a materiality band (A: silent wrong behaviour with data-loss/security consequence, or a criterion a wrong implementation passes; B: caught downstream; C: hygiene), then the verdict. Review is READ-ONLY: git status --porcelain must be identical before and after; probes only in mkdtemp OS-temp fixtures.
