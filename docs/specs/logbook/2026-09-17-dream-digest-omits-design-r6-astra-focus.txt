DESIGN REVIEW ROUND 6 of a work-package spec that was re-opened after going Ready; a partial implementation exists on another branch and is NOT in this diff. Review ONLY docs/specs/WP-dream-digest-omits-own-job-alerts.md and the 2026-09-17 dream-digest-omits logbook entries under docs/specs/logbook/ (prior raws are beside them, including -design-r4-astra-raw.json and -design-r5-astra-raw.json). Exclude docs/marketing/, memory/research/ and userreports/ entirely. THE DESIGN CHANGED in response to round 5: the unfiltered recovery re-render is REMOVED entirely. Now: the existing step-19 digest render in src/cli/dream.js stays exactly as on main - UNFILTERED. A second, FILTERED render is added as the LAST STATEMENT of run(), after the outer try/finally that tears down the workspace, cleans scratch and releases the dream lock, so it is reached only by falling through: the body succeeded and every finalizer succeeded. The spec claims NO guard is needed because every return and throw inside run() exits without resuming after that block, and that the only obstacle is scope (regenerateDigest and ledger are declared inside the try, so one binding is hoisted). The filter omits an own-job alert record only if ALL THREE hold: WIENERDOG_JOB resolves via findJob to run: builtin:dream; a valid supervisor-minted WIENERDOG_DREAM_RUN_TOKEN is present; the record's `at` is STRICTLY EARLIER than this process's start instant (missing, empty, unparseable or equal means SHOWN). Hiding a GENUINE unresolved alert is the worst outcome; showing a stale one is merely today's behaviour. PART ONE - verify, by EXECUTING the production run() control flow and function bodies with mocked I/O where you can: (1) the exit inventory - is there ANY path that reaches the statement after the outer try/finally other than full success (consider every return, every throw, async rejections, an early return inside a nested try, a finally that swallows); is step 19's render provably byte-identical to main's; (2) the round-5 findings: "recovery can silently erase unrelated job alerts" - disposed by removing recovery; the spec states as a pre-existing property that readAlerts returns [] on read errors so ANY render, today's included, can publish an alert-free digest, and routes it - is that true and is adding one more render per successful run stated honestly; "O3 prices the survivor risk using a fallback that is deleted" - O3 now says clearAlerts deletes every record for the job by name after a successful supervised run regardless of survivors and that email is best-effort, so what this WP changes is one render's worth of display - is that accurate; the machinery finding is moot, replaced by AC7a-f including a compound-failure case; (3) the consequences the spec names for the new placement: two digest writes per fully successful supervised run; the filtered render runs AFTER the dream lock is released while sync.js takes no dream lock at all (so a concurrent writer is pre-existing, last writer wins, both whole-file atomic); if the final filtered render throws before publishing the digest stays unfiltered (safe) and the run exits 1, if it throws after publishing (WD_F10_POST_RENAME) the filtered bytes are on disk and the run exits 1 - the one named residual. Are these complete and true - in particular, what does a run that exits 1 from the LAST statement do to the supervisor's success marker and alert clearing, and does that create a new way to hide a genuine alert or to raise a false one? PART TWO - closure: report only findings about PRODUCT behaviour a user or consuming process would observe that the spec does not already name as a residual or owner item; list verification-machinery findings separately, marked as machinery. Do the file:line citations resolve on this tree. State what you executed versus what you only read.
