# raw — hermetic Codex shadow (codex exec, read-only sandbox, CODEX_HOME=~/.codex-review-home, gpt-5.6-sol high) — design-r4
# tip reviewed: edec229dfb6f7768ecfd7d4683e5bf186da2c9b5 (detached worktree) ; base main = 4b629ec6d1173c3724f9bc0e3d7c658eed062541 ; committed pre-adjudication
# codex exec rc=0 ; porcelain before/after: identical

{"target":"edec229dfb6f7768ecfd7d4683e5bf186da2c9b5","verdict":"needs-attention","executed":["Verified HEAD, clean status, changed-file list, diff stat, and git diff --check against 4b629ec6d1173c3724f9bc0e3d7c658eed062541 — exit 0; HEAD matched target","Generated the full base-to-HEAD diff — exit 0","Read and SHA-256/line-count audited all mandated changed and cited files — exit 0","Mechanically resolved all 24 file:line citations in the primary spec at both endpoints — exit 0","Audited every Table U R2P/R3P citation for an exactly-one probe definition — exit 0; 26 ids, zero missing or duplicate definitions","Spot-checked U6, U9, U10, U22 and U23 against their committed probe output — command exit 0","Executed a Node counterexample constructing an allowlisted environment by spreading process.env and deleting every non-allowed key — exit 0; output was deeply equal to key-by-key construction and excluded both the canary and GIT_DIR","Executed getPaths() with two HOME values and with HOME unset — exit 0; values followed HOME and then os.homedir()","node --check on cited JavaScript files and JSON.parse on dream-pipeline.proofs.json — exit 0","npm test — exit 1 before tests ran; read-only sandbox refused mkdtemp with EPERM","node scripts/red-proofs.js — exit 1 before proofs ran; sandbox creation failed with EPERM","node scripts/red-proofs.js --wp WP-dream-git-env-pinning — exit 1 with the expected VACUOUS result because implementation declarations do not exist on this docs-only branch","npm run lint — exit 1 because markdownlint dependency lookup failed with ENOTFOUND; frontmatter validation passed","V5 mirror greps — exit 1 at the expected absent implementation-time ADR amendment","Boundary check — exit 1 on the documented design-only successor-stub exception","Final git status was clean and HEAD still matched the target — exit 0"],"summary":"Do not dispatch yet. R3-A is only partial: its dependency fix is contradicted later. R3-B, R3-D, R3-E and R3-F are fixed. R3-C's two-arm evidence and corruption consequence are fixed, but its Reach-cell extraction/citation is partial. A forbidden spread-and-filter implementation can still pass all nine criteria. Full tests and RED proofs could not execute in the read-only sandbox.","findings":[{"title":"R3-A's dependency contract is contradicted by the implementation notes","file":"docs/specs/WP-dream-git-env-pinning.md","line_start":679,"line_end":680,"materiality_band":"B","confidence":1,"body":"Classification: product/implementation-contract, not evidence or hygiene. The corrected Deliverables row explicitly requires git-env.js to require('../paths') so buildGitEnv(indexFile) obtains getPaths().home at call time, but this later operative constraint says the module needs node:path at most. A sonnet implementer must violate one instruction. Re-deriving HOME as process.env.HOME || os.homedir() would satisfy AC1 under current paths.js behavior and could make every criterion green while violating the required single-authority source; omitting the fallback would fail the HOME-unset case. I executed the source/signature audit and getPaths() set/unset probe.","recommendation":"Replace this note with an unambiguous constraint such as “zero new package dependencies; git-env.js may require node:path and the existing ../paths module exactly as its Deliverables row specifies.”"},{"title":"AC1 cannot reject spread-and-delete when deletion is driven by the allowlist","file":"docs/specs/WP-dream-git-env-pinning.md","line_start":765,"line_end":774,"materiality_band":"B","confidence":0.99,"body":"Classification: product/acceptance-contract, not record hygiene. The canary rejects spreading process.env and deleting only known bad channel names, but it does not reject spreading and then deleting every key not in the positive allowlist. I executed that counterexample: its final map was deeply equal to key-by-key construction and omitted the canary and GIT_DIR. Because all behavioral criteria observe only the resulting child environment, AC2–AC5 are indistinguishable; AC6–AC7 constrain mutations rather than the construction form, and AC8–AC9 do not help. Thus a builder explicitly forbidden by lines 294–295 and the security checklist can pass all nine criteria. The statement that any filtered copy fails the canary is false.","recommendation":"Add an enforceable structural check over git-env.js that rejects process.env spreading/copying or relax the exact contract to permit a positive-allowlist filter. Do not claim the runtime canary distinguishes source constructions that produce the same map."},{"title":"U9 attributes shape (6) to the wrong probe and still embeds interpretation in Reach","file":"docs/specs/WP-dream-git-env-pinning.md","line_start":410,"line_end":410,"materiality_band":"C","confidence":0.98,"body":"Classification: evidence/record hygiene, not product behavior. R3P6 genuinely proves shape (2)'s deduplication result and the broken-history consequence, but its committed output does not execute or print a discriminating shape-(6) comparison; it proceeds from hash-object to write-tree/commit-tree/update-ref. The separate R2P6b output is what proves read-tree reach. The cell also remains more than the required probe id plus one-word result by embedding the consequence. U9's ABSENT disposition remains well supported, so this does not change what the implementer builds.","recommendation":"Cite R3P6 for shape (2) and R2P6b for shape (6), reduce Reach to the extracted result form, and leave the deduplication/corruption consequence solely in the rationale and round record."}]}