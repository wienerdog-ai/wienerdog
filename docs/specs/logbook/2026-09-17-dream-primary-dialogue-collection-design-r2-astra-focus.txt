DESIGN REVIEW ROUND 2 of a NEW work-package spec; no product code has been written yet. Review ONLY docs/specs/WP-dream-primary-dialogue-collection.md and docs/specs/logbook/2026-09-17-dream-primary-dialogue-split.md (your round-1 raw is preserved beside it as 2026-09-17-dream-primary-dialogue-collection-design-r1-astra-raw.json). Sibling specs changed in the same diff are context only. Exclude docs/marketing/, memory/research/ and userreports/ entirely. This package wires a sibling's parsePrimaryWithOutcome into the nightly dream's collector (src/core/dream/scratch.js collectExtracts): admission against the byte budget X is measured on intakeBytes, the raw capped extract's size; what is written to scratch and shown to the model is the smaller primary dialogue; a text-free code-only gateExtract feeds the existing skill-learning authorization gate (src/core/dream/validate.js); the model no longer receives tool-record metadata; the dream skill is updated; a dated ADR-0020 amendment is a deliverable with Status "PROPOSED - awaiting owner signature". PART ONE - verify each round-1 item is GENUINELY fixed in substance, EXECUTING the real collector, ledger and validator with mocked I/O as you did before: (C-1) "The preprocessing deadline can admit additional sessions despite identical intake bytes" - the guarantee is now BYTE-POLICY EQUIVALENCE, not admitted-set identity: every byte decision for every VISITED session equals the base commit's; which sessions are visited can differ in either direction because the soft deadline measures wall-clock across discovery, parse, serialization and writes; new row C1a says when the deadline binds and states that large-backlog installs - where marking unread sessions processed matters most - are exactly where the set can move; AC1 is scoped to the non-deadline regime, AC1a moves the boundary both ways with a mocked clock, AC7's offline measurement must name the regime; priced as an owner item. Is every statement of the guarantee now TRUE on every surface, and is anything still claiming "unchanged by construction"? (machinery) AC4 now asserts refusal of an understated taint flag, acceptance of a correctly declared true, and a clean-invocation control; (clarity) new canonical row D6 separates CODE-enforced guarantees from PROMPT-only ones and is mirrored into the ADR-0020 amendment text; the spec also corrects a shipped inaccuracy - skills/wienerdog-dream/SKILL.md:329 tells the model the orchestrator RAISES an understated flag, whereas src/core/dream/validate.js (~l.647) and promote.js (~l.1386-1394) REFUSE the write - verify that claim against the code. PART TWO - closure: report only findings about PRODUCT or SECURITY behaviour that the spec does not already name as a residual or owner item; list verification-machinery findings separately, marked as machinery. Do the file:line citations resolve on this tree. State what you executed versus what you only read.
