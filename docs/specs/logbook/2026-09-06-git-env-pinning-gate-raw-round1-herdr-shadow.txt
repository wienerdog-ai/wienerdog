# raw — hermetic Codex shadow (codex exec, read-only sandbox, CODEX_HOME=~/.codex-review-home, gpt-5.6-sol high) — design-r1
# tip reviewed: 4cb6259f46fb6f3bf6f265ef72fb3654fa8f5098 (detached worktree) ; base main = 4b629ec6d1173c3724f9bc0e3d7c658eed062541 ; committed pre-adjudication
# codex exec rc=0 ; porcelain before/after: identical

{"target":"4cb6259f46fb6f3bf6f265ef72fb3654fa8f5098","verdict":"needs-attention","executed":["git rev-parse HEAD; git status --short; git diff 4b629ec6d1173c3724f9bc0e3d7c658eed062541 HEAD --stat; git diff --name-only ... — exit 0; HEAD matched target","git diff 4b629ec6d1173c3724f9bc0e3d7c658eed062541 HEAD — exit 0; full diff generated","Full-file read/SHA-256 audit over all 16 mandated changed and cited files — exit 0","rg audits for gitIn/withIndex/opts.env/indexEnv, pinned Git spawn points, exec-identity environment reads, and run-job clean-environment keys — exit 0","node --check on the seven cited JavaScript files and JSON.parse of dream-pipeline.proofs.json — exit 0","Mechanical cited-line anchor check — exit 0","git diff --check base HEAD — exit 2; found trailing whitespace in the design-gate logbook only","npm test — exit 1 before tests ran: read-only sandbox refused mkdtemp with EPERM","npm run lint — exit 1: markdownlint attempted an unavailable npm registry lookup; frontmatter check passed","node scripts/red-proofs.js — exit 1 before proofs ran: read-only sandbox refused sandbox creation with EPERM","node scripts/red-proofs.js --wp WP-dream-git-env-pinning — exit 1 with the expected VACUOUS result because the implementation declaration file is absent","V5 component greps — ADR amendment exit 1, Table W amendment exit 1, existing hook-residual grep exit 0","boundary-check against the design diff — exit 1, naming the expected successor-stub exception","Official git-scm.com documentation checked for config-file selection, hook execution/environment, replacement refs, pathspec variables, and commit identity variables"],"summary":"Do not ship the spec yet. AC1 permits value-wrong implementations, while the prescribed implementation still accepts arbitrary-code configuration selected by the supposedly untrusted launch environment. Table U also understates hook-related helper reach, and one MEASURED claim was obtained with a non-pinned argv.","findings":[{"title":"AC1 checks environment names but not their values","file":"docs/specs/WP-dream-git-env-pinning.md","line_start":444,"line_end":459,"materiality_band":"A","confidence":0.99,"body":"AC1 requires an exact key set but never requires PATH, HOME, XDG_CONFIG_HOME, or the Windows keys to equal their inherited values, nor GIT_INDEX_FILE to equal the per-run tmpIndex supplied to buildGitEnv. The existing watchIndexWrites classifier only requires the private index to be absolute, outside the vault, and different from the user's index. A builder using empty or constant config roots and a fixed external index path can therefore satisfy AC1–AC4 and have valid RED mutations. That can silently suppress configured guard hooks, and a fixed index can be shared by concurrent dreams from different state roots, allowing their per-command index updates to interfere. Optional XDG_CONFIG_HOME is additionally untested when absent on CI. Executed evidence: inspected AC1, gitIn/withIndex, watchIndexWrites lines 215–225, all env-related test references, and the pinned-call declarations with rg and full-file reads.","recommendation":"Make AC1 assert the complete key/value map, not merely Object.keys: explicitly set and restore every optional carried POSIX key (especially XDG_CONFIG_HOME), assert each carried value equals process.env, assert platform keys similarly on Windows, and assert all three private shapes receive exactly path.join(stateDir, `dream-index.${process.pid}.tmp`) while every unset shape lacks it."},{"title":"Inherited HOME and XDG_CONFIG_HOME preserve an untrusted arbitrary-code injection path","file":"docs/specs/WP-dream-git-env-pinning.md","line_start":300,"line_end":303,"materiality_band":"A","confidence":0.98,"body":"U2/U3 copy HOME and XDG_CONFIG_HOME verbatim from the launch environment even though the security checklist classifies that environment as untrusted. Git documents those variables as selectors for ~/.gitconfig and $XDG_CONFIG_HOME/git/config; this branch's P10 confirms the relocation, while P8/P14 confirm that selected config containing core.fsmonitor executes a script during a pinned write-tree. Thus HOME=/attacker or XDG_CONFIG_HOME=/attacker gives a conforming implementation the same arbitrary-code channel U11 rejects, and every AC remains green because those keys are required. The statements that the launching environment is not a configuration surface and that manual and scheduled runs become identical are also false: run-job constructs HOME from paths.home and does not carry XDG_CONFIG_HOME. Executed evidence: read Table U, P8/P10/P14, run-job buildCleanEnv, and exec-identity's env forwarding; checked Git's official git-config documentation.","recommendation":"Resolve config roots from the same bound/code-derived home used by the scheduled path instead of copying launch-time selectors. If launch-time HOME/XDG relocation is intentionally retained, require an explicit owner disposition that names it as an arbitrary-code exception, correct O1/security/manual-vs-scheduled claims, and add a behavioral test proving the chosen boundary."},{"title":"U20's no-reach/no-cost claim ignores the update-ref hook it deliberately preserves","file":"docs/specs/WP-dream-git-env-pinning.md","line_start":319,"line_end":319,"materiality_band":"B","confidence":0.93,"body":"U20 concludes that transport/signing helpers cannot reach a pinned shape by checking only direct remote activity and whether commit-tree signs. But pinned update-ref executes the preserved reference-transaction hook—the spec counts that descendant execution as reach in U10 and P8c. That hook inherits the Git child's environment and may consume SSH_AUTH_SOCK, GNUPGHOME, GPG_TTY, GIT_SSH_COMMAND, or askpass variables directly or through commands it launches. The construction removes all of them, so a currently working user-configured ref guard can change behavior or fail even though O1 says hooks remain honored; calling this “ABSENT, no cost” is unsupported. Executed evidence: inspected U10/U20, P8c, Table W row W1, spawnPinnedSync env forwarding, and Git's official githooks documentation.","recommendation":"Re-run U20 against the pinned update-ref with a configured reference-transaction hook that observes/uses these helper families. Record the resulting hook-environment cost, decide explicitly which values are carried or scrubbed, and revise the blanket hook-preservation claim and acceptance coverage accordingly."},{"title":"U10's second MEASURED result used an argv outside the nine pinned shapes","file":"docs/specs/WP-dream-git-env-pinning.md","line_start":291,"line_end":309,"materiality_band":"C","confidence":1,"body":"The provenance contract says every MEASURED cell used a pinned shape's own argv, and U10 claims core.fsmonitor executed during pinned update-index. The recorded P8 command is `update-index --refresh`; the actual pinned shape is `update-index --add --cacheinfo ...`. No cited run measures that argv. write-tree still proves the channel reaches one pinned shape, but the “twice”/update-index evidence and the claimed measurement method are false. Under the pinned STOP CRITERION this is a DESIGN branch, not a row-level wording fix. Executed evidence: compared logbook P8 lines 187–190, Table U U10, dream.js lines 253–258, and tests/unit/dream-pipeline.known-calls.js lines 85–99.","recommendation":"Re-run the config-injection probe with the exact pinned update-index --add --cacheinfo argv. Then mechanically re-derive Table U and its mirrors; either retain the claim with reproduced evidence or narrow U10 to the pinned write-tree result."}]}