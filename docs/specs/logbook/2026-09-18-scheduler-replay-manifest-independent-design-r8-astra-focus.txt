Adversarial DESIGN review, round 8 (confirming), of docs/specs/WP-scheduler-replay-manifest-independent.md (Draft, M). Rounds 1-7 raws and dispositions in docs/specs/logbook/2026-09-18-scheduler-replay-manifest-independent-design-r{1..7}-astra-raw.json and -design-review.md. Round 7: the act-time carve-out in D6 recreated the orphan. Fix: D6 rewritten — in D5a, ENOENT/ENOTDIR = absence (drop, record, continue); any other lstat/resolution failure stops the uninstall with D9's abort shape BEFORE the manifest entry loop and before disposeCoreMechanics; D5b acts only on items D5a passed with no independent re-check (its own checks may only skip); D15 spans discovery and act time with absence as the single special case; the isolation tension with manifest.js:842-846 (a throwing reverser must not leave an install partially reversed) is addressed because D5a's abort fires before any entry reverser has run; AC 17 has five outcomes; S12 spans both phases.

Confirming round — final. Execute read-only against the tree; never the real scheduler or ~/.wienerdog. (1) The abort placement: D5a runs inside reverse() before the entry loop (manifest.js:765) — confirm that an abort thrown there propagates out of reverse() without being caught by :842-846's protective catch (read that block: what does it catch, and would a D5a abort be swallowed and turned into "partially reversed" state, or does it re-throw?), and that uninstall.js's caller (:353-354 plan; the non-dry-run path) surfaces it before disposeCoreMechanics; (2) D5b acting only on D5a-passed items: D5a unloads then D5b removes — between them the manifest entry loop runs and may delete the same file (recorded entry) — D5b's "removedSet skip" covers it; a file removed by the user between D5a and D5b (ENOENT at rmSync) → absence, fine — is that stated; (3) dry-run: D5a's re-check must NOT run in the plan-building dry-run (it would abort a dry run on a transient EIO — is that the intended trade or does dry-run report instead, as D9 does?); (4) the uniform rule end-to-end once more: list every fs call in the specified discovery+act path (readdirSync, lstatSync, realpathSync ×3, rmSync, plus the spawn) and the bucket for each error class; any call without a stated bucket is a finding; (5) counts/cites/checklist/RED/owner items consistent, no approval claims. If nothing about the product is wrong, say approve.
