Adversarial DESIGN review, round 2, of docs/specs/WP-scheduler-replay-manifest-independent.md (Draft). Round-1 raw and dispositions: docs/specs/logbook/2026-09-18-scheduler-replay-manifest-independent-design-r1-astra-raw.json and 2026-09-18-scheduler-replay-manifest-independent-design-review.md. Round 1's three high findings (unreadable root read as empty; a non-scheduler manifest entry suppressing unload discovery; the widened deletion bypassing the nested-vault exclusion) are answered by three new canonical rows: D9 — ENOENT/ENOTDIR = absent, any other enumeration or lstat failure = unreadable; discovery returns {schedules, unreadable, skippedForVault}; a non-dry-run uninstall throws a WienerdogError naming each unreadable dir BEFORE the plan is printed, having deleted nothing; --dry-run reports instead. D10/D11 — only a validated scheduler-entry naming the same resolved path AND passing withinSchedulerRoot (reaching manifest.js:524-538) suppresses discovery; a schema-valid record of another kind neither unloads nor suppresses, it only withholds REMOVAL with a "keep … (another manifest entry owns this file)" notice; reverse()'s option is {path, remove} items fixed at discovery time. D12 — discovery takes the accepted vault path (the value uninstall.js:309 hands disposeCoreMechanics) and excludes candidates equal to or inside it from unload and removal. R4 gains a "ceiling, not a floor" clause. Two RED declarations added (unreadable root read as empty; vault exclusion removed); finding 2 deliberately carries none because its criterion asserts a positive effect.

Do NOT touch the real scheduler, ~/Library/LaunchAgents or ~/.wienerdog. Reason over the code and execute with mocked I/O: (1) D9 — enumerate the failure modes of readdirSync/lstatSync on the three roots (EACCES, EPERM, ELOOP on a symlinked root, EIO, ENAMETOOLONG, a root that is a FILE not a dir, a root that is a dangling symlink) and check each lands where the row says; can an attacker who controls one root's readability make uninstall permanently un-runnable (denial of uninstall) — and is that the intended trade, stated? (2) D10/D11 — trace the actual code path a scheduler-entry takes through reverse() (manifest.js:501-543) and confirm "reaches :524-538" is the right suppression predicate: what about a scheduler-entry whose recorded path resolves outside the roots (the existing reverser rejects it — does discovery then correctly NOT suppress, so the file is unloaded by the widened pass?); what about two records naming the same file; what about the {path, remove:false} item — does the act-time narrowing re-check still hold with the removal decision fixed at discovery? (3) D12 — the vault path is accepted where and how (uninstall.js:309); if the vault path is itself a symlink or inside a scheduler root, does contains() use realpath consistently on both sides; can a vault located at a scheduler root suppress ALL discovery (then the orphan survives — is that the right outcome, stated)? (4) The missing RED for finding 2: is the architect's argument (a positive-effect criterion cannot carry a pre-implementation mutation) sound, or is there an expressible mutation (e.g. make validateEntry's kind check accept 'file' as suppressing) that would redden the D10 test once it exists — ADR-0042 decision 5; (5) anything the three new rows broke in Tables R/S, the ACs (now 12), the checklist, the Security checklist; cites at both ends; owner items unchanged and in the standing form with no approval claims. Report findings with file:line, band, LIGHT/HEAVY; verdict: approve / request-changes.
