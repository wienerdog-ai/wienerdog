# Raw gate output — Codex plugin adversarial-review, round 1, branch docs/w1-wording-followups-2026-09-04 at 2af1bfc1 (base main = 705ae286). Committed BEFORE adjudication. Focus text: scratchpad focus-w1.txt (reproduced at the end).

# Codex Adversarial Review

Target: branch diff against main
Verdict: needs-attention

Do not ship yet: three documentation-contract defects remain. Review completed against final HEAD 2af1bfc1 (an externally added logbook rebase-note commit followed the supplied 4d6ac30d). Executed: initial `git status --porcelain && git diff main...HEAD --stat && git diff main...HEAD --name-only` (0); scoped per-file diffs with `--unified=80`, `--unified=6`, and `--unified=0` (all 0); W1(c) HEAD/main extraction greps (0); all seven canonical-locator greps (0, except the intentionally absent `MINTED` grep: 1 with count 0); locator-context `nl|sed` inspections (all 0); phase-environment constant/function greps and inspections (all 0); basename/abort and `secretGateAbortMessage` greps and inspections (all 0); logbook spot-checks—canonical count (0/count 1), executable-copy absence (1/count 0), constant locations (0), pair-rule locations (0), and `npm run lint` (0); `git diff --check main...HEAD` (0). The historical pre-rebase path probe against current HEAD returned 128 as expected, and the new rebase note explains that provenance. Final `git status --porcelain` exited 0 and remained byte-identical to the initial result: `?? node_modules`. Routed scope objections: none.

Findings:
- [medium] W1(c) changes the historical scope assertion while claiming it does not (docs/specs/done/WP-dream-promote-in-workspace.md:541)
  Materiality C. The old clause asserted that `merge-file` was outside W1(a)'s scope and that “(a) never reaches it”; the replacement asserts the opposite—that authorship puts it inside the scope and it satisfies the total vacuously. Those are different assertions, not merely different wording. The branch's own logbook confirms this at lines 475-477 (“same fact and different assertions”), contradicting both this row's “nothing either clause asserts changes” and the logbook disposition at lines 272-274. Because the old clause was replaced rather than retained followed by an amendment, the Done record no longer preserves what it asserted.
  Recommendation: Restore the original (c)(ii) wording verbatim, then append a dated amendment that explicitly supersedes its exclusion assertion with the in-scope/vacuous assertion. Remove the claim that no assertion changed.
- [medium] The round-5 correction replaces rather than annotates the Done-spec assertion (docs/specs/done/WP-criterion-red-harness.md:446-477)
  Materiality C. The diff deletes the historical assertion that row 2b's `OVERRIDE_VARS`-derived provided set was mirrored and substitutes the opposite assertion under a `CORRECTED` label. Although the replacement describes the former mistake, it does not retain the text the Done spec actually asserted. This violates the stated historical-record discipline, unlike the B3b and C3 changes, which leave their original assertions standing and append dated amendments.
  Recommendation: Restore the original round-5 paragraph unchanged and append the dated correction after it, explicitly marking which historical claims are superseded.
- [medium] The stated phase-environment authority map is false for XDG and incomplete for three controls (docs/specs/done/WP-criterion-red-harness.md:464-477)
  Materiality C. The paragraph says the five named constants are “applied by `phaseEnv()`” and ends by directing readers to the constants. `rg` shows `XDG_VARS` is only declared and exported; `phaseEnv()` never references it and instead hardcodes the four XDG assignments. Likewise, `PWD`, `OLDPWD`, and `PATH` are controlled directly in `phaseEnv()` and exist in none of the five constants. A future removal from `XDG_VARS` can therefore leave runtime behavior unchanged while also shrinking the test iteration, defeating the claimed authoritative surface.
  Recommendation: State the actual map: four constants are consumed by `phaseEnv()`; `XDG_VARS` mirrors hardcoded assignments for tests; and `PWD`, `OLDPWD`, and `PATH` are owned by explicit statements inside `phaseEnv()`. Do not tell readers that the constants alone decide the complete name set.

Next steps:
- Convert both historical rewrites into additive dated amendments.
- Correct the phase-environment authority description, then rerun the named greps, `git diff --check main...HEAD`, and `npm run lint`.

---- focus text ----
SCOPE: ONLY the five files changed by this branch relative to main (run: git diff main...HEAD --stat): dated errata/amendments in four Done specs under docs/specs/done/ (WP-show-slot-own-value-kind.md, WP-dream-promote-in-workspace.md row W1(c) and its Mirrored Surface Checklist, WP-secret-fence-ep2-redact-arm.md row B3b, WP-criterion-red-harness.md line ~446 region) and the new logbook entry docs/specs/logbook/2026-09-04-w1-wording-followups.md. These close seven findings the PR gates of #203/#204/#205 routed to the architect as out-of-boundary. Done specs are historical records: the discipline is dated errata/amendments, never rewriting what the spec said.
CHALLENGE: (a) does any erratum or amendment change what a Done spec ASSERTED rather than annotate it — i.e. is history rewritten? (b) do the new non-positional locators in WP-show-slot-own-value-kind's canonical-extraction table actually resolve in tests/unit/dream-pipeline.test.js and tests/unit/dream-pipeline.known-calls.js at HEAD — execute the greps; (c) does the corrected line-446 region of WP-criterion-red-harness state what scripts/red-proofs.js actually does (read phaseEnv() and the five constants it names) — any name set it claims that the code does not implement, or vice versa? (d) does the B3b amendment leave any surface in WP-secret-fence-ep2-redact-arm still instructing that the abort message carry the basename (grep the file), and is the pair rule it cites stated correctly against src/cli/dream.js / src/core/dream/promote.js secretGateAbortMessage? (e) does the W1(c) reframing of clause (a) vs (c)(ii) change either clause's assertion? (f) does the logbook's evidence match the tree — spot-check three of its pasted commands by re-running them.
OUT OF SCOPE — file as scope objections in a separate routed section, do NOT count toward the verdict: the merged code in src/ and tests/ (nothing here changes code), the closed design loops of the three WPs, the two named residuals (the `computed` JSDoc registered in W1(c)'s registry; the absent canonical table for the phase-environment contract, which is an owner contract decision), and every file outside the five.
REPORT: state what you EXECUTED (each command with exit status), then findings with file:line and a materiality band (A: silent wrong behaviour with data-loss/security consequence; B: caught downstream; C: hygiene), then the verdict. Review is READ-ONLY: git status --porcelain must be identical before and after.
