Adversarial DESIGN review, round 2, of the three chained specs + ADR-0043: docs/specs/WP-secret-sink-redact-before-truncate.md, docs/specs/WP-secret-stream-safe-cut-redactor.md, docs/specs/WP-secret-sink-chunk-fix.md, docs/adr/0043-safe-cut-stream-redaction.md. Round-1 raw and the dispositions table: docs/specs/logbook/2026-09-18-secret-sink-fix-design-r1-astra-raw.json and 2026-09-18-secret-sink-fix-design-review.md. Round 1 found (a) the last-line cut predicate leaked across permitted cuts (password:\n\nV, password\n:\nV, JSON quoted values spanning lines) and (b) the flush wiring could write into an ended log on the watchdog path. Fixes: (a) Table S restated over incomplete-match STATE on the whole buffered prefix P — S1 P ends with \n; S2 no open key binder at the end of P (SENSITIVE_KEYS/authorization keyword followed only by optional quote, whitespace incl. CR/LF, one SEP token, whitespace, quote); S3 no open sensitive quoted value anywhere in P (the JSON rule's value class includes \n); S4 no open private-key block; seven leak shapes L1..L7 published as the RED baseline (AC3a); (b) spawnBrain returns { child, done, shutdown }; shutdown() is synchronous, idempotent, emits redactor.end() for each owned redactor through the stream's emit, then latches closed so later 'data'/'end' write nothing; src/cli/dream.js added to Deliverables bounded to two edits (capture at :402, call first in the finally at :479, always before :958's logStream.end()); run-job latches in its outer finally before the :1120 failure write; AC2a is the regression for both sinks.

Execute against src/core/secret-scan.js with mocked I/O: (1) implement the four-condition predicate as specified over P and attack it with every rule family in the scanner (SENSITIVE_KEYS × SEP × quoting × CR/LF, Bearer, PEM, JSON value, extended assignment, keyword-bound entropy, and the truncation/oversized paths) — find any input that redacts when scanned whole and leaks across a cut the predicate accepts; check the "one SEP token" and "optional quote" choices against the actual SEP definition and the key-binder regexes (can a binder be `key = "` with two whitespace runs, or `key:` followed by a comment?); check what the predicate does with a line longer than STREAM_REGION_MAX and a prefix that never reaches an accepted cut (forced cut: is the residual named and bounded?); (2) shutdown: trace every settle path in src/cli/dream.js (:402 spawn, :414-445 pidfile throw, :457 watchdog rejection, :479 finally, :958 logStream.end()) and in run-job around :1120 — is there a path where output arrives after latch and is silently lost while the child is still the legitimate producer (a slow but healthy child)? is the latch placed so the SUCCESS path still flushes the final partial line? does emitting redactor.end() through the stream's emit keep the per-stream byte order the spec declares a contract? any unhandled 'error' on either stream? (3) the RED-proof design for the seven probe conversions after the shutdown change; (4) ADR-0043 decisions 2 and 7 honest and consistent with the specs; (5) chain coherence and cites at both ends. Report findings with file:line, band A/B/C, LIGHT/HEAVY; end with a verdict per spec and the ADR: approve / request-changes.
