DESIGN REVIEW ROUND 4 of a re-derived work-package spec; no product code has been written yet. Review ONLY docs/specs/WP-dream-report-run-skips.md and the 2026-09-17 report-run-skips logbook entries under docs/specs/logbook/ (prior raws are beside them: -design-r1-raw.json and -r2-raw.json from another model, and -design-r3-astra-raw.json). Exclude docs/marketing/, memory/research/ and userreports/ entirely. The spec makes the nightly dream's vault report state, in plain language for a non-developer, how many sessions a run could not consolidate, as six integer counts composed inside src/core/dream/promote.js composeRecord via a new optional promote({runSkips}) argument. PART ONE - verify the two round-3 findings are GENUINELY fixed, by executing the collector and ledger logic as you did before, not by reading: (A-1) "Deferred counts can promise retries for permanently memoized oversized sessions" - the capacity, deadline and read-deferred bullets no longer promise a retry; they say the sessions were not reached (or were still being written) and that Wienerdog will consider them again on the next run, though some may turn out to be too big to dream over on their own; a new canonical row owns that promise and "no ledger record" is narrowed to "no QUARANTINE record"; two fixtures put a memoized-oversized session behind each kind of stop. Is every one of the six bullets now TRUE in every case you can construct, including a memoized-oversized session behind a stop, a file that changes, an app version change, and the limit being raised? (A-2) "Re-quarantined sessions are falsely reported as skipped for the first time" - B1 now reads "were set aside by this run and will be skipped from now on, until they change". Is that true for a first quarantine AND a re-quarantine, and do B1 and B2 still partition the quarantine cases with no session counted in both or neither? PART TWO - closure: report only findings about PRODUCT behaviour a user would observe that the spec does not already name as a residual or an owner item; list verification-machinery findings separately, marked as machinery. Do the file:line citations resolve on this tree. State what you executed versus what you only read.
