DESIGN REVIEW ROUND 3 of a NEW work-package spec; no product code has been written yet. Review ONLY docs/specs/WP-dream-primary-dialogue-collection.md and docs/specs/logbook/2026-09-17-dream-primary-dialogue-split.md (your round-1 and round-2 raws are preserved beside it as …-collection-design-r1-astra-raw.json and -r2-astra-raw.json). Sibling specs in the same diff are context only. Exclude docs/marketing/, memory/research/ and userreports/ entirely. This package wires a sibling's parsePrimaryWithOutcome into the nightly dream's collector (src/core/dream/scratch.js collectExtracts): admission against the byte budget X is measured on intakeBytes, the raw capped extract's size; what is written to scratch and shown to the model is the smaller primary dialogue; a text-free code-only gateExtract feeds the existing skill-learning authorization gate (src/core/dream/validate.js) through an in-memory map that replaces today's re-read of scratch files; the model no longer receives tool-record metadata. PART ONE - verify the round-2 findings are GENUINELY fixed, EXECUTING the real parser, collector and validator with mocked I/O: (C2-1) "Filename collisions retain authorization for overwritten dialogue" - row C4 now says a later write to a scratch filename an earlier entry already wrote EVICTS that earlier entry (before setting a key, delete any key whose recorded filename equals the one about to be written), so the map holds exactly one session per distinct filename, the last one written, which the spec claims is bit-for-bit what src/cli/dream.js (about l.1042-1049 on the base it cites) produces today by re-reading the surviving file; the dialogue loss caused by the collision itself is stated as pre-existing and routed, not fixed. Re-run your s_1 / s.1 counterexample against that rule: are the gate verdicts now identical to the baseline for the overwritten session AND the surviving one, including when three or more ids collide, when the colliding sessions come from different harnesses, and when the LATER session projects to an empty messages array; does admission/processed accounting for the evicted session stay exactly as today? (C2-2) "Owner-facing guarantees still contradict the priced deadline residual" - row C1a now conditions admitted-set equality on BOTH the baseline run and the projected run avoiding deadline deferral, and every surface was swept; is ANY surface still promising an unchanged or identical admitted set, or that marking unread sessions processed cannot get worse, unconditionally? PART TWO - closure: report only findings about PRODUCT or SECURITY behaviour that the spec does not already name as a residual or owner item; list verification-machinery findings separately, marked as machinery. Do the file:line citations resolve on this tree (main has since gained a test-only probe block at the end of tests/unit/dream-collect.test.js). State what you executed versus what you only read.
