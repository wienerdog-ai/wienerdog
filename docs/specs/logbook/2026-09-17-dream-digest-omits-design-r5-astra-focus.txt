DESIGN REVIEW ROUND 5 of a work-package spec that was re-opened after going Ready; a partial implementation exists on another branch and is NOT in this diff. Review ONLY docs/specs/WP-dream-digest-omits-own-job-alerts.md and the 2026-09-17 dream-digest-omits logbook entries under docs/specs/logbook/ (prior raws are beside them: -design-r1..r3-raw.json from another model and -design-r4-astra-raw.json). Exclude docs/marketing/, memory/research/ and userreports/ entirely. The spec proposes that the nightly dream job, when it re-renders state/digest.md at the END of a successful run (exactly one call site), omits "job has failed" alert records for its OWN job. A record is omitted only if ALL THREE hold: WIENERDOG_JOB resolves via the scheduler's findJob to run: builtin:dream; a valid supervisor-minted WIENERDOG_DREAM_RUN_TOKEN is present; and the record's `at` timestamp is STRICTLY EARLIER than this dream process's start instant (missing, empty, unparseable or equal means SHOWN). Recovery is ARMED BEFORE the filtered render is attempted; any in-process failure during or after it triggers exactly one unfiltered re-render across two recovery sites (a catch on the outer try and one inside the lock-release finally), the original error wins, and a failed recovery prints one fixed-text stderr line. Hiding a GENUINE unresolved alert is the worst outcome; showing a stale one is merely today's behaviour. PART ONE - verify the round-4 findings are GENUINELY fixed, by executing production function bodies with mocked I/O as you did before: (A-1) "timeout survivors invalidate the post-exit-only residual" - does the date conjunct keep a failure recorded against THIS run visible when the watchdog fires while the child is alive and the child later reaches the final render; the spec PRICES rather than fixes the second case (a PRIOR run's surviving process group is not proven gone by this run's success) under owner item O3 and withdraws the post-exit-only and universally-resolved claims - is that statement now true and complete; is the clock-step analysis right about which direction hides; is every writer that appends under the dream's own name accounted for (launcher refuse alert, run-job's pre-spawn managed-policy warning - which the date rule OMITS and which a 2026-09-10 maintainer ruling defers - and failLoud on the reap and timeout paths); (A-2) "recovery assumes a thrown writer could not have published" - arming now precedes the render and the preservation guarantee is withdrawn in favour of a pre-publication / post-rename distinction; (A-3, machinery) AC7a-e now inject failures in the post-render body, workspace teardown and scratch cleanup and count digest writes. PART TWO - closure: report only findings about PRODUCT behaviour a user or consuming process would observe that the spec does not already name as a residual or owner item; list verification-machinery findings separately, marked as machinery. Do the file:line citations resolve on this tree. State what you executed versus what you only read.
