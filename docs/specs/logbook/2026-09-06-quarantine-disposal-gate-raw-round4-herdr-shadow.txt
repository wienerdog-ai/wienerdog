# raw — hermetic Codex shadow (codex exec, read-only sandbox, CODEX_HOME=~/.codex-review-home, gpt-5.6-sol high) — qdd4
# tip reviewed: 2511c44dd11e81e6f56b1ea906eda4e2905984e1 (detached worktree) ; base main = 66b2b1f8040b93a9af4f1cda25c3ac307da64f6d ; committed pre-adjudication
# codex exec rc=0 ; porcelain before/after: identical

{"target":"2511c44dd11e81e6f56b1ea906eda4e2905984e1","verdict":"needs-attention","executed":["git diff 66b2b1f8040b93a9af4f1cda25c3ac307da64f6d HEAD --stat / full diff / --check / --name-status — exit 0","Read the six required documents, six committed raws, and cited source/contract regions","V1–V4 read-only reconstruction against pinned base — exit 0; V2 changed exactly lines 356, 944, 1793","Predecessor archived V1 literally — exit 1 because it names the obsolete pending-spec path; done-path reconstruction — exit 0","node scripts/mirror-walk.js --scope quarantine-disposal — exit 0; RESOLVED with 11 reported ambiguities","node scripts/mirror-walk.js — exit 1 with 14 documented pre-existing unresolved entries","node scripts/check-frontmatter.js — exit 0; 271 specs, 4 agents","Parsed all 12 red-proof JSON files — exit 0","Verified all six raw-file introduction SHAs — exit 0","Source/claim lexical reconstruction for removal branches, flush disposition, and remaining spanning claims — exit 0","npm test — exit 1 before tests ran; read-only sandbox denied mkdtemp with EPERM","npm run red-proofs — exit 1 before proofs ran; read-only sandbox denied mkdtemp with EPERM","NPM_CONFIG_OFFLINE=true npm run lint — exit 1 because markdownlint-cli2 was not cached; shellcheck/PSScriptAnalyzer ran and frontmatter passed","QD-P1–P12 were not independently rerun because their scripts are explicitly uncommitted and the checkout is read-only; no power-loss crash was staged"],"summary":"Do not close round 4. R3-B and R3-C are fixed, and the F7(a) reconstruction, scoped mirror walk, and Draft-state checks pass. R3-A remains partial because the successor still promises class-wide closure, while R3-D/R3-E remain partial because O8 still reinstates M6's withdrawn equivalence and summarizes multiple acts outside Table M.","findings":[{"title":"The successor still promises total closure that its best-effort predicate cannot provide","file":"docs/specs/WP-quarantine-failed-preserve-disposal-flush.md","line_start":135,"line_end":155,"materiality_band":"A","confidence":0.99,"body":"Classification: decision-support (M2/M3). Lines 137–140 say the fix ensures a completed run cannot later be contradicted by a resurrected artifact. The same stub's acceptance predicate at lines 76–86 says the opposite: a failed flush is ignored, every win32 run issues no flush, and either case may complete and publish while retaining the residual. Source inspection confirms `flushDir` catches failures and returns false (`validate.js:806-816`). The governing spec repeats the false closure at lines 672–676 by saying O11 decides M2/M3 “are closed.” A future gate could therefore accept the proposed best-effort implementation while claiming the unrecorded secret-artifact class is eliminated when it is not.","recommendation":"Replace the class-wide promise with the exact predicate: only a completed supported-POSIX directory flush closes that act's window; failed flushes and win32 retain the priced post-completion residual. Remove the “are closed” mirror from the governing spec."},{"title":"O8 still reinstates M6's false equivalence and a cross-act rule outside Table M","file":"docs/specs/done/WP-quarantine-disposal-durability.md","line_start":88,"line_end":118,"materiality_band":"B","confidence":0.99,"body":"Classification: sentence outside rows, supporting M1/M4/M6. O8 says M6's duplicate is present and the record omits it “in both” states, then says the three permanent exclusions concern acts whose two states are “measured identical.” This directly contradicts corrected row M6: a pre-removal crash prevents the gate returning at `validate.js:1497`; post-removal completion publishes a withheld-only record; after the user follows `promote.js:600-603` and deletes that copy, resurrection leaves the redacted entry as the sole, unbannered copy. Row M6 explicitly says these states are not equal and are code-derived, not measured. O8's final “Those three” and shared-identical-state rationale also violate the claimed removal of every sentence spanning acts.","recommendation":"Delete O8's per-act recap and shared equivalence rationale, replacing them with a pure pointer to each Table M decision. Keep M6's decision supported only by O10's explicit priced acceptance, and do not describe its state model as measured."}]}