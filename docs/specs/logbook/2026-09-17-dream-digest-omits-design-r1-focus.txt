DESIGN REVIEW of a work-package spec; no product code has been written yet. Review ONLY these two files: docs/specs/WP-dream-digest-omits-own-job-alerts.md and docs/specs/logbook/2026-09-17-dream-digest-omits-own-job-alerts-round-zero.md. Exclude everything else in the diff, and exclude docs/marketing/, memory/research/ and userreports/ entirely. The spec proposes that the nightly dream job, when it re-renders state/digest.md at the end of a successful run, omits the "job has failed" alert records belonging to the job whose success this very run establishes (identity taken from the WIENERDOG_JOB environment variable, accepted only when the scheduler's job list resolves it to run: builtin:dream; every doubt shows the callout). Challenge specifically: (1) whether WIENERDOG_JOB plus the findJob cross-check can be spoofed or can mis-resolve so that a REAL, still-unresolved failure callout is hidden from the user - hiding a genuine alert is the worst outcome here, showing a stale one is merely the status quo; (2) whether every path in src/cli/dream.js that renders the digest, or that exits before or after rendering it, is accounted for in Table A, including the owner-unknown lock throw, the busy lock decline, and the "nothing admitted but something excluded" throw that now sits AFTER the first render site; (3) whether the filter can drop alerts for a DIFFERENT job, or for an earlier failure of the same job that this run's success does not actually resolve; (4) whether every acceptance criterion and RED-proof declaration can discriminate a correct implementation from a plausible wrong one; (5) whether every file:line citation resolves to the construct it names on this tree - verify them by reading the files. The spec's "Dispatch precondition - owner items" section lists decisions reserved for the owner as recommendations with overrule costs; disagreement with a recommendation is welcome as a finding, but the fact that they are unratified is by design and is not itself a finding. Report what you executed versus what you only read.
