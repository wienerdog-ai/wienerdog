# Raw gate output — wd-reviewer (opus), PR #215 round 1, head a1bd795f (base main = 5fa23ea5). Committed BEFORE adjudication. Condensed verbatim from the agent's final report.

## REQUEST-CHANGES

1. [criteria] band A — tests/integration/adopt-e2e.test.js:563 (body :600): the new round-trip test calls the real adopt path without a resolvable `claude`, so it fails on any host without one (CI, both test legs). Confirmed by re-running with claude removed from PATH: WienerdogError "adoption needs a working claude on your PATH" at src/cli/adopt.js:258. The test's `claude-absent` is only CLAUDE_CONFIG_DIR; PATH is never set, so it inherits the developer's PATH and pins the real binary. Criterion 4 not established on a clean host; V1 not reproducible.
2. [contract] band B — same site: the Deliverables cell requires the test be "built on the setup the existing test in this file already performs"; the omitted step is the load-bearing one — :106-119 seeds FAKE_BRAIN at <home>/.local/bin/claude (0755) and prepends it to PATH (PATH is in ENV_KEYS :47, so the finally already restores it). Apply exactly that.
3. [quality] band C — src/core/dream/promote.js:1063: the rewritten row-Z2 sentence cites `layout.js:65-71`, stale by this same hunk (the function now occupies layout.js:67-74). Correct the range or name the construct.
4. [convention] band C — the commit is `feat(dream): …` while the spec's DoD pins the PR TITLE as `fix(dream): …` (the PR carries it); both valid; the dispatch message and the DoD restate one contract with different prefixes (ADR-0031 mirror shape) — no change to this PR.
5. [quality] band C — tests/unit/dot-segment-denial.test.js:139 and :276 each call readSeedAndN(): in acceptance mode T1 and T4 draw independent seeds (three disjoint samples, two seed lines); under WD_HELD_OUT_REPLAY=1 both seed makeRng from the same string, so B1's and B2's samples coincide — a replay is marginally weaker than the acceptance run. Not a contract violation; recorded.

### What verified clean
Boundary: eight files = seven Deliverables rows + the spec; boundary-check 0; the spec diff is exactly the one-line status flip (Table D not shrunk). Contract fidelity: the loop is LAST in admit before `return null`, refusal string byte-identical; exactly one loop + three comment edits in promote.js, nothing else moved (INSTRUCTION_BASENAMES, DENIED_SEGMENTS, DENIED_BASENAME, clause order, refusal strings, exports, the reportRel filter); layout.js one clause after `..`, one JSDoc sentence, export added; layout-infer.js copy deleted, shared one required, `path` still used; dream-promote.test.js drops exactly the three values. Test identities: six titles byte-match Table C, band markers on every assertion. Oracles: Table D (29) and Table E hand-written literals; refSeg/refPath/refValue are A11 written out; boundary total 39 matches V2; codepoint audit exact (U+00E9 precomposed vs NFD U+0301 where the spec distinguishes; U+2024/U+FF0E). B3 grades the joined reports_dir (:326); E9 pins .myreports → reports/dreams (:401). Input controls mirror V2's guards; generator distribution asserted for all three sets. RED partition: C1 → exactly T1+T2+T3 (ERR_ASSERTION, pass 3/fail 3); C2 → exactly T4+T5 (pass 4/fail 2); find strings occur exactly once; expectRed equals observed both ways. Conventions clean; ADR-0004 holds. ADR-0031: no new contract-dense prose; the row-Z2 mirror corrected in both directions the spec required; residual drift = finding 3. Spec observation (not routed): the adopt-e2e Deliverables cell's parenthetical enumerates the borrowed setup and omits the .local/bin/claude seeding + PATH prepend.

### EXECUTED (19 commands, git-archive copies; worktree porcelain identical before/after)
V1 npm test 0 (2618/2606/0 fail/12 skipped); V2 acceptance 0 (seed 1788540899607, 112/112 first code points, dot share 0.482, 2000/2000|2000/2000|1000/1000|12/12|87/87|39/39, V2 OK); N=999 1; seed w/o replay 1; replay w/o seed 1; V3 red-proofs 0 (5 declared/5 selected, PROVEN both ids, RUN: PROVEN); V4 lint 0 (635 files); boundary-check 0; C1 self-check 1 (expected); C2 self-check 1 (expected); find-occurrence audit 1/1, 1/1; title audit 6/6; codepoint audit 0; adopt-e2e with claude 0 (6/6); adopt-e2e without claude 1 (4/6, finding 1).
