Adversarial DESIGN review, round 18 (confirming, final), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-17 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..17}-astra-raw.json and -design-review.md. Round 17: hypothetical shelf roots had lost descendant protection. Fix: a hypothetical shelf root keeps full class (i) subtree protection in every deletion guard (X16/X18/X20); the ONLY thing conditioned on an existing shelf is X19's alias-retention decision, stated in X19's own cell; rounds 15 and 16 re-checked and still closed; AC 14 re-checked; AC 18(b) gains the physical-path arm; RED quse-hypothetical-root-not-subtree; twenty-two declarations over thirteen criteria; nothing inside the three named residuals for three rounds.

Final confirming round. Apply the three named residuals first (R-alias-outside-closure, R-post-ledger-preserve, R-post-uninstall-preserve) — anything inside them is CLOSED, do not report. Then, read-only: (1) with the definition settled, walk the guard predicate ONE more time over the complete list of measured shapes from rounds 1–17 (the race, the case-fold, the symlinked state, the symlinked quarantine, the sibling alias, the state→logs alias, the symlink replay kind, the two-hop chain, the symlinked core, the absent-shelf state alias, the physical-path entry) and confirm every one gets its intended outcome under the FINAL X16/X17/X18/X19/X20 text — a table, shape → outcome → row that decides it; (2) twenty-two declarations: anchors unique; no cross-reddening; (3) whole-document consistency: Tables K/X/V/W/Y/B, ACs 1–19, both mirror classes, Platform scope, owner items 1–4 (standing form, no approval claims), cites at both ends; (4) size — M or split (say which and why). If every measured shape gets its intended outcome and nothing about the product is wrong outside the residuals, say approve.
