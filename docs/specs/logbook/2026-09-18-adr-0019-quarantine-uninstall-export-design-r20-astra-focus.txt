Adversarial DESIGN review, round 20 (confirming, final), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-19 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..19}-astra-raw.json and -design-review.md. Round 19: X19(b) tested only "inside a swept dir". Fix: X19(b) retains a <state> alias whenever its resolved target overlaps (equals, contains, or is contained by) ANY of withinAllowedRoot's roots — [paths.core, paths.claudeDir, paths.codexDir, <home>/.local/bin] (manifest.js:742) — closed by construction since every path this command can delete sits under one of them; the swept-directory enumeration kept only as derivation; AC 9 ancestor arm (copy before unlink, injected secrets failure, full retry); AC 14's fixture disjoint from all four roots; RED quse-alias-ancestor-of-target-unlinked; two stale tail sentences in X19 caught by the architect's own whole-cell re-read; twenty-four declarations over thirteen criteria; nothing inside the residuals for five rounds.

Final confirming round. Apply the three named residuals first — anything inside is CLOSED. Then, read-only: (1) the closure claim "every path this command can delete sits under one of the four roots": verify against reverse()'s gate (:872-883) for every kind AND the disposer's sweeps AND the scheduler package's D5b (Ready; it removes recognized schedule files under scheduler roots — ~/Library/LaunchAgents, the systemd user dir, <core>/schedules: are the first two under any of the four roots? if NOT, a state alias into ~/Library/LaunchAgents would be unlinked while D5b sweeps there — is that a hole, or is it excluded because a shelf can never live there (a shelf under LaunchAgents = the alias's own creation, inside R-alias-outside-closure?) — decide and say); (2) AC 14's disjoint fixture still reaches the unlink branch; (3) twenty-four declarations: anchors unique, no cross-reddening; (4) whole-cell re-read of X19, X17, X16, W10, W11; Tables K/X/V/W/Y/B, ACs 1–19, both mirror classes, Platform scope, owner items 1–4 (standing form, no approval claims), cites at both ends; size verdict. If nothing about the product is wrong outside the residuals, say approve.
