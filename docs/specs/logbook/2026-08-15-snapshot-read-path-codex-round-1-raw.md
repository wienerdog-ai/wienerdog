# Adversarial design review — external round 1, on the drafted read-path spec

WP: WP-snapshot-read-path-hardening
Backend: gptsol (Codex subscription), one agent per gate run
Tree: `wp/snapshot-read-path-hardening` @ `7e012a9`, on top of `main` @ `79ba77f`
Read-only check: `git status --porcelain` empty before and after, byte-identical
(the reviewer ran and reported both).

Raw final output, byte-unchanged inside the fence, committed **before any
adjudication** per `docs/runbooks/codex-review.md` ("Rules"). One transport
artifact was decoded and nothing else was touched: the notification channel
delivered `&amp;&amp;` inside one `executed` string where the reviewer had run
`&&`. No finding text, verdict, confidence or line number was altered.

The reviewer's own honesty requirement was met: `executed` lists what actually
ran and with what exit status, including a whole-file-read counterexample and a
Unix-socket open probe. This is a run, not a reading.

`````text
{"verdict":"needs-attention","summary":"Do not ship the spec yet: its descriptor scope leaves a concrete leak/throw path, its bounded-read criterion accepts an unbounded implementation, and its socket expectation contradicts the specified open-failure precedence.","executed":["git -C /Users/felho/dev/repos-to-learn-from/wienerdog status --porcelain (initial) — exit 0; stdout empty","git branch --show-current && git rev-parse HEAD — exit 0; confirmed wp/snapshot-read-path-hardening at 7e012a985d1304de14cb8c9cb1f18477e7e919c1","node limit/flag/scanner-boundary probe — exit 0; confirmed Node v24.18.0 on darwin, MAX_FILE_BYTES == SCAN_MAX_BYTES == 262144, scanner accepts 262144 bytes and returns oversized at 262145, and O_NOFOLLOW/O_NONBLOCK are present","wc -l plus grep -a coverage/comment scan plus CI-platform grep — exit 0; reproduced 205/411 line counts, six broker-wiring call sites, the registered binary-test comment, and ubuntu/macos-only CI","node staged lstat grow/swap/source-directory-symlink reproductions — exit 0; grow skipped as appears-to-contain-a-secret, leaf swap copied outside bytes with skipped=[], directory symlink copied outside bytes with skipped=[]","npm --prefix /Users/felho/dev/repos-to-learn-from/wienerdog test -- --test-name-pattern \"vault-snapshot\" — exit 0; 120 passed","npm --prefix /Users/felho/dev/repos-to-learn-from/wienerdog run lint — exit 0; markdown/frontmatter passed, shellcheck and PSScriptAnalyzer unavailable and skipped","node whole-file-read counterexample using process.execPath — exit 0; read 120965360 bytes successfully, showing an unbounded read can still return the per-file-cap reason","npm --prefix /Users/felho/dev/repos-to-learn-from/wienerdog test — exit 0","node O_NOFOLLOW/O_NONBLOCK symlink-and-FIFO probe — exit 0; symlink open failed ELOOP, unguarded open followed it, nonblocking FIFO open returned immediately with isFile() false, and unguarded FIFO open remained blocked after 500 ms","node Unix-socket open probe with the contract flags — exit 0; lstat reported a socket but open failed before any descriptor/fstat with Unknown system error -102","MANPAGER=cat man 2 close | col -b | grep around EINTR — exit 0; Darwin documents EINTR as a close failure","node oversized Buffer decode probe — exit 0; Buffer larger than MAX_STRING_LENGTH threw ERR_STRING_TOO_LONG","git -C /Users/felho/dev/repos-to-learn-from/wienerdog status --porcelain (final) — exit 0; stdout empty, byte-identical to initial status"],"findings":[{"file":"/Users/felho/dev/repos-to-learn-from/wienerdog/docs/specs/WP-snapshot-read-path-hardening.md","line_start":190,"line_end":198,"confidence":0.99,"title":"The mandated finally starts after a descriptor can already need closing","body":"The open acquires the descriptor before fstat, but the lifecycle rule requires one finally \"around the read.\" A regular-file-to-FIFO or regular-file-to-directory swap can open successfully and then fail the fstat type check without performing any read; the FIFO probe confirmed that O_NONBLOCK open succeeds and fstat returns isFile() false. Following the stated scope literally therefore skips without entering the read-finally and leaks the descriptor. A thrown fstat has no specified skip reason or closure path and can instead escape the snapshot. The same row swallows close failures while the acceptance criteria claim no descriptor is ever left open, so that failure outcome is also internally unresolved.","recommendation":"Define one descriptor-scope try/finally beginning immediately after every successful open and encompassing fstat plus the bounded read; map fstat failure to a named existing reason (normally unreadable), and require fstat refusals to pass through that finally. State an honest, testable close-failure posture rather than simultaneously swallowing close errors and guaranteeing unconditional closure."},{"file":"/Users/felho/dev/repos-to-learn-from/wienerdog/docs/specs/WP-snapshot-read-path-hardening.md","line_start":341,"line_end":346,"confidence":1,"title":"The large-file criterion does not distinguish bounded from unbounded reading","body":"The claimed discriminator is false: an implementation can read the whole source successfully, compare the resulting Buffer length, and return the per-file-cap reason. The executed counterexample read the 120,965,360-byte Node binary—over 461 times the cap—into memory without failing; such an implementation would satisfy this criterion while retaining the resource-exhaustion defect the WP exists to close. This makes the round-zero decision to rely on acceptance coverage instead of a mechanical guard materially unsafe.","recommendation":"Replace the outcome-only discriminator with a binary criterion that observes the read primitive and proves cumulative bytes read and the largest requested range never exceed MAX_FILE_BYTES + 1, while separately asserting the resulting cap reason and absence of a copied/truncated file."},{"file":"/Users/felho/dev/repos-to-learn-from/wienerdog/docs/specs/WP-snapshot-read-path-hardening.md","line_start":328,"line_end":335,"confidence":0.99,"title":"The socket acceptance criterion contradicts open-failure precedence","body":"The criterion requires a socket \"at the descriptor\" to receive the fstat non-regular reason, while the next criterion and Table A require every open failure to become unreadable. On the measured darwin checkout, opening a Unix-domain socket path with O_RDONLY|O_NOFOLLOW|O_NONBLOCK fails before a descriptor exists, so a post-lstat socket swap must be unreadable and can never reach fstat. A pre-existing socket is instead rejected by the advisory lstat. The required socket result is therefore not realizable on the production path and leaves reason precedence ambiguous.","recommendation":"Define outcomes by the first operation that fails: a pre-existing socket is refused by lstat as non-regular; a post-lstat socket swap whose open fails is unreadable; only non-regular objects that open successfully, such as a directory or nonblocking FIFO on POSIX, receive the descriptor-type reason. Remove socket from the fstat criterion unless a real supported platform can supply such a descriptor through fs.openSync."}]}
`````
