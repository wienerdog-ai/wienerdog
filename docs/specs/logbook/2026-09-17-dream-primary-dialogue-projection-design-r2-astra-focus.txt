DESIGN REVIEW ROUND 2 of a NEW work-package spec; no product code has been written yet. Review ONLY docs/specs/WP-dream-primary-dialogue-projection.md and docs/specs/logbook/2026-09-17-dream-primary-dialogue-split.md (your round-1 raw is preserved beside it as 2026-09-17-dream-primary-dialogue-projection-design-r1-astra-raw.json). Sibling specs changed in the same diff are context only. Exclude docs/marketing/, memory/research/ and userreports/ entirely. The spec adds an opt-in, deterministic, code-only projection parsePrimaryWithOutcome returning {extract, gateExtract, intakeBytes, parse} from the Claude Code and Codex CLI transcript parsers (src/core/transcripts/): `extract` is primary dialogue only; `gateExtract` is a text-free projection of the ORIGINAL capped timeline preserving roles and skill-invocation geometry; `intakeBytes` is the raw capped extract's size; derived_from_untrusted is computed over the original stream before projection. PART ONE - verify each round-1 finding is GENUINELY fixed in substance, EXECUTING the real parsers and streaming reader on synthetic inputs as you did before: (P-1) "User-role text bypasses the never-resetting provenance flag" - the disposition KEEPS user text false by role, because that is TODAY's rule (skills/wienerdog-dream/SKILL.md:101-105: true iff any supporting message has role tool_result, false only when every supporting message has role user or assistant), and FIXES the spec's self-contradiction: new row A5b says false claims exactly one thing - the harness attributed the record to the user role (or, for an assistant message, that no tool output and no gap preceded it) - and is NOT a claim a human authored the words, that the content is true, verified, or safe to obey; the monotonic taint state governs ASSISTANT text only; your laundering fixture is now AC3a as a test of STATED behaviour; an owner item prices the alternative (tainting user text would end Tier-3 learning from user statements in any session with an early tool call). Judge: is the statement now internally consistent on EVERY surface, is the claim about today's rule true, and is the residual stated honestly - disagreement with the owner-item recommendation is a welcome finding but its being unratified is not. (P-2) "Explicitly signal depth-limit drops as provenance gaps" - new row A5a enumerates context-losing returns G1 over-long line, G2 JSON depth over the limit (silent today), G3 JSON.parse threw (silent today), G4 budget exhausted mid-file, G5 total outcomes (over-ceiling, read-error, too-many-lines) which return an empty extract; and says records the raw policy classified and declined are NOT gaps. Read stream.js, claude.js and codex.js end to end: is that list COMPLETE - is there any other return or continue that discards a record whose type the code could not determine - and is "could not determine the record's type" the right line between a gap and a declined record? PART TWO - closure: report only findings about PRODUCT or SECURITY behaviour that the spec does not already name as a residual or owner item; list verification-machinery findings separately, marked as machinery. Do the file:line citations resolve on this tree. State what you executed versus what you only read.
