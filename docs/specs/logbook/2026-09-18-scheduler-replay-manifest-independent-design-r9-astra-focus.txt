Adversarial DESIGN review, round 9 (confirming), of docs/specs/WP-scheduler-replay-manifest-independent.md (Draft, M). Rounds 1-8 raws and dispositions in docs/specs/logbook/2026-09-18-scheduler-replay-manifest-independent-design-r{1..8}-astra-raw.json and -design-review.md. Round 8: a launchd plist recorded as a non-scheduler kind is unloaded but preserved (D11 + the file reverser's roots) and reloads at next login; AC 10 still carried the pre-round-3 suppression. Fixes: Table R row R9 names the residual R-preserved-reloadable-plist (reachable only via a hand-edited/corrupted manifest — no code path records a schedule file as anything but scheduler-entry, schedule.js:358; strictly narrower than c05a575b where the job is not even unloaded); the ADR-0041 amendment narrowed to "CLOSED except for R-preserved-reloadable-plist" with the residual as its own row; D3 requires every launchd `keep` line to be followed by a plain-language warning that the job restarts at next login until the named file is removed by hand; AC 10 rewritten (recorded files stay discovered with remove:false; D5a unloads them independently); owner item 3 carries the retryable-refusal alternative with its cost; sweep found no other pre-round-3 vocabulary.

Confirming round, final. Read-only against the tree; never the real scheduler or ~/.wienerdog. (1) R9's reachability claim — verify schedule.js:358 is the only writer of schedule-file records and that it always uses kind 'scheduler-entry'; is there any migration/adopt path (src/cli/adopt.js, src/core/manifest.js migrations) that could record a plist under another kind; (2) the narrowed amendment: consistent with the residual table's format in ADR-0041 (`:186-196`), Status/OWNER-SIGNED lines untouched, no approval claim; (3) D3's warning line: plain language, and does it also fire for the systemd/schtasks equivalents (are they reloadable at login? systemd user timers enabled via symlink in the wants dir — a preserved .timer file alone does not re-enable; schtasks XML on disk does not re-register — so launchd-only is correct: confirm and confirm the spec says why); (4) final consistency pass over D1–D15, R1–R9, S1–S12, ACs 1–18, the RED table, checklist, Security; counts, cites at both ends, owner items 1–3 in the standing form. If nothing about the product is wrong, say approve.
