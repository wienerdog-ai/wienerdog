# Raw gate output — Codex plugin adversarial-review, round 1, docs/wp-quarantine-banner-location at 007858a7 (base main = 8302ce8e). Committed BEFORE adjudication. Focus text reproduced at the end.

# Codex Adversarial Review

Target: branch diff against main
Verdict: needs-attention

NO-SHIP: the pointer is false on report-failure arms, and V2 admits a demonstrated second-author implementation. V5 also false-greens incomplete amendments. Census confirmed 13 `state/quarantine` hits across 7 source files, no matching template/skill carrier, and the six existing pins match the spec. `npm run lint` passed; `npm test` could not start because the read-only sandbox denied `mkdtemp`. Initial and final `git status --porcelain` were both empty.

Findings:
- [high] [A] The pointer targets a report that may not contain the preservation record—or exist (docs/specs/WP-quarantine-banner-location.md:376-381)
  The byte-exact sentence promises that “each dream report names its own copies and the folder each one is in,” but the spec's own cited fallback contradicts this. When the report is refused, no report is written and the complete record exists only in command output; when only the report body is published, the report exists but its enforcement section is absent and the record is again printed separately. `src/cli/dream.js:1135-1163` explicitly distinguishes these cases and says refused output is “not stored anywhere else.” Persistent ledger, warnings, and doctor surfaces can therefore direct a later user to a nonexistent or incomplete report, concealing the only actionable location of a redacted-shelf copy.
  Recommendation: Change the canonical sentence to name both real channels—for example, the applicable dream report or that run's command output—and add assertions for refused-report and partially-published-report outcomes. Do not claim the report itself always contains the record.
- [high] [A] V2 false-greens a composed duplicate and dead import (docs/specs/WP-quarantine-banner-location.md:681-697)
  V2 counts only the contiguous prefix and merely checks that each file contains the identifier somewhere. A consumer can import `PRESERVED_COPIES_POINTER` but never use it, then render the identical sentence from two concatenated literals. The prefix count remains one, every carrier contains the symbol, V1 stays green, and runtime equality/contains tests also pass because the duplicated value equals the constant. An in-memory probe reproduced exactly `V1/V2 OK (false green)`. This defeats criterion 5's central single-author guarantee, so the stated residual is not honest: V2 catches contiguous retyping, not equivalent composition or a one-byte variant.
  Recommendation: Make V2 verify the exact syntactic use at every render site, not mere identifier presence, and count expected identifier occurrences/contexts. Add a guard that proves each carrier's rendered value changes with the canonical definition, or explicitly record equivalent/composed retyping as an uncovered residual.
- [medium] [B] V5 accepts placeholder amendments while criterion 8 claims byte-exact coverage (docs/specs/WP-quarantine-banner-location.md:699-724)
  V5 checks only whether the common date/WP marker appears somewhere in cell 4. Appending just `Amended 2026-09-05 (`WP-quarantine-banner-location`)` to Q1, Q2, and Q9 passes V5 even though none of the three required row-specific clauses exists; edits elsewhere on those same lines are also invisible. An in-memory simulation produced `V5 OK` for all three marker-only rows. Tests and lint do not validate these canonical Done-spec assertions, leaving only downstream human review to catch the corruption.
  Recommendation: Extend V5 to compare each fourth-cell suffix with its full byte-exact clause and verify the diff changes exactly Q1, Q2, and Q9, one line each, with no other edits in the Done spec.

Next steps:
- Revise the pointer contract and V2 before dispatch; strengthen V5 or narrow criterion 8's evidence claim.
- Scope-routed objection, excluded from the verdict as requested: L5's `this notice clears when no withheld copies are left` remains false when `listSecretQuarantine()` excludes a fall-through copy in `redacted/`. The parenthetical itself remains correct and protective. Route the wording correction to separately authorized L5 scope or the successor WP.
- Executed commands and statuses: combined initial `git status --porcelain && git diff main...HEAD --stat && git diff --name-only` (0); two `rg` carrier censuses (0, 0); digest/promote/dream `sed` inspection (0); report-record `rg` inspection (0); proof-runner/test `sed` and `rg` inspection (0); source/spec `sed` and `nl` inspection (0); untouched-tree V5 (1, expected RED); Q1/Q2/Q9 cell split (0); exact census counts (0); heredoc probe (1, sandbox denied temporary file); in-memory V2 false-green probe (0); in-memory V5 false-green probe (0); `npm run lint` (0); `npm test` (1, sandbox `EPERM` on `mkdtemp`); remaining numbered source/spec inspections (0, 0); `git diff --check` plus numstat (0); final `git status --porcelain` checks (0, 0; empty).

---- focus text ----
SCOPE: docs/specs/WP-quarantine-banner-location.md (Draft, 824 lines) and docs/specs/logbook/2026-09-05-quarantine-banner-location-design-gate-rounds.md, as changed relative to main (run: git diff main...HEAD --stat). Design review of a work package handed to a sonnet implementer who reads ONLY the spec and CLAUDE.md. The WP: the EP2 secret gate's redact-arm fall-through can leave a withheld note's ONLY preserved copy on the `state/quarantine/redacted/` shelf (disposable per GLOSSARY; evicted above 50 by pruneRedactedOriginals), while four ledger-derived user-facing surfaces (ledger.js's exhausted banner L1 and summary line L2, warnings.js's remediation line L3, doctor.js's quarantineReport line L4) tell the user the copies are in `state/quarantine/` and one instructs "delete the rest". The fix: ONE exported code-owned pointer sentence (Table L row L0, `PRESERVED_COPIES_POINTER` in ledger.js) that names no folder and issues no disposal instruction, imported by L3/L4; the digest's own banner L5 (which reads the folder it names) is NOT changed; three RED proofs (Table C, identities T1–T3 with testNamePattern); dated additive clauses on three rows (Q1/Q2/Q9) of the Done spec WP-secret-fence-ep2-redact-arm, placed in the cell carrying the claim (V5 checks placement). Owner items: the Dispatch precondition asks the owner to confirm the four surfaces stop naming a folder and stop instructing a delete (recommended); the `remediation: 'delete'` value in the dream report is owner-ruled (WP-dream-promote-module Table Q row Q9) and is NOT changed; the durability of the only copy is the successor WP-quarantine-preserve-durability's.
CHALLENGE — every item is about whether a WRONG implementation passes or a RIGHT one breaks something: (a) the census — is the list of carriers complete: grep `state/quarantine` and `quarantine/` across src/ and templates/ and skills/ (the WP says 13 hits, 7 files, 5 user-facing, 4 to change, L5 kept); is any user-facing sentence that names the folder missing from Table L, including in templates or the digest's rendered markdown, CLI help text, the doctor's other lines? (b) L5 — the digest's banner is kept because "it reads the folder it names": verify from src/core/digest.js that listSecretQuarantine's `.isFile()` filter means the banner is emitted only when a copy is directly under state/quarantine/, and judge whether the parenthetical "(not the redacted/ folder inside it)" remains correct and non-misleading when a fall-through copy also sits in redacted/ — any way L5 now says something false? (c) the pointer sentence — read the byte-exact sentence under Implementation notes: does it point at a surface that ALWAYS names each copy's folder (the dream report's refusal record) — verify from src/cli/dream.js and promote.js that the report names `state/${location}/${artifact}` for every preserved copy including the redacted/ one; any preserved copy the report does not name? (d) V2's single-author check — it is lexical; can a retyped sentence with one different byte (or a template literal composing the same text) pass V2 and defeat L0? Is the residual stated honestly? (e) Table C — read scripts/red-proofs.js validateProof/evaluateRed and the three declarations as specified: do the `find` strings occur exactly once on the FIXED tree, does each mutation redden exactly its identity under the declared testNamePattern, and does T3 (testing warnings.js from inside ledger.test.js) create an attribution problem for the runner? (f) the Done-spec amendments — split rows Q1/Q2/Q9 of docs/specs/done/WP-secret-fence-ep2-redact-arm.md on ` | ` and confirm the claim each clause scopes lives in the named cell; run V5 as written on the untouched tree (red) and reason about the compliant/violating states; does any clause change what the Done row ASSERTED rather than annotate it? (g) the six breaking tests — the WP names exactly six existing assertions in four files that the fix reddens and updates; is any updated pin weakened to a substring or a regex where a full-string equality existed (a vacuity regression)? (h) Deliverables/boundary — does the implementer need to touch anything not listed (digest.js, GLOSSARY, templates, docs)? ADR-0004 and size M honest?
OUT OF SCOPE — file as scope objections in a separate routed section, do NOT count toward the verdict: the Dispatch-precondition item itself (the owner's), the owner-ruled `remediation: 'delete'` value, the successor's durability/retention/shelf questions, any code change (nothing in src/ or tests/ changes on this branch), and every file other than the two in scope.
REPORT: state what you EXECUTED (each command with exit status), then findings with file:line and a materiality band (A: a criterion a wrong implementation passes, or silent wrong behaviour, or a user-facing sentence left false; B: caught downstream; C: hygiene), then the verdict. Review is READ-ONLY: git status --porcelain must be identical before and after; probes in memory where the sandbox denies temp writes.
