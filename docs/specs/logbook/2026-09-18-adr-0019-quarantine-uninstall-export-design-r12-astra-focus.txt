Adversarial DESIGN review, round 12 (confirming), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-11 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..11}-astra-raw.json and -design-review.md. Round 11: the disposer's own recursive sweep could delete a shelf through a sibling alias. Fix: Table X row X18 — X16's symmetric containment check over X17's anchors applied to every recursive deletion the package performs, in the order protected targets → step 2 → step 3; a target that contains, equals or is contained by a protected resolved target is preserved-and-reported; extended to the mechanics loop's :1148 rmSync over logs/schedules/secrets; Table V accounts for every `recursive: true` hit instead of a fixed count; criteria 1 and 9 assert the alias target's survival before anything about the link; RED quse-step2-deletes-through-alias; convergence note: two rows (X16 containment, X17 resolution) cover the whole deletion surface, no site exempt.

Confirming round, final. Execute against the tree (temp dirs only): (1) the last mutating operation this package does not yet name — step 0's unlinkSync of a symlinked <state> and the pre-existing empty-core rmdir: can EITHER remove a shelf through an alias (unlink removes only the link — the target survives — fine; the empty-core rmdir is non-recursive — fine) — confirm both are stated as outside X16 for that reason; (2) with X18 extended to logs/schedules/secrets: a shelf symlink resolving INTO <core>/secrets now preserves secrets/ (credentials left behind) — is that outcome disclosed and is it the right trade (preservation over credential cleanup because the alias means user text is there) — and does the refusal/notice make that clear; (3) fifteen declarations: anchors unique; no cross-reddening; the new one's mutation produces the dangling-link-in-preservedQuarantine signature and nothing else reddens; (4) a whole-document consistency read: Tables K/X/V/W/Y/B, ACs 1–19, both mirror classes, Platform scope, owner items 1–3 (standing form, no approval claims), cites at both ends; every "exempt"/"ordinary mechanics" phrase gone. If nothing about the product is wrong, say approve.
