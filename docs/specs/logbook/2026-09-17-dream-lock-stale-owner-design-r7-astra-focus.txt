DESIGN REVIEW ROUND 7 (closure round) of a work-package spec; no product code has been written yet. Review ONLY docs/specs/WP-dream-lock-stale-owner-loud.md and the 2026-09-17 dream-lock-stale-owner / dream-lock-silent-stall logbook entries under docs/specs/logbook/ (prior raws are beside them, including -design-r5-astra-raw.json and -design-r6-astra-raw.json). Exclude docs/marketing/, memory/research/ and userreports/ entirely. Background: src/core/dream/lock.js keeps an EXPIRED single-run dream lock when its recorded local PID still exists, and src/cli/dream.js exits 0 on that busy decline, so the supervisor records success; a lock left by an uncleanly-killed dream whose PID is later reused stops memory consolidation indefinitely with no alert. The spec does exactly three things: (a) a busy decline more than 6 hours past the lock's deadline throws (exit 1 - the existing alert and email path) with a byte-exact plain-language message; (b) a finite deadline more than an absolute 24 hours ahead of now is classified as the existing owner-unknown; (c) an exit-code doc comment is corrected. No takeover decision changes. PART ONE - verify the round-6 finding is GENUINELY fixed: "S5 still equates an expired busy lock with stopped vault activity". The message now begins "dream lock is overdue:" and states only what this attempt observed (the lock has been held more than N hours past its own time limit, so this dream did not start); says the dream that took the lock may still be working or may have stopped without releasing it and that Wienerdog cannot tell which; says that if it is still working it will release the lock when it finishes and the messages will stop on their own; offers a restart as an option that ends whatever holds the lock including a dream that is still working, after which Wienerdog normally clears the lock itself at its next run; names the lock file; and says removing it by hand is only safe while no dream is running, so someone should check first rather than deleting it on a guess. Check EVERY sentence of the byte-exact message against the code for truth, and check that no mirror (Table S, acceptance criteria, declaration find strings, ADR amendment block, owner items, worked timelines) still carries the withdrawn claims or the old "stale" literal where the new one is required. PART TWO - closure: report only findings about PRODUCT behaviour a user or consuming process would observe that the spec does not already name as a residual or owner item; list verification-machinery findings separately, marked as machinery. Do the file:line citations resolve on this tree. State what you executed versus what you only read.
