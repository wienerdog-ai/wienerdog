DESIGN REVIEW ROUND 2 of a work-package spec; no product code has been written yet. Review ONLY: docs/specs/WP-dream-digest-omits-own-job-alerts.md and docs/specs/logbook/2026-09-17-dream-digest-omits-design-review.md (plus the round-1 raw at docs/specs/logbook/2026-09-17-dream-digest-omits-design-r1-raw.json for reference). Exclude docs/marketing/, memory/research/ and userreports/ entirely. The spec proposes that the nightly dream job, when it re-renders state/digest.md at the END of a successful run, omits the "job has failed" alert records belonging to its own job. PART ONE - verify each round-1 finding is GENUINELY fixed in substance, not re-worded: (F1) "The config cross-check does not establish that run-job launched this process" - the revision adds a second conjunct: the supervisor-minted WIENERDOG_DREAM_RUN_TOKEN (src/cli/run-job.js about l.934-938) must be present and pass the existing validation (src/cli/dream.js about l.827-828), and it records "possession, not parentage" as a residual under owner item O1. Is that an honest narrowing, is the win32 claim right, is the token really minted on the catch-up path, and does any AC prove that a direct `wienerdog dream` with inherited WIENERDOG_JOB but no valid token still shows the alert? (F2) "The shared filter hides an unresolved prior failure before this run is known to succeed" - the revision filters ONLY at the final render (step 19), leaves the early quarantine render unfiltered, and re-renders UNFILTERED exactly once if anything in-process fails after the final render (original error wins). Is the default really unfiltered, is there exactly one filtering call site, and do AC6a-d assert digest CONTENT on all four paths? (F3) "The unconditional-filter RED proof does not prove the required cross-check" - Table C is rebuilt one-rule-per-mutation with AC4 in the config-ignoring mutation's exact expectRed set. Check it. PART TWO - attack the NEW mechanisms: can the late-failure re-render itself hide or corrupt anything, double-render, or mask the original error; is there any remaining path where a GENUINE unresolved failure is absent from digest.md that the spec does not name (the spec names exactly one narrowing: out-of-process failures B1/B2 after the dream process has exited - is that narrowing stated truthfully and is its bound real); can the token conjunct make the feature silently never engage on a supported platform; do the six RED declarations each discriminate; do all file:line citations resolve on this tree. Owner items are recommendations with overrule costs by design; disagreeing with one is a welcome finding, their being unratified is not. Report what you executed versus what you only read.
