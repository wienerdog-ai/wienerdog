# Raw gate output — Codex plugin adversarial-review, round 3, docs/wp-quarantine-banner-location at f198263a (base main = 8302ce8e). Committed BEFORE adjudication. Focus text reproduced at the end.

# Codex Adversarial Review

Target: branch diff against main
Verdict: needs-attention

NO-SHIP: one Band A gap remains. R2-A is genuinely closed by V5’s byte comparison; R2-B is closed; the R2-C reorder is coherent, but its detector accepts an incorrect placement. V5 returned the expected RED on the untouched tree; current clauses contain no `|`, and their backticks survive shell capture safely. All six declaration mutations matched once and passed `node --check` in an in-memory candidate. Full RED-suite rehearsal was impossible under the read-only sandbox. Size M/one atomic WP is honest: L7 is mechanically separable but semantically required by the new pointer sentence, so splitting creates sequencing risk.
Executed: `git status --porcelain && git diff -- <scope> && git diff --stat main -- <scope>` (0); scoped `rg` census (0); four `nl|sed` WP reads, `nl` dream read, broad Done-spec `rg` (all 0); focused dream/test/Done-spec inspections (three calls, 0); three further WP chunks (0); extracted `sed -n '1015,1049p' ... | sh` V5 (1, expected RED); drift/base `rg` (0); Done-spec G11 inspection (0); write-path trace (0); logbook round-2 inspection (0); source-form inspection (0); scratch heredoc attempt (1, sandbox denied temp creation); in-memory six-declaration find/parse validation (0); clause delimiter/backtick probe (0); `git diff --check main -- <scope>` (0); one status wrapper (1, zsh reserved-variable error); final `git status --porcelain` (0, zero bytes). Initial and final porcelain output were identical and empty.

Findings:
- [high] [Band A] The L7 test allows delivery after the ledger claim (docs/specs/WP-quarantine-banner-location.md:939-945)
  Criterion 6 injects failure only at the digest write, after `writeLedger` and the pointer-bearing summary. An implementation that relocates the record print after those operations but before `regenerateDigest()` will satisfy the proposed assertion—the record is present before the digest fault—while violating L7’s required pre-step-18 order. A ledger failure in that incorrect ordering can therefore persist or print the new claim before the only-copy record is delivered. The round-2 evidence exercises shipped-versus-proposed ordering, but not this intermediate wrong ordering.
  Recommendation: Require a detector at the ledger boundary: inject on `fs.renameSync` for the ledger destination and assert the complete record was already captured before that call/fault, or record and assert exact call order (`record output` before `writeLedger`, summary, digest, and warnings). Rehearse the explicit wrong placement between summary and digest and require RED.

Next steps:
- Close the Band A detector gap before dispatch.
- Routed scope objection, not counted: owner must confirm L1–L4 stop naming a folder and instructing deletion.
- Routed scope objection, not counted: carry L5’s false clearing sentence to the shelf/retention successor.

---- focus text ----
SCOPE: docs/specs/WP-quarantine-banner-location.md (Draft, 1194 lines) and docs/specs/logbook/2026-09-05-quarantine-banner-location-design-gate-rounds.md, as changed relative to main. ROUND 3 — the closing confirmation. The WP: four ledger-derived user-facing surfaces (L1–L4) stop naming `state/quarantine/` and stop instructing a delete; one exported pointer constant (L0, PRESERVED_COPIES_POINTER) imported by L3/L4; the digest's banner L5 unchanged; NEW since round 2 — src/cli/dream.js reordered so the undelivered `report.record` is printed at step 17b BEFORE the withheld ledger is written and before any pointer-bearing surface is persisted (Table L row L7, criterion 6, a failure-injection test in tests/unit/dream-pipeline.test.js patching fs.renameSync on the digest destination); V5 reconstructs each amended Done-spec row as `git show main:` base + clause and byte-compares; six RED proofs in two inlined declaration files (three shelf-claim, three derivation). Owner items (Dispatch precondition, routed, not counted): confirm the four surfaces stop naming a folder / instructing a delete (recommended); L5's "clears when no withheld copies are left" recommended to the successor.
ROUND 2 FINDINGS (verify each is GENUINELY closed): (R2-A, converged) V5 accepted same-line reauthoring inside an amended row — now a byte-compare against base+clause (rehearsed: marker-only, cell 2, extra line, prefix reauthoring, middle-cell reauthoring all red). (R2-B) the doctor-proof contradiction in Out of scope removed; counts corrected. (R2-C, the pointer-truth family twice → design move) the crash window between persisting the pointer and printing the undelivered record is DELETED by reordering dream.js (two hunks, no line rewritten, step 17b; full suite unchanged; injection test red on the shipped ordering, green on the reordered).
CHALLENGE THE FIXED POINT: (a) the reorder — read src/cli/dream.js around the current steps 17–20 and the spec's byte-exact comment header: after the move, is there ANY durable pointer-bearing write that still precedes the undelivered-record print (the ledger, the digest, warnings.md, the report file itself on the `promoted`-unpublished arm, the doctor's inputs, git commits), and is there any arm where the record is printed twice or not at all now? Does moving the print change what row G11 or any other Done-spec row guarantees about output ORDER (grep the done specs for the step numbers and for "before"/"after" claims about this print)? Does step 17b's placement interact with the exhausted-transcript flow or with a run that has no record at all (empty record → nothing printed, still fine)? (b) the failure-injection test — patching fs.renameSync on the destination: does that seam actually sit inside the window on the current tree (trace writeFilePrivate/regenerateDigest), does the test assert the record's PRESENCE in output captured BEFORE the fault (not merely that a fault occurred), and could a wrong reorder (printing after the ledger write but before the digest) pass it? (c) V5 — extract it from the fenced block and run it on the untouched tree (red for the right reason); reason about whether `git show main:` is the right base when the branch is rebased or when main moves under dispatch (dispatch-time re-verification catches drift — is that stated?), and whether a clause containing a `|` or a backtick can break the reconstruction. (d) the six declarations — on a scratch copy carrying the Exact-contracts fix AND the reorder: each find once, each mutation parses, each reddens exactly its identity under its testNamePattern (the dream-pipeline.test.js file now gains a test — does any declaration's suite include it and does the L0 mutation redden it undeclared?). (e) size M and one atomic WP with two src files now — honest? Should the reorder + injection test be its own S package sequenced first (it is a standalone correctness fix) — argue both ways and recommend. (f) anything ELSE material a sonnet implementer reading only this spec would get wrong.
OUT OF SCOPE — file as scope objections in a separate routed section, do NOT count toward the verdict: the two Dispatch-precondition owner items, the owner-ruled `remediation: 'delete'` value, the successor's shelf/retention/durable-state questions, the adjudicated round-zero/1/2 dispositions, any code change on this branch (there is none), and every file other than the two in scope.
REPORT: state what you EXECUTED (each command with exit status), then findings with file:line and a materiality band (A: a criterion a wrong implementation passes, silent wrong behaviour, or a user-facing sentence left false; B: caught downstream; C: hygiene), then the verdict. Review is READ-ONLY: git status --porcelain must be identical before and after.
