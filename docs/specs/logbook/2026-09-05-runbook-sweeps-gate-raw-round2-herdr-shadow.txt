# Design gate raw — hermetic Codex shadow (codex exec, read-only sandbox, CODEX_HOME=~/.codex-review-home, gpt-5.6-sol high) — WP-process-runbook-sweeps
# tip reviewed: 3e3bfde28b13be2951fff23da820460aae9131c6 (detached worktree) ; base main = 98a8b49a6ba1dae627d0d93470a19385c7e89657 ; committed pre-adjudication

{"target":"3e3bfde28b13be2951fff23da820460aae9131c6 vs 98a8b49a6ba1dae627d0d93470a19385c7e89657","verdict":"needs-attention","summary":"Do not ship yet. The sentinel wrapper retains a concrete stale-script false-green path; R12's promised band mapping is absent from the live criterion; the supposedly exhaustive stop criterion still has unrouted/unranked outcomes; and R1-F's owner-mirror sweep remains incomplete. No scope objections.","findings":[{"title":"Failed script creation can execute a stale sentinel and report green","file":"docs/specs/WP-process-runbook-sweeps.md","line_start":398,"line_end":463,"materiality_band":"A","confidence":0.99,"body":"The fence writes the predictable persistent path `/tmp/wd-sweeps-sentinels.sh` but does not check `cat` before executing that path. If a previous sentinel remains and the current write fails due to permissions or a full filesystem, Bash runs the stale version. I executed the equivalent control flow: creation returned 1, the pre-existing script returned 0, and the wrapper returned 0. A stale round-1 script checking 20 anchors can therefore certify a commit that satisfies those 20 while omitting R13's newly declared 21st anchor. The inner heredoc and processed-count guards do not help because the current script never runs.","recommendation":"Create a fresh path with `mktemp`, fail immediately if writing it fails, remove it with a trap, and propagate the executed script's status. At minimum, guard the existing write with `if ! cat ...; then exit 1; fi` and ensure later commands cannot overwrite a failing gate status."},{"title":"R12's required materiality-band mapping is absent from the live criterion","file":"docs/specs/WP-process-runbook-sweeps.md","line_start":179,"line_end":180,"materiality_band":"B","confidence":0.99,"body":"R12 requires the pinned criterion to map every A/B/C band to an outcome and express outcomes in terms of both consequence bands and LIGHT/HEAVY fix weights. The live round-2 criterion at logbook lines 1481-1507 never consults A/B/C; its branches classify subject matter and its aggregation ladder contains only CLOSE/LIGHT/HEAVY/EXTRACTION/DESIGN. The round-1 table also leaves R1-C as the non-band `A/B`. Thus the claimed R1-E reconciliation has not produced the operative mapping Table B requires.","recommendation":"Define the outcome for each A/B/C and LIGHT/HEAVY combination in the live criterion, and adjudicate every consolidated finding to exactly one band. If bands are intentionally reporting-only, narrow R12 instead of claiming they determine outcomes."},{"title":"The revised stop criterion is still not exhaustive or totally ordered","file":"docs/specs/logbook/2026-09-05-process-runbook-sweeps-design-gate-rounds.md","line_start":1481,"line_end":1507,"materiality_band":"B","confidence":0.98,"body":"Two counter-cases remain. First, a finding changing the spec's global operative/provenance contract at spec lines 112-118—without changing a Table A disposition or Table B row—is implementation-affecting but matches none of branches 1-6 and cannot match branch 7's `Nothing about the product`. Second, branch 6 produces FALLBACK for a repeated screen finding, but FALLBACK is absent from the aggregation ladder, so a mixed round containing it has no defined `most escalating` outcome. R1-D was therefore narrowed, not fully fixed.","recommendation":"Add an ordered catch-all for any other implementation-affecting contract change, and place FALLBACK explicitly in the outcome order or define it as a terminal override with stated fresh-round behavior."},{"title":"R1-F's owner-item mirror enumeration still omits required edits","file":"docs/specs/WP-process-runbook-sweeps.md","line_start":282,"line_end":327,"materiality_band":"B","confidence":0.99,"body":"O3 claims to enumerate the whole PARTIAL/ALREADY-BOUND set but lists R01, R02, R08, R09 and R15-R18, omitting newly-PARTIAL R12; the later per-file cost list does include R12, making the item internally inconsistent. O1 also says to update the Context's five-file scope only if the count changes, although moving R14 from `wd-architect.md` to `docs/ARCHITECTURE.md` replaces one file with another and leaves the count at five while still requiring the exact path list at lines 25-30 to change. These are precisely the mirrored edits R1-F required O1-O3 to enumerate.","recommendation":"Add R12 to O3 and its checklist mirror, and require O1's Context path list to change whenever the host file changes, regardless of whether the file count changes. Re-walk every O1-O3 overrule against the registered mirrors."}],"executed":"READ: complete numbered reads of the two reviewed files; the named runbooks, template, agent files, relevant HANDOVER/inbox evidence, and both round-1 raw records using `nl`, `sed`, `cat`, and `rg`. EXECUTED: scoped `git diff`/`git status`/`git rev-parse`; the round-1-to-HEAD diff; Table A versus re-derived ID comparison (R01-R19 aligned); Table/EXTEND/DATA counts (19/17/9/21); the extracted sentinel (rc=1 on observed heredoc-transport failure); a shortened-DATA sentinel (rc=1); an equivalent failed-write/stale-script wrapper (creation rc=1, stale execution and overall rc=0); non-regression greps (all rc=0); `git diff --check` (rc=0); and `npm run lint` (rc=1 because markdownlint could not resolve registry.npmjs.org; frontmatter passed for 268 specs and 4 agents). Final worktree porcelain was empty."}
# codex exit=0 ; porcelain before/after: identical
