# raw — hermetic Codex shadow (codex exec, read-only sandbox, CODEX_HOME=~/.codex-review-home, gpt-5.6-sol high) — design-r2
# tip reviewed: 235af36983b51f2b3e0f1e8c9640b7f65b3b5300 (detached worktree) ; base main = 4b629ec6d1173c3724f9bc0e3d7c658eed062541 ; committed pre-adjudication
# codex exec rc=0 ; porcelain before/after: identical

{"target":"235af36983b51f2b3e0f1e8c9640b7f65b3b5300","verdict":"needs-attention","executed":["Verified HEAD, clean status, changed-file list, and diff stat against 4b629ec6d1173c3724f9bc0e3d7c658eed062541 — exit 0; HEAD matched target","Generated the full base-to-HEAD diff — exit 0","Read all mandated changed and cited files in full; SHA-256/line-count audit covered 18 files — exit 0","Mechanically resolved all 25 file:line citations in the primary spec — exit 0","node --check on the cited JavaScript files and JSON.parse of dream-pipeline.proofs.json — exit 0","Targeted rg/nl audits of git seams, path construction, Table W, Table U, acceptance criteria, owner records, and RED-runner equality logic — exit 0","git diff --check — exit 2; seven trailing-whitespace lines remain in the design-gate logbook","npm test — exit 1 before tests ran; read-only sandbox refused mkdtemp with EPERM","npm run lint — exit 1; markdownlint dependency lookup failed with ENOTFOUND, while frontmatter validation passed","node scripts/red-proofs.js — exit 1 before proofs ran; sandbox creation failed with EPERM","node scripts/red-proofs.js --wp WP-dream-git-env-pinning — exit 1 with the expected VACUOUS result because implementation declarations do not yet exist","V5 mirror greps — exit 1 at the expected absent ADR amendment; boundary check — exit 1 on the expected design-only successor stub","Executed git var GIT_AUTHOR_IDENT/GIT_COMMITTER_IDENT with ambient identity variables plus the exact -c user.name/user.email options — exit 0; ambient identities won","Executed buildCleanEnv/getPaths comparison — exit 0; manual PATH and scheduled PATH differed, HOME matched, XDG_CONFIG_HOME was absent"],"summary":"Do not dispatch. R1-A is fixed; R1-B is only partially fixed because its owner record still says XDG_CONFIG_HOME is carried and the claimed manual/scheduled equivalence remains false through PATH; R1-C, R1-D, and R1-E are genuinely fixed. AC1's value map, HOME-unset discriminator, canary, stateDir/process.pid index path, and nine-shape split are satisfiable; AC5's file-config hook mechanism is realizable; HOME/state overrides and deliverable scope do not invalidate those mechanisms. The config-count RED contract nevertheless omits AC5, and Table U still hides direct identity/replacement behavior and descendant PATH divergence. The latter two are Table U row findings, so STOP branch 3 fires: ADR-0031 requires contract extraction before another row patch. The RED issue is a criterion/machinery finding and the owner-queue contradiction is a record finding. No scope objection to O1–O3 is raised.","findings":[{"title":"The config-count RED contract excludes AC5 even though its mutation breaks AC5","file":"docs/specs/WP-dream-git-env-pinning.md","line_start":316,"line_end":325,"materiality_band":"B","confidence":0.98,"body":"The mandated mutation restores the GIT_CONFIG_COUNT/KEY/VALUE triple, but AC5(b), lines 578–589, uses that same triple to inject core.hooksPath and requires the hook not to fire. Therefore the mutation necessarily reddens AC5 as well as AC1/AC4. With an unfiltered suite, scripts/red-proofs.js:1613–1677 rejects AC5 as an undeclared own-body failure; an implementer can obtain PROVEN only by filtering AC5 out, leaving the newly added positive-control assertion without mutation evidence. I executed the runner/source audit; full proofs could not run because sandbox creation was denied. This is a criterion/RED-machinery finding, not a Table U finding, so it does not itself fire STOP branch 3.","recommendation":"Add AC5(b) to git-env-inherits-config-count's mandatory expectRed set and require its stable test identity/signal. Then demonstrate the declaration both unfiltered and under any intended testNamePattern."},{"title":"The adopted owner record still says XDG_CONFIG_HOME is carried","file":"docs/specs/logbook/2026-09-05-owner-rulings-git-env-pinning-queue.md","line_start":49,"line_end":68,"materiality_band":"B","confidence":1,"body":"The record says O1's text lives only in the primary spec, then restates it and says both HOME and XDG_CONFIG_HOME are carried. That directly contradicts O3 at lines 91–108 and primary Table U row U3. Thus R1-B was not propagated through the supplied decision record, leaving mutually exclusive adopted recommendations for future dispatch or overrule. I read and line-compared both sections and Table U. This is a record/mirror finding, not a Table U-row finding; STOP branch 6 applies.","recommendation":"Amend the O1 entry to say HOME is set from paths.home and XDG_CONFIG_HOME is not carried, or remove the duplicated disposition entirely and defer explicitly to O1/O3 in the primary spec."},{"title":"Table U hides direct identity and replacement-object channels in its catch-all","file":"docs/specs/WP-dream-git-env-pinning.md","line_start":382,"line_end":382,"materiality_band":"B","confidence":0.94,"body":"U21 silently absorbs GIT_AUTHOR_*, GIT_COMMITTER_*, GIT_NO_REPLACE_OBJECTS, and GIT_REPLACE_REF_BASE despite their direct semantic relevance. Executed with dream.js's exact -c user.name/user.email options, git var still returned the inherited author and committer identities: environment identity wins over -c. The pin therefore changes existing commit attribution without a named reach or cost. Replacement controls likewise govern whether and where replacement objects are used by object reads, but no exact-shape runtime reproduction was possible in the read-only sandbox. These variables must be named even if their O1 disposition remains ABSENT. This is a Table U row finding; following R1-D, STOP branch 3 fires.","recommendation":"Run exact-shape probes for commit-tree identity and replacement-object effects on read-tree/ls-tree/show/commit-tree. Add explicit Table U rows recording reach, ABSENT disposition, and user-visible cost, plus an assertion for the resulting commit identity/object semantics. Do not change O1's pin decision without separate owner routing."},{"title":"Inherited PATH keeps manual and scheduled hook behavior observably different","file":"docs/specs/WP-dream-git-env-pinning.md","line_start":362,"line_end":362,"materiality_band":"B","confidence":0.99,"body":"U1 says PATH selection is closed by the pinned top-level spawn, while lines 115–118 and U20 claim the manual run becomes identical to the scheduled one. That closure does not extend to preserved hooks or lazy-fetch descendants: they receive the Git child's PATH. I executed buildCleanEnv and confirmed the scheduled PATH differed from process.env.PATH, while Table U requires manual runs to inherit the latter. A configured hook invoking a bare helper can therefore resolve different code in the two modes. This does not argue that PATH should be dropped; it falsifies the equality claim and omits a descendant cost. It is a Table U row finding, so STOP branch 3 fires.","recommendation":"Narrow every manual/scheduled equality claim to the HOME/XDG/GIT-selector properties actually equalized, and record under U1/U20 that descendant command resolution remains launch-mode-dependent because PATH is intentionally carried."}]}