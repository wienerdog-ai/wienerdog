Adversarial DESIGN review, round 13 (confirming), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-12 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..12}-astra-raw.json and -design-review.md. Round 12: unlinking a symlinked state erased the alias a second disposer pass needed. Fix: Table X row X19 — order 0a compute protected targets → 0b classify <state> → 2 → 3; a <state> symlink whose resolved target overlaps a protected shelf is preserved and reported, never unlinked; one covering no shelf is unlinked as at 5b77865f; criteria 1/14 across both live disposer calls; RED quse-state-alias-unlinked. Convergence: the deletion surface is CLOSED as an enumeration that is the contract — (1) step 0b unlinkSync, (2) step 2 per-child recursive rmSync, (3) step 3 rmdirSync climb, (4) the mechanics loop at manifest.js:1148, (5) the two Table V replay kinds, (6) the unchanged core removal at :1151-1170 — every one of (1)–(5) gated by X16 over X17 anchors; a further alias finding is closed by adding the site to the enumeration.

Confirming round, final. Execute against the tree (temp dirs only): (1) the enumeration's completeness — grep src/core/manifest.js and src/cli/uninstall.js for EVERY mutating fs call (rmSync, rmdirSync, unlinkSync, renameSync, rename, truncate, writeFileSync into state/, chmod) reachable from uninstall's non-dry-run path, and confirm each is either in the enumeration, provably non-recursive and unable to remove a shelf file, or outside state/ entirely; (6)'s core removal at :1151-1170 — non-recursive rmdir of an empty core: confirm; (2) uninstall.js:467's second disposer call — with X19 retaining the state symlink, the second call classifies it again as overlapping → preserved again → uninstall reports the preserved link and exits with the W2 refusal shape: is the message on the second call identical to the first, and does the run terminate (no loop)?; (3) the sixteen declarations: anchors unique; each new-code declaration's mutation is specific enough that an implementer can apply it; no cross-reddening; (4) final whole-document consistency: Tables K/X/V/W/Y/B, ACs 1–19, both mirror classes, Platform scope, owner items 1–3 (standing form, no approval claims), cites at both ends; size still M?; anything that still says "exempt", "ordinary mechanics", "cannot be answered". If nothing about the product is wrong, say approve.
