Adversarial DESIGN review of two work-package specs, not of code: docs/specs/WP-doctor-recognizes-parse-threw.md (S) and docs/specs/WP-transcript-parsers-harden-text-values.md (M), both Draft. Context: WP-dream-collect-parse-throw-quarantine (docs/specs/done/) added a parse-threw quarantine reason and an error boundary in src/core/dream/scratch.js; these are its two successors.

Doctor spec: verify against src/cli/doctor.js (the switch over quarantine reasons ~:515-521 and the unrecognized-reason row ~:565) that one switch arm is the whole change, that the literal message the spec pins is consistent with its three siblings and with reports/warnings.md's heading in src/core/dream/warnings.js, that the spec's claim "no golden renders this surface" is true (tests/golden/), and that the RED declaration's mutation (drop the arm) reddens what the spec says and nothing else.

Transcripts spec: this is the load-bearing one. The rule: a text block whose `text` is not a string is declined (dropped block, never thrown on, never coerced), mirroring src/core/transcripts/primary-dialogue.js joinTextBlocks exactly, applied at four sites in src/core/transcripts/codex.js and claude.js. Verify by executing with mocked I/O: (1) that the four sites are the complete set of non-string-text coercion/throw points in the default parsers — look for any other `.text` join, template-literal or String() coercion the spec misses, and for object-shaped `text` values reaching output as "[object Object]"; (2) that the "first law" (parse()/parseWithOutcome() byte-identical to base for every existing fixture) is actually guaranteed by the spec's baseline-snapshot mechanism — can a fixture exist where hardening changes output but the snapshot does not notice? consider fixtures with numeric or null text already present in tests/fixtures; (3) the retarget of the three sibling tests [PT-1] [PT-4] [PT-6] to [PT-2]'s parse-seam mock: does it preserve each of the four existing declarations' exact expectRed sets in tests/red-proofs/dream-collect-parse-throw.proofs.json (in particular pt-only-parse-is-caught must keep reddening exactly [PT-3])? reason it through against the test file and the proofs file, and say whether the spec's Table C row C3 argument holds; (4) the substring hazard Table D names (claude.js:61 vs :176) — is the authoring constraint sufficient, or can a declaration still match both? (5) the owner item (metadata fields deferred to a further successor): is the reason given (hardening them breaks [PT-3], the last end-to-end proof of the boundary's extent) actually true on the code? Check every cite at both ends, the Mirrored Surface Checklist, the Security checklist. Report each finding with file:line, what the spec says, what the code says, band A/B/C and LIGHT/HEAVY. End with a verdict per spec: approve / request-changes.
