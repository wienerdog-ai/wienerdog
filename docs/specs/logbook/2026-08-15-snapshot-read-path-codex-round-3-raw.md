# Adversarial design review — external round 3, after the Table C extraction

WP: WP-snapshot-read-path-hardening
Backend: gptsol (Codex subscription), one agent per gate run
Tree: `wp/snapshot-read-path-hardening` @ `437202b`; the reviewed spec and ruling
are byte-identical to `58de7ea`, which the reviewer verified with
`git diff --exit-code`.
Read-only check: `git status --porcelain` empty before and after, byte-identical
(the reviewer ran and reported both).

Raw final output, byte-unchanged inside the fence, committed **before any
adjudication** per `docs/runbooks/codex-review.md` ("Rules"). Two transport
artifacts were decoded and nothing else was touched: the notification channel
delivered `&amp;&amp;` where the reviewer had run `&&`, and `&lt;exclusion&gt;`
where it had written `<exclusion>`. No finding text, verdict, confidence, status
or line number was altered.

Round ≥ 2, so the reviewer also had to verify round 2's findings are genuinely
fixed rather than re-worded; that verdict is `prior_findings_verification`.

`````text
{"verdict":"needs-attention","summary":"Do not ship the spec yet: the allocation bound is still ambiguous, Table C contradicts its own crossover order, and the acceptance criteria miscount the preserved skip strings.","executed":["git -C /Users/felho/dev/repos-to-learn-from/wienerdog status --porcelain (initial) — exit 0; stdout empty","git -C /Users/felho/dev/repos-to-learn-from/wienerdog branch --show-current && git -C /Users/felho/dev/repos-to-learn-from/wienerdog rev-parse HEAD — exit 0; confirmed wp/snapshot-read-path-hardening at 437202b5808c27a1eb47e24dad6427f7250f17b8","git diff --exit-code 58de7ea 437202b for the reviewed spec/ruling, plus wc and grep -a checks — exit 0; reviewed files are byte-identical to 58de7ea and reproduced 205/411 line counts","node limit/flag/scanner/allocation probe — exit 0; Node v24.18.0 on darwin, both limits are 262144, scanner accepts 262144 and rejects 262145, O_NOFOLLOW/O_NONBLOCK are present, and a 262145-byte view retained a 120965360-byte backing store","git show --stat and unified diff for 58de7ea — exit 0; inspected the round-2 fixes and Table C extraction","npm test -- --test-name-pattern \"vault-snapshot\" — exit 0; 120 passed","npm run lint — exit 0; markdown/frontmatter passed, shellcheck and PSScriptAnalyzer unavailable and skipped","grep call-count/CI/symlink claims — exit 0; confirmed six broker-wiring call sites, ubuntu/macos test matrix, and the two current symlink surfaces","node current refusal/size-race probe — exit 0; reproduced grow→secret reason, shrink→old cap reason, over-cap+read-failure→cap without a read, lstat failure→unreadable, leaf-symlink refusal, and followed source-directory symlink","grep threat-model boundaries — exit 0; inspected the remote-content versus local-writer boundary","node candidate-type open probe — exit 0; dangling symlink failed ELOOP, directory/FIFO/character device opened then fstat as non-file, and Unix socket failed before a descriptor","git diff --exit-code main...HEAD -- src tests — exit 0; implementation and tests are unchanged","node swap/crossover/oversized-decode probe — exit 0; leaf swap copied outside bytes, over-cap mode-000 currently reports the cap reason, and oversized decode threw ERR_STRING_TOO_LONG","npm test — exit 0; 2002 tests, 1993 passed, 9 skipped, 0 failed","grep full-suite summary — exit 0; confirmed zero failures","node fs API signature probe — exit 0; readSync.length=5 and closeSync.length=1","node concrete-reason enumeration — exit 0; reproduced ten distinct current skip strings, not eight","git -C /Users/felho/dev/repos-to-learn-from/wienerdog status --porcelain (final) — exit 0; stdout empty and byte-identical to initial status"],"prior_findings_verification":[{"id":"2-1","title":"The primitive bound still permits whole-source allocation","status":"partially-fixed","evidence":"The new quantity (c) would exclude the prior 120965360-byte allocation only if capacity means aggregate retained backing storage. The executed probe showed why that definition matters: a Buffer view can expose 262145 bytes while retaining a 120965360-byte backing store. The spec does not define aggregation, backing-store versus view capacity, aliases, or whether the unchanged gate's decoded string and re-encoded Buffer count; under the broad reading the probe found 786432 logical bytes of simultaneous source representations for a 262144-byte input, while under a narrow view-capacity reading the original counterexample survives."},{"id":"2-2","title":"The new operation order cannot preserve today's skip precedence","status":"genuinely-fixed","evidence":"The owner now explicitly accepts the crossover, Table C orders open/fstat/read before caps, and the acceptance criteria require over-cap+unopenable to become unreadable. The executed mode-000 probe reproduced today's cap reason, establishing that this is a real behavior decision rather than a rewording. Table C nevertheless has a separate internal contradiction reported below."}],"findings":[{"file":"/Users/felho/dev/repos-to-learn-from/wienerdog/docs/specs/WP-snapshot-read-path-hardening.md","line_start":382,"line_end":392,"confidence":0.99,"title":"Allocated capacity still has no testable unit or scope","body":"Quantity (c) never defines whether capacity is aggregate or per allocation, whether a Buffer slice counts its view length or its full backing store, whether aliased stores are counted once, or whether the unchanged gate's decoded string and re-encoded Buffer count. The executed probe produced a 262145-byte view retaining a 120965360-byte backing store: a view-length assertion passes while source-proportional memory remains. Conversely, an aggregate all-representations reading conflicts with the preserved gate: for a 262144-byte ASCII input, the source Buffer, decoded text, and re-encoded comparison Buffer represent 786432 logical bytes while the gate runs. An implementer and reviewer can therefore choose incompatible readings, one retaining the defect and the other making acceptance impossible without changing an out-of-scope gate.","recommendation":"Define (c) in bytes over a named scope. For example, bound the aggregate backing-store capacity retained by the bounded-reader stage, count each backing allocation once at its full size even through slices, and explicitly state that later gate-derived representations are outside that exact bound but remain bounded by a named fixed multiple of the already-bounded input. Mirror that precise metric in the acceptance criterion."},{"file":"/Users/felho/dev/repos-to-learn-from/wienerdog/docs/specs/WP-snapshot-read-path-hardening.md","line_start":235,"line_end":246,"confidence":1,"title":"Table C denies a read-failure crossover it explicitly creates","body":"Row 6 says bounded-read failure moves ahead of every cap and has the same crossover as row 3, but the closing contract says rows 3 and 5 are the ONLY reason-assignment changes. The executed over-cap+staged-read-failure case currently returned the per-file-cap reason without invoking the read; under row 6 it must return unreadable. Thus row 6 is a concrete third changed assignment, and analogous fstat failures can also outrank caps. The canonical table gives contradictory answers, while the acceptance criteria exercise only the unopenable crossover, so an implementation can miss the read-failure case and still appear compliant.","recommendation":"Remove the rows-3-and-5-only claim. Distinguish newly introduced refusal mechanisms from changed precedence, state that rows 3-6 outrank all cap rows when both apply, and add a binary acceptance criterion for an over-cap candidate whose bounded read fails."},{"file":"/Users/felho/dev/repos-to-learn-from/wienerdog/docs/specs/WP-snapshot-read-path-hardening.md","line_start":377,"line_end":381,"confidence":1,"title":"The exact reason-set criterion has the wrong cardinality","body":"The spec requires exactly today's eight strings, but the executed enumeration reproduced ten concrete strings: unreadable; the non-regular reason; three cap reasons; invalid UTF-8; three distinct provenance strings (malformed, untrusted-exact, untrusted-invalid); and the secret reason. Treating `provenance gate: <exclusion>` as one form explains the mistaken eight, but it is not one string, and Table A requires the exclusion class verbatim. A literal test cannot satisfy this criterion without collapsing Done behavior that the package also freezes.","recommendation":"Either enumerate all ten concrete strings and require that exact set, or explicitly define eight reason forms while separately requiring all three allowed provenance substitutions. Use the same terminology and cardinality in Table A, Table C, and the acceptance criterion."}]}
`````
