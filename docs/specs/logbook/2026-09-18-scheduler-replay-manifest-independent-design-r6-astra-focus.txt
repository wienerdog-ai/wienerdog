Adversarial DESIGN review, round 6 (confirming), of docs/specs/WP-scheduler-replay-manifest-independent.md (Draft, M). Rounds 1-5 raws and dispositions in docs/specs/logbook/2026-09-18-scheduler-replay-manifest-independent-design-r{1..5}-astra-raw.json and -design-review.md. Round 5: D14's root filter used contains()'s bare boolean, which swallows realpath errors and would exclude an in-home root with EACCES/EIO before D9 could report it unreadable. Fix: Table D row D15 — root classification is three-valued via an explicit try/catch surfacing err.code: (i) confirmed external (resolved, inside neither resolved paths.home nor paths.core) → excluded silently; (ii) absent (ENOENT/ENOTDIR) → contributes nothing; (iii) any other code → {root, code} into unreadable, abort through D9 before mutation; a failure to resolve paths.home or paths.core itself is (iii) for every root. contains() byte-unchanged (its fail-closed boolean is right for withinSchedulerRoot :556 and the vault guard :1144). AC 17 covers all three outcomes; RED srm-resolution-failure-read-as-external declared on new code (no pre-measurable anchor, disclosed).

Confirming round: review ONLY the round-5 fix and regressions, and give a final verdict on the whole. Execute read-only against the tree (never the real scheduler or ~/.wienerdog): (1) D15 vs the vault guard D12 — the vault exclusion still uses contains() fail-closed (unresolvable vault → nothing excluded? or everything?) — which way does contains() fail for the vault check at :1144 and is that direction safe here (a vault path that fails to resolve must not cause discovery to delete vault-contained files); (2) ELOOP on a symlink chain, ENAMETOOLONG, a root whose parent is unreadable but the root is absent — which bucket, and is the bucket assignment stated as "anything not ENOENT/ENOTDIR" so no third list can drift; (3) paths.home resolution failure → (iii) for every root: does uninstall then abort even when scheduler authority is absent (a no-op dry-run on a broken home), and is that the intended trade; (4) AC 17's three outcomes are each non-vacuous with fixtures buildable under a redirected HOME; (5) with 18 ACs, ~15 D rows, S1–S12, and the RED table: any stale count, cite, or checklist entry; owner items unchanged, no approval claims. If nothing about the product is wrong, say approve.
