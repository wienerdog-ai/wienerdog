DESIGN REVIEW of a NEW work-package spec; no product code has been written yet. Review ONLY docs/specs/WP-dream-collect-parse-throw-quarantine.md and docs/specs/logbook/2026-09-17-dream-collect-parse-throw-quarantine.md. Exclude docs/marketing/, memory/research/ and userreports/ entirely. THE DEFECT (pre-existing, reproduced on main by two reviewers today): the transcript parsers (src/core/transcripts/claude.js and codex.js) join content-block text values with no string check at four sites, so a block whose text is an object with a poisoned toString throws TypeError inside parse() and parseWithOutcome(); src/core/dream/scratch.js collectExtracts calls the parser inside its admission loop with no try/catch, so ONE crafted rollout file in a watched transcript directory aborts the whole nightly dream run - and because the file is never quarantined, it aborts it EVERY night until the user finds and removes it. The spec also measured two further content-driven throws outside the parser: sanitize(extract.session_id) on a poisoned payload.id, and writeFilePrivate with ENAMETOOLONG on a 4000-character id. THE DESIGN (size S): a per-candidate FAULT BOUNDARY from the parse call through the payload stringify; a throw becomes a NEW quarantine reason `parse-threw` via the existing recordQuarantined (same five fields; the catch binds nothing, so only the code-owned literal and the file's existing identity fields become durable - never the exception text); the loop continues with the next candidate; writeFilePrivate stays OUTSIDE the boundary so a full disk still fails loudly, and sanitize gains a 128-character bound so the write cannot throw on the id; a throwing file is retried when it changes (existing fingerprint rule); dry-run inherits the boundary and persists nothing; the new reason is informational, decays like the others, and no code path reads the reason to decide anything; parser HARDENING is deferred to a successor package as an owner item; the #264 report bullet covers it with no wording change via newlyQuarantined; src/cli/doctor.js may need the new literal so it does not print "not recognized". Challenge, EXECUTING the real collector, parsers, ledger and warnings code with mocked I/O and a crafted fixture where you can (synthetic only): (1) is the boundary COMPLETE - enumerate every statement in collectExtracts' per-candidate path that can throw on file CONTENT and say whether each is inside the boundary, outside on purpose with a stated reason, or missed; can a crafted file still abort the run through any of them, including through the memo lookup, fingerprinting, the deadline/capacity bookkeeping, or the write after sanitize; (2) can the boundary MASK a real infrastructure failure that should be loud (a disk error, EMFILE, a permission failure inside the parser's stream reader) by quarantining a healthy file as parse-threw - what error classes does the existing stream layer already convert into a non-ok outcome, and does the boundary distinguish content errors from I/O errors; (3) the recorded record: can ANY attacker-controlled bytes reach the ledger, reports/warnings.md, the digest banner, the run report or doctor output through this path; does the 128-character sanitize bound change the scratch filename of any EXISTING legitimate session (a pre-existing long id) and thereby its gate evidence or its quarantine key; (4) ADR-0023: is a new quarantine reason allowed without an amendment, and does the decay/retry behaviour match the amendments; (5) the sibling WP-dream-primary-dialogue-collection (Ready) rewrites the same call site and asserts sanitize is unchanged - does the spec name that interaction and say who rebases; (6) can each acceptance criterion and each RED-proof declaration discriminate (the spec notes the RED runner refuses non-ERR_ASSERTION reds - is every declared red an assertion failure?); (7) do the file:line citations resolve on this tree. Report PRODUCT or SECURITY findings in the main list; verification-machinery findings separately, marked as machinery. A risk the spec already names and prices as a residual or owner item is not a finding unless its statement is untrue. Owner items are recommendations with overrule costs by design. State what you executed versus what you only read.
