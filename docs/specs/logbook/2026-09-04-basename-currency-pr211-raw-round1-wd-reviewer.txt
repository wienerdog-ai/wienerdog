# Raw gate output — wd-reviewer (opus), PR #211 round 1, head deeba7a4 (base main = 1f44371d). Committed BEFORE adjudication. Verbatim final report.

## REQUEST-CHANGES

1. [criteria] — Acceptance criterion 3 has no test in the suite; only two of the three required new tests were written. tests/unit/dream-promote.test.js:335 (criterion 2) and :363 (criterion 7) are the only new tests. Nothing in tests/ references INSTRUCTION_BASENAMES at all. The spec is unambiguous in two places: the Deliverables cell says "(c) NEW tests implementing acceptance criteria 2, 3 and 7", and V1's own comment says "criteria 2, 3 and 7. The new tests carry all three." Criterion 3 is carried only by V2, a manual step not part of npm test. Consequence confirmed: adding a stray member to INSTRUCTION_BASENAMES passes the entire suite; nothing in the suite bounds the Set from above, so the spec's completeness argument (Implementation notes :485-490) does not hold post-merge. Fix: a third test under the row-C9 section that reads src/core/dream/promote.js from disk, extracts the literal with V2's regex (/const INSTRUCTION_BASENAMES = new Set\(\[([\s\S]*?)\]\)/), and asserts (i) the regex matched (non-vacuity guard, mirroring :340), (ii) every member equals its own .normalize('NFC').toLowerCase(), (iii) the member count equals the `| DENY |` row count parsed from docs/instruction-file-inventory.md, asserted > 0. This test WILL redden under the RED mutation (4 vs 9) but the declaration's testNamePattern "criterion 7: the independent oracle" scopes the run to criterion 7 only (verified: selects exactly one test), so the proof and declaration need no change. Band B.

2. [contract] — The shipped canonical inventory carries a source line-number citation that this diff itself invalidated. Routes to wd-architect. docs/instruction-file-inventory.md:86 (Table B, the OMIT row for SKILL.md / LEARNINGS.md) cites promote.js:111-116 in prose and src/core/dream/promote.js:111-116 in its Citation cell. On main that range was LEDGER_BASENAME/SKILL_BASENAME; this PR adds +13 lines above (10 for the Set literal, 3 for the JSDoc), moving them to :124-129; post-diff :111-116 is DENIED_SEGMENTS / DENIED_BASENAME — a different contract. The implementer cannot fix it: the bytes live in the spec's canonical rendering block (docs/specs/WP-instruction-basename-currency.md:295) and the inventory is generated. Recommend wd-architect amend the OMIT row to cite the two constants STRUCTURALLY (LEDGER_BASENAME / SKILL_BASENAME in src/core/dream/promote.js), never a line range (ADR-0036 A2's principle), then re-render via V3 --write; and register source-line citations inside the rendering block in the Mirrored Surface Checklist as a surface that moves when promote.js moves. Band B.

### What passed
- [boundary] clean: six changed files = five Deliverables rows + the spec. node scripts/boundary-check.js <spec> <files> exit 0.
- [criteria] V1–V5 all re-run and green. Criterion 1 verified the strong way: inventory deleted in a throwaway copy, regenerated with V3 --write 2026-09-04, cmp against the shipped file — byte-identical, 13895 bytes, 100 lines, 10 placeholder sites. V5 from a clean copy: 3 declared, 3 selected, PROVEN … criterion 7, RUN: PROVEN, exit 0.
- RED self-check reproduced: find matched exactly once; after replace, node --test --test-name-pattern="criterion 7: the independent oracle" failed 1/1 with AssertionError [ERR_ASSERTION]: 01-Projects/example/agent.md must reach denial through makeAdmit (half (b), as Table D specifies). On the full mutated file exactly 4 tests redden, all own-body, confirming Decision 2.
- [contract] V4 byte-equal exactly once outside any fence; Set literal keeps the parseable shape (V2: 9 = 9, all pre-folded); JSDoc adds exactly one sentence and keeps the "will NOT cover the next convention" sentence; the declaration satisfies every validateProof rule (scripts/red-proofs.js:631-700).
- [quality] criterion 7's expected is a genuine hand-written literal; half (a) guards duplicates; criterion 2 fails on zero rows (:340); depths and spellings match the criterion; altCase correct; admit(rel) || '' keeps assert.match non-vacuous; hostile fixture gains exactly the five names; RED-side list gains exactly one mixed-case spelling. Pre-existing vacuous NFD assertion (:329-330 post-diff) confirmed and not counted.
- [convention] clean: zero runtime deps, JSDoc, conventional commit, PR title verbatim, Generated-by present, status In-Review, npm run lint green (634 files).
- ADR-0031: the spec is a good example of the pattern; the single drift is finding 2.

### EXECUTED (27 commands, all in the review worktree or git-archive copies; worktree porcelain identical before/after, empty)
boundary-check 0 (after a first arg-order error, 1); V2 0; V3 0; V4 0; V1 0 (npm test: 2610 tests, 2598 pass, 0 fail, 12 skipped); V3 --write in copy 0 + cmp identical 0; RED copy: pattern-scoped run 1 (expected), full file 1 (4 fail, expected); validator inspection 0; V6 npm run lint 0 (634 files); citation probes 0; V5 npm run red-proofs in clean copy 0 (RUN: PROVEN).
