# raw — hermetic Codex shadow (codex exec, read-only sandbox, CODEX_HOME=~/.codex-review-home, gpt-5.6-sol high) — qdd3
# tip reviewed: 40504f64d3e12577ef2f5f313138564683bf6a8b (detached worktree) ; base main = 66b2b1f8040b93a9af4f1cda25c3ac307da64f6d ; committed pre-adjudication
# codex exec rc=0 ; porcelain before/after: identical

{"target":"40504f64d3e12577ef2f5f313138564683bf6a8b","verdict":"needs-attention","executed":["git diff 66b2b1f8040b93a9af4f1cda25c3ac307da64f6d HEAD --stat / full diff / --check — exit 0","Read the six requested documents, four committed raws, cited contracts, source paths, relevant tests, proof declarations, README lifecycle rule, and mirror-walk implementation","Disposition V1–V4 read-only equivalents — exit 0; V2 reconstructed changed lines 356, 944, 1793","Predecessor literal archived V1 — exit 1 because it names the obsolete pending-spec path; current done-path equivalent — exit 0","node scripts/mirror-walk.js --scope quarantine-disposal — exit 0; RESOLVED with the recorded 11 ambiguities","node scripts/mirror-walk.js — exit 1 with 14 pre-existing unresolved entries","Parsed all 12 tests/red-proofs/*.proofs.json files — exit 0","Verified the four raw-file introduction SHAs — exit 0","Source-form assertions distinguishing validate.js:984, :995 and :1020 and checking flushDir/O11/universal-sentence text — exit 0","node scripts/check-frontmatter.js — exit 0; 271 specs, 4 agents","npm test — exit 1 before tests ran; read-only sandbox denied mkdtemp with EPERM","npm run red-proofs — exit 1 before proofs ran; read-only sandbox denied mkdtemp with EPERM","npm run lint — exit 1 because markdownlint dependency lookup failed with ENOTFOUND; shellcheck/PSScriptAnalyzer completed and frontmatter passed","No power-loss crash was staged; crash outcomes remain code-derived"],"summary":"Do not ship this disposition. R2-B, R2-D and R2-E are fixed; R2-A is not: QD-P11 never exercises M2, and O11's ignored flush failure recreates the exact post-completion residual it claims to close. R2-C's lifetime correction landed, but M6 still relies on a false before/after equivalence. Two universal sentences also survived outside Table M.","findings":[{"title":"O11's best-effort flush cannot provide its promised CLOSE disposition","file":"docs/specs/done/WP-quarantine-disposal-durability.md","line_start":212,"line_end":228,"materiality_band":"A","confidence":0.99,"body":"Classification: contract; DECISION support for rows M2 and M3. Option (b) is labelled “CLOSE IT,” and the successor says it makes D4 survive a crash, but it deliberately ignores `flushDir`'s boolean. Executed source inspection confirmed `flushDir` catches open/fsync failures and returns false. If that happens, the caller continues, may publish and commit the fallback record, and the directory remains unflushed—the exact R2-A state. Line 227's claim that the residual can only precede publication is therefore false. Win32 deterministically takes the same non-closing outcome because no flush is issued. This is a false contract claim, not an objection to the adopted decision.","recommendation":"Either give an incomplete flush a disposition that prevents the run from completing as though D4 were durable, or rename option (b) as a best-effort attempt and explicitly retain/price the post-completion residual for failed flushes and win32. The successor title, Context, acceptance predicate, O11, M2/M3 and F7(a) must agree."},{"title":"QD-P11 is falsely cited as measurement of M2","file":"docs/specs/done/WP-quarantine-disposal-durability.md","line_start":419,"line_end":419,"materiality_band":"B","confidence":0.99,"body":"Classification: measured-claim; DECISION support for row M2. QD-P11 injects the artifact-fsync failure after the commit: its first `rm redacted/.tmp…` is validate.js:995 (M1), followed by the failed fsync and validate.js:1020 (M3). It cannot be validate.js:984 (M2), because that shared catch returns `null` before any fsync. The executed source-form check confirmed :984, :995 and :1020 are distinct branches. D4's actual text does justify considering both owned states—“null means the owned path is absent”—but the record's stronger statements that R2-A measured M2 and that QD-P11 “shows both leftovers” are false. M2's completed-run path and O_EXCL consequence remain unmeasured.","recommendation":"Keep the D4-based scope if desired, but describe it as a contract inference. Add a dedicated probe that fails write/commit before :969, executes :984, follows the outer redact fallback through record publication, traces the absence of a qdir flush, and exercises the resurrected temp's later O_EXCL collision before claiming M2 is measured."},{"title":"M6 still compares materially different pre-crash and post-completion states","file":"docs/specs/done/WP-quarantine-disposal-durability.md","line_start":423,"line_end":423,"materiality_band":"A","confidence":0.94,"body":"Classification: contract; DECISION support for row M6. The row now correctly admits that the owner may delete the withheld twin, but still concludes that the two states are identical because “the record omits it either way.” A crash before validate.js:1488 prevents the gate from returning and no record is published; after the unflushed delete, the gate can return at :1497 and the run can publish and commit a record naming only the withheld copy. If the owner then follows promote.js:600-603 and deletes that named copy before the unflushed directory state is written back, a later power loss can restore an unbannered `redacted/` copy as the sole surviving bytes. That recovery and disclosure posture is not the pre-removal state. This is code-derived; no power-loss crash was staged.","recommendation":"Re-model M6 using the same pre-return/post-completion distinction applied to M3, including publication and subsequent owner deletion. The no-flush decision may remain only if O10 explicitly accepts and prices that exact state; it cannot rest on equivalence."},{"title":"The supposedly withdrawn universal disposition still appears outside Table M","file":"docs/specs/done/WP-quarantine-disposal-durability.md","line_start":277,"line_end":280,"materiality_band":"B","confidence":1,"body":"Classification: record; this finding concerns sentences outside the rows. “This package's answer is that none of them needs one” is a universal no-flush rule and directly contradicts M2/M3's CLOSE decisions. The platform paragraph at lines 425-431 also says the “same equivalence” carries every row even though M2/M3 explicitly reject equivalence. The executed lexical assertion confirmed the first sentence remains. This falsifies the F7(a) clause's claim that no spanning claim survives and can misroute a later implementer who reads Context before the table.","recommendation":"Delete both spanning assertions or replace them with a pure pointer stating that only Table M's decision column governs, with no summarized rule across acts."}]}