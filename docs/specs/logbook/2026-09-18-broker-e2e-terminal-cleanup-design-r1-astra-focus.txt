Adversarial DESIGN review of a work-package spec, not of code: docs/specs/WP-broker-e2e-terminal-cleanup.md (Draft), plus its logbook entry. This spec re-derives a stale July package: the terminal-auth premise was fixed by WP-cleanenv-keychain-auth (see docs/specs/done/ and ADR-0025 Amendment 5), so the residue is harness cleanup of tests/scenarios/broker-e2e/run-broker-e2e.js and two decisions the spec makes for the implementer.

Review the DECISIONS against the code, by reading and executing where possible (do not run the live scenario; it spends model calls): (1) the weekly-review non-vacuity floor becomes the literal `methods.includes('gmail.users.drafts.create')` — verify against src/core/runtime-profile.js (all three routine profiles have tools ['Read'] only) and src/gws/broker/verbs.js (create_draft_to_self maps to drafts.create) that this floor is satisfiable, non-vacuous (the fail-loud watchdog also writes into the fake call log on routine failure — does the floor discriminate a routine that never ran?), and consistent with what LP2 is meant to prove; (2) the vault-snapshot seeding fix — verify against src/core/vault-snapshot.js SNAPSHOT_PLANS and the harness's seedCore that an absent source dir is skipped silently today, that the seeded fixtures the spec specifies actually mount (the spec claims both files mount with skipped: []), and the provenance trap the spec records (a daily note carrying derived_from_untrusted: true is skipped by the provenance gate). Check every contract row and every cite (file:START-END at both ends). Check the Mirrored Surface Checklist is complete and the Security checklist is honest. Check the owner items are in the standing form (recommendation + overrule cost) and that nothing claims owner approval. Report each finding with file:line, what the spec says, what the code says, materiality band A/B/C (A = silent wrong behaviour with data-loss/security consequence; B = caught downstream; C = hygiene) and LIGHT/HEAVY (HEAVY = fixing it changes what the implementer builds). End with a verdict: approve / request-changes.
