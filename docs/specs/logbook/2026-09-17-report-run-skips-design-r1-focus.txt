DESIGN REVIEW of a re-derived work-package spec; no product code has been written yet. Review ONLY these two files: docs/specs/WP-dream-report-run-skips.md and docs/specs/logbook/2026-09-17-report-run-skips-rederived.md. Exclude docs/marketing/, memory/research/ and userreports/ entirely. Background: the nightly dream job's transcript collector (src/core/dream/scratch.js collectExtracts, canonical contract in docs/specs/done/WP-dream-filtered-input-budget.md Table A, ADR-0023 Amendment 3, ADR-0012's second 2026-09-15 amendment) now has five disjoint exclusion arms besides admission: quarantine (ledger record), readDeferred, oversized (individually larger than dream_max_input_bytes, memoized in the ledger, NOT retried until the file or the setting changes), capacity-deferred and deadline-deferred. This spec makes the dream's vault report state, in plain language for a non-developer, how many sessions a run could not consolidate, in six integer counts, composed inside src/core/dream/promote.js composeRecord via a new optional promote({runSkips}) argument. Challenge specifically: (1) TRUTHFULNESS of each bullet's promise against the real collector: is "will be retried on the next run" true for every arm that says it, and is the oversized wording ("will pass over them every night until the session file changes or you raise dream_max_input_bytes") exactly true, including when a cached oversized memo is invalidated by an app version change; are the six counts really disjoint and does each map to exactly one collector field; (2) the run that admits NOTHING but excluded something now throws before the brain runs, so no report is written - does the spec say so truthfully and is any count silently lost on that path or on the idle/quarantine-only path; (3) composing the section inside composeRecord: does it survive every write path the spec claims (the single write, the fallback, the refused-report console dump), can untrusted or non-integer input reach the rendered markdown, and can the section collide with or be mistaken for brain-authored content or the existing "Refused by policy" section; (4) the pointer line to reports/warnings.md renders only when a quarantine count is non-zero - can a user be sent to a file that does not name what the report just counted; (5) can every acceptance criterion, the long inline node -e verification command, and both RED-proof declarations discriminate a correct implementation from a plausible wrong one; (6) does every file:line citation resolve on this tree - verify by reading the files. The "Dispatch precondition - owner items" section lists decisions reserved for the owner as recommendations with overrule costs; disagreeing with a recommendation is a welcome finding, but their being unratified is by design and not itself a finding. Report what you executed versus what you only read.
