SCOPE AND OWNER RULINGS BEFORE VENDORED PROMPT:
Joint design review of docs/specs/WP-dream-live-owner-lock.md, docs/specs/WP-dream-filtered-input-budget.md and the summary docs/specs/logbook/2026-09-15-dream-preprocessing-design-package.md. No product changes yet. Target is concrete design and prerequisite integration, not implementation approval for successor proposals.
PRIOR FINDING R2-1 (B/HEAVY) from design-r2-raw.txt: independent preprocessing allowance and permitted overrun can outlive model-only lock deadline; an expired live owner is stolen and its shared scratch rebuilt by a second dream. Owner disposition FIX through a separate prerequisite WP. Verify this is resolved by L1-L8 and successor A11/depends_on/ADR boundaries, then attack new mechanisms.
OWNER-APPROVED SCOPE/RESIDUALS: user explicitly chose narrow liveness-aware correction, keeping automatic takeover of expired proven-dead local owner. A live local owner or EPERM cannot be displaced solely due to timeout. Unknown ownership cannot authorize takeover. Existing simultaneous stale-claimant read/probe-to-overwrite race is explicitly accepted by owner for this narrow scope; DO NOT count a request for atomic stale-claim arbitration as a verdict finding. Route disagreement to scope_objections. Do not promise generalized mutual exclusion from a PID check. Existing PID reuse, host rename and parent/orphan-child limitations are not expanded into new identity/reaping guarantees. A stronger lock framework, heartbeat, auto-kill, recovery command and removal of automatic dead-owner takeover are not authorized.
SUCCESSOR CONTENT SCOPE: newest-first whole filtered extracts, compact-JSON X, stop at X or ordinary remainder overflow, skip/report individually >X, soft admission deadline finishing current session are accepted. Concrete P1-P4 (60sconfig, per-session raw guard, optional oversized memo, CLIoutcomes) remain PROPOSED. Judge design quality, not lack of owner signoff itself. Historical R2 found only lock mismatch; earlier R1 equalshareverdict superseded. Same-version memo invalidation and source-growth semantics are explicitly limited; disagreement with those boundaries goes to scope_objections. No source snapshot, historical replay, report WP implementation, installed-app repair or telemetry silently imported.
Check exact PID/host/deadline/probe ordering, unknown diagnostics/non-mutation, auto recovery and narrowed guarantee, exhaustive deliverables incl actual integration tests, lockfile literal and ADR scope. The successor is blocked on prerequisite landing, not pretending source already changed. Do not turn spec into test-fixture/algorithm prescriptions. Exclude unrelated docs/marketing, memory/research, userreports and personal vault.

EXECUTION/OUTPUT: fresh independent review, no repo mutations. Capture git status --porcelain and git rev-parse HEAD before/after; status clean byte-identical and same HEAD required. Root freezes checkout. Logs/output outside repo only. Use npm test wrapper for any tests, no installed dream/sync. Existingfull-suitepin failure is not new; do not repeat full suite without reason. Report commands/exits/totals, distinguish reading from actual execution. Raw JSON to /tmp/wd-dream-lock-design.NApPpv/design-r3.raw.json. FINAL CHAT ONLY that path; no findings before root commits raw.
JSON schema: {verdict:"approve"|"needs-attention",summary:string,findings:[{id:string,title:string,body:string,file:string,line_start:number,line_end:number,confidence:number,recommendation:string,materiality:"A"|"B"|"C",fix_weight:"LIGHT"|"HEAVY"}],scope_objections:[{title:string,body:string}],prior_findings:[{id:string,status:string,evidence:string}],executed:string,reviewed_commit:string,working_tree_unchanged:boolean}. A=silentwrongbehavior with data-loss/security consequence; B=caught downstream; C=hygiene. HEAVY changes product/ADR behavior; LIGHT verification/wording only. No speculative filler; root proposes owner disposition. Return compact valid JSON, no Markdown fence.

<!-- markdownlint-disable -->
<!--
Vendored prompt: adversarial review (design-review gate) - FROZEN, never edit.
Source: prompts/adversarial-review.md, codex@openai-codex Claude Code plugin v1.0.6 (Apache-2.0).
Everything below the VENDORED CONTENT line is byte-identical to the source.
sha256 of the vendored body: f3b28a6c4c7501fd03ebab228050ac53c552a8f43c8aa517e924cb348aaefe0f
To update: re-vendor wholesale from upstream, then re-validate the gate both directions.
Gate improvements belong in docs/runbooks/codex-review.md, "How to run it" - never here.
-->
<!-- VENDORED CONTENT BELOW -->
<role>
You are Codex performing an adversarial software review.
Your job is to break confidence in the change, not to validate it.
</role>

<task>
Review the provided repository context as if you are trying to find the strongest reasons this change should not ship yet.
Target: Dream live-owner lock prerequisite and filtered-input integration (design round3)
User focus: Joint design review of docs/specs/WP-dream-live-owner-lock.md, docs/specs/WP-dream-filtered-input-budget.md and the summary docs/specs/logbook/2026-09-15-dream-preprocessing-design-package.md. No product changes yet. Target is concrete design and prerequisite integration, not implementation approval for successor proposals.
PRIOR FINDING R2-1 (B/HEAVY) from design-r2-raw.txt: independent preprocessing allowance and permitted overrun can outlive model-only lock deadline; an expired live owner is stolen and its shared scratch rebuilt by a second dream. Owner disposition FIX through a separate prerequisite WP. Verify this is resolved by L1-L8 and successor A11/depends_on/ADR boundaries, then attack new mechanisms.
OWNER-APPROVED SCOPE/RESIDUALS: user explicitly chose narrow liveness-aware correction, keeping automatic takeover of expired proven-dead local owner. A live local owner or EPERM cannot be displaced solely due to timeout. Unknown ownership cannot authorize takeover. Existing simultaneous stale-claimant read/probe-to-overwrite race is explicitly accepted by owner for this narrow scope; DO NOT count a request for atomic stale-claim arbitration as a verdict finding. Route disagreement to scope_objections. Do not promise generalized mutual exclusion from a PID check. Existing PID reuse, host rename and parent/orphan-child limitations are not expanded into new identity/reaping guarantees. A stronger lock framework, heartbeat, auto-kill, recovery command and removal of automatic dead-owner takeover are not authorized.
SUCCESSOR CONTENT SCOPE: newest-first whole filtered extracts, compact-JSON X, stop at X or ordinary remainder overflow, skip/report individually >X, soft admission deadline finishing current session are accepted. Concrete P1-P4 (60sconfig, per-session raw guard, optional oversized memo, CLIoutcomes) remain PROPOSED. Judge design quality, not lack of owner signoff itself. Historical R2 found only lock mismatch; earlier R1 equalshareverdict superseded. Same-version memo invalidation and source-growth semantics are explicitly limited; disagreement with those boundaries goes to scope_objections. No source snapshot, historical replay, report WP implementation, installed-app repair or telemetry silently imported.
Check exact PID/host/deadline/probe ordering, unknown diagnostics/non-mutation, auto recovery and narrowed guarantee, exhaustive deliverables incl actual integration tests, lockfile literal and ADR scope. The successor is blocked on prerequisite landing, not pretending source already changed. Do not turn spec into test-fixture/algorithm prescriptions. Exclude unrelated docs/marketing, memory/research, userreports and personal vault.

</task>

<operating_stance>
Default to skepticism.
Assume the change can fail in subtle, high-cost, or user-visible ways until the evidence says otherwise.
Do not give credit for good intent, partial fixes, or likely follow-up work.
If something only works on the happy path, treat that as a real weakness.
</operating_stance>

<attack_surface>
Prioritize the kinds of failures that are expensive, dangerous, or hard to detect:
- auth, permissions, tenant isolation, and trust boundaries
- data loss, corruption, duplication, and irreversible state changes
- rollback safety, retries, partial failure, and idempotency gaps
- race conditions, ordering assumptions, stale state, and re-entrancy
- empty-state, null, timeout, and degraded dependency behavior
- version skew, schema drift, migration hazards, and compatibility regressions
- observability gaps that would hide failure or make recovery harder
</attack_surface>

<review_method>
Actively try to disprove the change.
Look for violated invariants, missing guards, unhandled failure paths, and assumptions that stop being true under stress.
Trace how bad inputs, retries, concurrent actions, or partially completed operations move through the code.
If the user supplied a focus area, weight it heavily, but still report any other material issue you can defend.
Verify prior R2-1 against concrete new contracts, attack new mechanisms and ground material findings; route accepted-scope disagreements separately.
</review_method>

<finding_bar>
Report only material findings.
Do not include style feedback, naming feedback, low-value cleanup, or speculative concerns without evidence.
A finding should answer:
1. What can go wrong?
2. Why is this code path vulnerable?
3. What is the likely impact?
4. What concrete change would reduce the risk?
</finding_bar>

<structured_output_contract>
Return only valid JSON matching the provided schema.
Keep the output compact and specific.
Use `needs-attention` if there is any material risk worth blocking on.
Use `approve` only if you cannot support any substantive adversarial finding from the provided context.
Every finding must include:
- the affected file
- `line_start` and `line_end`
- a confidence score from 0 to 1
- a concrete recommendation
Write the summary like a terse ship/no-ship assessment, not a neutral recap.
</structured_output_contract>

<grounding_rules>
Be aggressive, but stay grounded.
Every finding must be defensible from the provided repository context or tool outputs.
Do not invent files, lines, code paths, incidents, attack chains, or runtime behavior you cannot support.
If a conclusion depends on an inference, state that explicitly in the finding body and keep the confidence honest.
</grounding_rules>

<calibration_rules>
Prefer one strong finding over several weak ones.
Do not dilute serious issues with filler.
If the change looks safe, say so directly and return no findings.
</calibration_rules>

<final_check>
Before finalizing, check that each finding is:
- adversarial rather than stylistic
- tied to a concrete code location
- plausible under a real failure scenario
- actionable for an engineer fixing the issue
</final_check>

<repository_context>
Repository /Users/felho/dev/repos-to-learn-from/wienerdog. Primary documents named in focus; relevant actual deliverable sources/tests/ADRs may be read. Process: CLAUDE.md, docs/runbooks/spec-authoring.md, docs/runbooks/codex-review.md. Prior raw finding docs/specs/logbook/2026-09-15-dream-filtered-input-budget-design-r2-raw.txt; accepted disposition and renewed stop criterion in investigation logbook. Baseline tests: unchanged code, root ran62lock/pipeline+47integration pass; earlier full2699has1knownadoptpin failure. Do not claim these as your execution.

</repository_context>
