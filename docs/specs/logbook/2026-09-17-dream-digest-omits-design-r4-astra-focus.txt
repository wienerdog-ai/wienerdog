DESIGN REVIEW (confirming round, fresh reviewer) of a work-package spec that has been moved to Ready; product code is being written in parallel and is NOT in this diff. Review ONLY docs/specs/WP-dream-digest-omits-own-job-alerts.md and the 2026-09-17 dream-digest-omits logbook entries under docs/specs/logbook/. Exclude docs/marketing/, memory/research/ and userreports/ entirely. Three earlier rounds by a different model are preserved beside them as -design-r1..r3-raw.json with dispositions in the design-review entry; do not assume they were right. The spec proposes that the nightly dream job, when it re-renders state/digest.md at the END of a successful run (exactly one call site), omits the "job has failed" alert records for its OWN job, identified by WIENERDOG_JOB resolving via the scheduler's findJob to run: builtin:dream AND a valid supervisor-minted WIENERDOG_DREAM_RUN_TOKEN; every other render stays unfiltered; an in-process failure after the filtered render triggers exactly one unfiltered re-render, the original error wins, and a failed recovery render prints one fixed-text stderr line. Hiding a GENUINE unresolved alert is the worst outcome; showing a stale one is merely today's behaviour. Challenge: (1) any path on which a genuine unresolved failure is absent from digest.md that the spec does not already name - the spec names as a pre-existing property that a failed dream never reaches a digest render, so a callout for the dream's own failures reaches the digest only via an attended sync or an early quarantine render, and that doctor does not read alerts; is that statement TRUE against src/cli/dream.js, src/cli/run-job.js, src/cli/sync.js, src/cli/doctor.js, src/cli/alerts.js; (2) can the filter drop an alert for a DIFFERENT job, or for a failure of the same job that this run's success does not resolve; (3) can the identity check be satisfied by an attended or inherited environment, and is the residual (possession, not parentage) stated truthfully; does it silently never engage on a supported platform; (4) can the recovery re-render double-render, corrupt the digest or mask the original error; (5) can each acceptance criterion and each of the six RED-proof declarations discriminate a correct implementation from a plausible wrong one; (6) do the file:line citations resolve on this tree. Report findings about PRODUCT behaviour in the main list; list verification-machinery findings separately, marked as machinery. A risk the spec already names and prices as a residual or owner item is not a finding unless its statement is untrue. Owner items are recommendations with overrule costs by design. State what you executed versus what you only read.
