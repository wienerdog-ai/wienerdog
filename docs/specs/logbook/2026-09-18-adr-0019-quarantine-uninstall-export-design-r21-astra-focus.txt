Adversarial DESIGN review, round 21 (confirming, final), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-20 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..20}-astra-raw.json and -design-review.md. Round 20: replay-guard skips did not reach the manifest-retention decision. Fix: Table X row X22 — SET-LEVEL: an unanswerable protected set at reverse()'s guard initialisation aborts live replay before any mutation (disposer's unreadable shape; manifest/config untouched; --dry-run reports); PER-ENTRY: an entry whose own path fails to resolve stays preserved-and-reported and propagates into W10 via a new `shelfGuarded: string[]` return field; W10 consults three inputs; the asymmetry recorded (aborting per-entry would make a damaged tree un-uninstallable); X16's "no return-shape change" corrected; W6 holds (shelfGuarded empty on an ordinary install); AC 19 both arms; RED quse-shelf-guard-skip-not-propagated; twenty-five declarations over thirteen criteria.

Final confirming round. Apply the three named residuals first — anything inside is CLOSED. Then, read-only: (1) W10's three inputs — preservedQuarantine (both sweeps), the fresh inventory, shelfGuarded — enumerate every way this uninstall can leave a component behind AND delete the manifest: is there a fourth input (the scheduler package's D5b `remove:false` keeps / R9's preserved plists — do those retain the manifest today? they are not shelf matters, but a user-facing retry ledger is the same question — say whether it is in scope or routed); (2) X22's set-level abort vs the pre-plan gate W3: on a live run the protected set is computed twice (plan phase read-only, then reverse() init) — if the second is unanswerable and the first was not, the user consented to a plan the abort now refuses — is the abort message clear that nothing was done; (3) twenty-five declarations: anchors unique, no cross-reddening; (4) whole-cell re-read of X22, W10, X16, X13; Tables K/X/V/W/Y/B, ACs 1–19, both mirror classes, Platform scope, owner items 1–4 (standing form, no approval claims), cites at both ends; size verdict — with 25 declarations over 22 X-rows, say plainly whether this must be split (e.g. deletion guards vs uninstall UX) before implementation. If nothing about the product is wrong outside the residuals, say approve.
