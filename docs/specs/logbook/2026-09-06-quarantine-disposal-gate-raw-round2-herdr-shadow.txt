# raw — hermetic Codex shadow (codex exec, read-only sandbox, CODEX_HOME=~/.codex-review-home, gpt-5.6-sol high) — qdd2
# tip reviewed: 03c56ed118414049383db5bbf7eaa5a31fc4c849 (detached worktree) ; base main = 66b2b1f8040b93a9af4f1cda25c3ac307da64f6d ; committed pre-adjudication
# codex exec rc=0 ; porcelain before/after: identical

{"target":"03c56ed118414049383db5bbf7eaa5a31fc4c849","verdict":"needs-attention","executed":["git diff 66b2b1f8040b93a9af4f1cda25c3ac307da64f6d HEAD --stat / --name-status / full diff — exit 0","git diff --check 66b2b1f8040b93a9af4f1cda25c3ac307da64f6d HEAD — exit 0","Read the requested specs, logbooks, round-1 raws, validate.js removal/flush/prune paths, relevant predecessor Tables D/F/N, banner routing, tests, and RED declarations — exit 0","Disposition V2 reconstruction plus V3/V4 read-only checks — exit 0; predecessor changed only lines 356, 944, 1793","Predecessor's literal archived V1 — exit 1 because it references the obsolete docs/specs/WP-quarantine-preserve-durability.md path; equivalent using the current done/ path — exit 0","node scripts/mirror-walk.js --scope quarantine-disposal — exit 0; RESOLVED but reports 9 AMBIGUOUS references, not the documented 1","node scripts/mirror-walk.js — exit 1 with the documented 14 pre-existing unresolved entries","node scripts/check-frontmatter.js — exit 0; 270 specs","Parse all tests/red-proofs/*.proofs.json and verify raw-file introduction SHAs — exit 0","npm test — exit 1 before tests ran; read-only sandbox denied mkdtemp with EPERM","npm run red-proofs — exit 1 before proofs ran; read-only sandbox denied mkdtemp with EPERM","NPM_CONFIG_OFFLINE=true npm run lint — exit 1; markdownlint-cli2 was unavailable and network cache was empty; shellcheck/PSScriptAnalyzer completed and frontmatter passed","QD-P6–QD-P10 could not be independently rerun because their scripts are explicitly uncommitted and the sandbox is read-only; their recorded results were checked against the shipped control flow"],"summary":"Do not ship. R1-A, R1-B, and R1-C are substantively fixed, but O8's permanent disposition still relies on a false temporal equivalence at M3, and the revised mirror record reports verification results that the actual mirror walk disproves.","findings":[{"title":"M3's non-durable window survives return, invalidating the crash-before/crash-after equivalence","file":"docs/specs/done/WP-quarantine-disposal-durability.md","line_start":61,"line_end":68,"materiality_band":"A","confidence":0.98,"body":"Classification: measured-claim. The universal claim compares every post-removal crash state with a crash before the removal, but M3's own row at line 344 incorrectly bounds its window to `rmSync` returning through `quarantinePreserve` returning even though it immediately says nothing flushes afterward. On a failed redacted preservation, validate.js:1020 removes `dest` and returns null; execution can then preserve a withheld copy at :1429, record only that copy at :1463, publish the report in promote.js:1647-1651, and commit it after dream.js:969-1044 without ever flushing the `redacted/` directory. A later power loss can therefore restore the deleted redacted entry after the product has completed and published a record that omits it. A crash before :1020 necessarily precedes the withheld fallback and report, so it does not leave the same product state or recovery posture. This is the silent, unbannered secret-bearing artifact O10 describes, but reached after a completed run rather than an interrupted one; the permanent O8 value decision currently prices those states as identical.","recommendation":"Extend M3's window until an actual `redacted/` directory flush or writeback, explicitly compare the pre-return and post-completion states—including the withheld twin and published record—and re-evaluate O8/F7(a) from that corrected state model. This does not itself require choosing to implement durable disposal."},{"title":"The mirror checklist and recorded mirror-walk result are already false","file":"docs/specs/done/WP-quarantine-disposal-durability.md","line_start":323,"line_end":330,"materiality_band":"B","confidence":0.99,"body":"Classification: mirror. The spec says the scoped walk has exactly one ambiguous reference, but executing it reports 9: Table M plus two occurrences each of M3, M4, M5, and M6. The new checklist entry at line 389 also says the shelf stub cites M3–M6 and “restates no cell”; the stub explicitly cites only M3/M4/M6 at line 89 and locally repeats their persistence result, while its selection section repeats M5's QD-P3/P4 predicate and guard without citing row M5. The logbook's claims of one ambiguity at lines 267 and 285 are consequently stale. Because this stub is the live carrier for the acknowledged only-copy data-loss class, an inaccurate mirror registry defeats the mechanism intended to keep later Table M corrections synchronized.","recommendation":"Register the stub honestly as a cross-document mirrored summary, add an explicit M5 citation, update the ambiguity accounting and logbook with the actual scoped output, and rerun mirror-walk after the checklist no longer asserts facts contradicted by the referenced file."}]}