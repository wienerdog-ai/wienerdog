Adversarial DESIGN review, round 2, of docs/specs/WP-quarantine-only-copy-shelf.md (Draft, S). Round-1 raw and dispositions: docs/specs/logbook/2026-09-18-quarantine-only-copy-shelf-design-r1-astra-raw.json and 2026-09-18-quarantine-only-copy-shelf-design-review.md. Round 1 found the universal only-copy premise false, the RED declaration malformed (nested path), and the fixture unable to demonstrate the loss. Fixes: row O1 is now three classes at the one write site src/core/dream/validate.js:1416 — A successful redaction (shelf copy = sole pre-scrub form; vault holds the scrubbed note), B refused fall-through with no withheld twin (:1452-1473; vault note untouched at baseline), C retained duplicate (:1489-1504 identity unproven, or the best-effort rmSync at :1500 threw; twin on the withheld shelf) — plus a fourth caveat that the shelf does not dedupe (two runs over identical content leave two byte-identical files, OC-P5); candidate (a) re-argued per class (C = housekeeping, A/B = the count cap's bargain); (b) floors A+B only and now needs a never-persisted class record (:1431 builds it, nothing stores it); (c) "mark is vacuous" withdrawn — a mark would be the missing durable record; the doc clauses say the deleted file MAY be the only copy and are forbidden from claiming it always is or that the vault holds a redacted form (false for B). RED redesigned by measurement: a copy-then-delete mutation keeping the unlink real, whole suite green while [OC-1]'s absence walk finds the bytes → [OC-1] sole red; expectRed one entry, one-element path. Fixture OC-P6: run 1 publish:true, R aged oldest, 49 newer dummies, run 2 judges a DIFFERENT note with a DIFFERENT token asserted distinct from R, 50 → no prune, 51 → R evicted, walk of 50 files finds R's bytes nowhere.

Execute against the code (probes at /private/tmp/claude-501/-Users-gyulafeher-Documents-Claude-Projects-wienerdog/f170987d-e33b-4ab8-8cd7-66ae3b6be861/scratchpad/oc-probe.js and oc-probe2.js; temp state dir only, never ~/.wienerdog): (1) is the three-class partition now exhaustive and correct — walk every exit of the redact arm from :1416 to :1504 and the withhold arm, including a redaction whose promote() later refuses the note (does the vault then hold the scrubbed form or the original? which class is the shelf copy then?), a scrub that succeeds but the commit fails, and the case where the SAME note is redacted in two consecutive runs (class A twice — does the older copy stop being the sole form of anything once the newer exists?); (2) the copy-then-delete mutation: does it really keep every other test green (the dream-pipeline N2 spy counts rmSync — verify the mutation's unlink is still an rmSync on the same path so the spy count is unchanged) and does [OC-1]'s absence walk reliably find the copied bytes (where is the copy written; is that location inside the walk; could the walk be vacuous if the copy is written somewhere the walk skips)? (3) OC-P6: can run 2's "different note, different token" fail to redact (token strength; the gate's severity thresholds) and silently leave the fixture at 50, making the eviction assertion vacuous — is there an assertion that run 2 COMPLETED a redaction? (4) the per-class doc clauses: read the exact sentences; is anything they say false for any class, and is the recovery guidance in docs/runbooks/secret-incident.md honest for a class-B user (whose vault note is intact and unredacted — the secret is STILL IN THE VAULT NOTE; does the runbook say so?); (5) owner items 1–3 in the standing form, no approval claims; cites at both ends; Mirrored Surface Checklist; the structure of the file (round 1's edit once truncated the Contract reference section — confirm all template sections are present and Table O intact). Report findings with file:line, band, LIGHT/HEAVY; verdict: approve / request-changes.
