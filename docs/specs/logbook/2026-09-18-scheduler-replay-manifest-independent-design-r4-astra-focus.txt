Adversarial DESIGN review, round 4, of docs/specs/WP-scheduler-replay-manifest-independent.md (Draft, M). Rounds 1-3 raws and dispositions in docs/specs/logbook/2026-09-18-scheduler-replay-manifest-independent-design-r{1,2,3}-astra-raw.json and -design-review.md. After three rounds each finding a hole in the manifest-coverage suppression predicate, the predicate is DELETED: D10 now reads "coverage never suppresses an unload — discovery does not read the manifest at all"; D5a unloads every candidate D1 yields (recognized basename, contained by withinSchedulerRoot realpath, regular file by lstat, not inside the accepted vault subtree) BEFORE any manifest reverser runs; D11 is the manifest's only influence — a validated entry naming a discovered path sets remove:false (it can only delete less); D3 discloses that the same `would run:` line may appear twice; D7's consent argument re-derived (the acted set no longer depends on manifest contents). Double unload accepted (D13): reverseSchedulerEntry wraps its spawn in try/catch and DISCARDS the result (src/core/manifest.js:529-536, "Best-effort: the entry may already be unloaded"), so a second attempt cannot trip any failed-unload path; per-platform second-attempt exit codes stated (bootout non-zero; systemctl disable --now 0; schtasks /delete non-zero) all discarded at that line; costs = one extra schedulerSpawn per recorded scheduler entry and one extra ADR-0041 refusal line on an unauthorized run. The inverse suppression (D5a records what it unloaded so the recorded reverser skips) was weighed and not taken. RED: srm-alias-counts-as-coverage dropped; srm-record-suppresses-unload added (anchor `const removedSet = new Set([paths.manifest]);`, unique). The surface is declared frozen.

Execute with mocked I/O; never the real scheduler or ~/.wienerdog. Review the simplified design as a whole: (1) with discovery independent of the manifest, enumerate again what can be acted on that should not: a recognized basename inside a scheduler root that is NOT ours but a user's hand-written job with our naming prefix (ai.wienerdog.<x>.plist by a user) — it will be unloaded and, under unload-and-remove, deleted; is that acceptable, stated, and bounded by the owner item (recognition = our prefix + charset is the consent boundary — is that argued honestly)? (2) the manifest byte-compare that guards recorded entries against modified files — does D5a now unload a recorded entry whose file was modified by the user (which the recorded reverser would have refused)? If so, is unloading (not deleting) a modified file the right call, and is it disclosed? (3) D13's discard claim — verify :529-536 is the ONLY spawn path for scheduler entries and that D5a's own spawn also discards (or propagates — which, and is it consistent with R-failed-unload staying deferred)? on an UNAUTHORIZED run (ADR-0041 opt-in absent), does D5a produce refusal lines only, and is the plan/dry-run still complete? (4) the consent argument under D5a-first: disclosed set computed pre-plan, acted set = D5a's + D5b's — can D5a act on something not disclosed (a file appearing between discovery and act — the narrowing re-check covers removal; does it cover the unload phase)? (5) the new RED anchor and the 15 ACs — consistent; cites at both ends; checklist; owner items unchanged, no approval claims. If nothing about the product is wrong, say approve; findings with file:line, band, LIGHT/HEAVY.
