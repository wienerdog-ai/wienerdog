# Raw gate output — wd-reviewer (opus), PR #215 round 2, head 0cbf1b48 (base main = 5fa23ea5). Committed BEFORE adjudication. Condensed verbatim from the agent's final report.

## APPROVE

Round-1 findings verified fixed by execution: #1/#2 PATH stub (3b5f5b5c) — adopt-e2e with no claude on PATH: tests 6, pass 6; #3 row-Z2 citation by construct name (0cbf1b48). Other channels' fixes independently verified: the readdirSync-derived B3 expected spelling applies the reference to exactly the string pick/inferLayout sees (topLevelDirs = readdirSync withFileTypes), behaviour-neutral on this APFS host (NFC and NFD names round-trip byte-identically; old and new gradings agree on four directed cases; ref3 1000/1000 under both), RED partition unchanged (C2 → exactly T4, T5; the new assert.ok carries the B2/B3 reference marker). The round-trip non-vacuity guard is real: reverting both source edits → pass 5 / fail 1, only the round-trip test, "persisted projects_dir must fall back to the built-in default, got \".projects\""; the six-key guard matches renderLayoutBlock (adopt.js:91-103); the '01-Projects' pin is not fragile (inference precedes the config write and scaffold; the fixture's only top-level dir is .projects). Boundary: eleven files = seven Deliverables rows + spec (status flip only, Table D not shrunk) + three logbook raws; boundary-check 0.

New findings:
1. [contract] band B — SPEC-SIDE, route to wd-architect: docs/specs/WP-dot-segment-denial.md:951 — V2's B3 loop still grades against the pre-filesystem drawn string (`const trimmed=name.trim()`), contradicting Table F's own B3 pre-step (the picked directory name after pick's trim, which only ever sees readdirSync output). Two surfaces implementing one canonical grading now disagree (ADR-0031 mirror drift; the spec's V2 is the stale mirror). Cannot flake on this host (normalization-preserving APFS, measured) but can on HFS+ / a normalization-insensitive mount used as TMPDIR. Not the implementer's to fix (the spec file is touchable only for the status flip). Recommended: apply the same readdirSync-derived change to V2 and name topLevelDirs' readdirSync explicitly as B3's source in row A11 / F8. Repeat pattern across two rounds and three channels on "what string B3's reference is applied to" (reports_dir join; readdir spelling; now the spec's V2) → the B3 pre-step deserves one canonical statement with V2 and the test registered as mirrors.
2. [quality] band C — tests/unit/dot-segment-denial.test.js:334: `.find(e => e.isDirectory())` with `assert.ok(fsEntry, '… expected exactly one directory …')` checks existence, not count; assert the directory count is 1 so a leaked entry fails loud instead of grading a sample not drawn. Keep the B2/B3 reference marker. Optional.
3. [quality] band C — tests/integration/adopt-e2e.test.js:587: the new comment cites `src/cli/adopt.js:~245` (the preflight begins at :253-255, throws at :259); name the construct instead ("the dry createPins block in adopt.run"). Optional, cosmetic.

EXECUTED (16 commands, git-archive copies; worktree porcelain identical before/after): V1 npm test 0 (2618/2606/0 fail/12 skipped); V2 acceptance 0 (seed 1788542171656, 110/112 first code points, dot share 0.516, 2000/2000|2000/2000|1000/1000|12/12|87/87|39/39, V2 OK); V3 red-proofs 0 (5 declared/5 selected, PROVEN both ids, RUN: PROVEN); V4 lint 0 (635 files); boundary-check 0; adopt-e2e without claude 0 (6/6); with claude 0; both mutations reverted → adopt-e2e 1 (expected, 5/6); NFC/NFD filesystem probe 0; directed inferLayout probe 0; C1 self-check 1 (expected, T1–T3); C2 self-check 1 (expected, T4–T5); source reads 0.
