Adversarial DESIGN review, round 9 (confirming), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-8 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..8}-astra-raw.json and -design-review.md. Round 8: a forged hash-less {kind:'file'} manifest entry could delete a shelf file during reverse() replay. Fix: Table X row X16 — a shelf guard inside reverse()'s entry loop immediately after the existing global deferred-member guard (manifest.js:781), before kind dispatch, blocking every path-based route for every kind: an entry is shelf-resident if its literal path, its resolved path, or an unanswerable containment question puts it at/under a shelf root (X12 fold, X11 validation; contains() not used because its boolean fails to "not contained" = delete); roots computed once beside schedulerOpts; only narrows; no reverser body/signature/return change; W7 composition verified disjoint by construction (every scheduler D5b target is under a scheduler root; quarantinePreserve date-prefixes every shelf name so none can match R2); landing order stated both ways; AC 18; RED quse-reverse-shelf-guard-dropped anchored on the deferred-member guard line; twelve declarations over eleven criteria.

Confirming round, final. Execute with mocked I/O (temp dirs): (1) X16's placement at :781 — trace what the deferred-member guard at that line does today and confirm inserting the shelf guard after it cannot change the outcome for any entry that is NOT shelf-resident (order of guards; an entry that is both deferred and shelf-resident); (2) "unanswerable containment = preserve": what makes the question unanswerable (realpath error on the entry's path — ENOENT means the target is gone: skip as today, or preserve? — state the bucket) and confirm it is consistent with D15's three-valued rule in the scheduler package, which this package cites as the model; (3) the scheduler-entry kind under X16: a scheduler-entry whose recorded path is inside the shelf is nonsense, but the guard applies "for every kind" — confirm it cannot suppress a legitimate scheduler unload (scheduler roots vs shelf roots disjoint — by construction on all three platforms, including the win32 XML root at <core>/schedules vs <core>/state); (4) the twelve declarations: anchors unique; the new one's mutation restores shipped-main behaviour and reddens exactly AC 18's test; (5) final consistency across Tables K/X/W/Y/B, ACs 1–19, both mirror classes, Platform scope, owner items 1–3 (standing form, no approval claims), cites at both ends. If nothing about the product is wrong, say approve.
