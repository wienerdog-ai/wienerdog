# Raw gate output — Codex plugin adversarial-review, round 5, docs/wp-quarantine-banner-location at 005eed52 (base main = 8302ce8e). Committed BEFORE adjudication. git status --porcelain identical before and after (both empty). Focus text reproduced at the end.

# Codex Adversarial Review

Target: branch diff against main
Verdict: needs-attention

No-ship pending one contradictory dispatch-gate label. Otherwise round-4 closure holds: R3-A’s detector is genuinely three-state at the delegated ledger rename; the no-watermarks precondition is explicit; pre-step-17b migration, quarantine-only, commit, and registry writes do not make the current run’s pointer claim; isolated mkdtemp fixtures plus finally restoration prevent ledger leakage; historical digest-seam references are clearly negative; and Table C, V5, the Q-clauses, and pointer form byte-match f198263a. Initial and final porcelain were both empty. Executed, all exit 0: git status --porcelain=v1 --untracked-files=all; git rev-parse --show-toplevel; git diff --name-status main...HEAD; targeted main...HEAD and f198263a..HEAD diffs (unified 80/25/0, word-diff, stat, name-only, log); targeted rg searches across both scoped documents, ledger.js, dream.js, and dream-pipeline.test.js; targeted nl/sed reads of all contract, criteria, verification, runtime-order, and test-harness regions; SHA-256 comparisons for Table C, V5, Q-clauses, and pointer form; JSON parsing of both declarations (4+2 proofs); bash -n of V5; git diff --check against f198263a and main; git show --stat for fc088b3c, 0b76f0af, and 005eed52; 8302ce8e..HEAD source/test/script diff checks; final status hash e3b0c442… (empty). One rg invocation emitted two command-not-found diagnostics because backticks were shell-expanded; the correctly quoted rerun exited 0 and no state changed.

Findings:
- [medium] [B] Dispatch heading still says only one owner confirmation (docs/specs/WP-quarantine-banner-location.md:17-19)
  Round 4 added a third owner item and the operative gate at line 138 now says all THREE must be answered, but the section’s first-scanned contract still says “one owner confirmation” and line 19 says it is “the only one.” That can cause dispatch to treat the L5 and legacy-record decisions as informational routing rather than blocking owner decisions. The parenthetical “changes no Deliverables row” is also conditional: line 138 says overruling the second or third item can add files or a ledger-schema deliverable.
  Recommendation: Change the heading to state three owner decisions and clarify that only the first is the product-text wording question. Qualify the Deliverables claim as applying only when the recommended dispositions are accepted.

Next steps:
- Correct the dispatch heading/opening sentence, then rerun the closing confirmation.
- Scope objections, routed and excluded from this verdict: no new objections. The existing L1–L4 wording choice, L5 successor wording, and parked legacy-record residual remain explicitly routed.

---- focus text ----
SCOPE: docs/specs/WP-quarantine-banner-location.md (Draft, ~1230 lines) and docs/specs/logbook/2026-09-05-quarantine-banner-location-design-gate-rounds.md, as changed relative to main. ROUND 5 — the closing confirmation after round 4. The WP: four ledger-derived user-facing surfaces (L1–L4) stop naming `state/quarantine/` and stop instructing a delete; one exported pointer constant (L0, PRESERVED_COPIES_POINTER) imported by L3/L4; the digest's banner L5 unchanged; src/cli/dream.js reordered so the undelivered `report.record` is printed at step 17b BEFORE the withheld ledger is written (Table L row L7, criterion 6); V5 reconstructs each amended Done-spec row as `git show main:` base + clause and byte-compares; six RED proofs in two inlined declaration files. Owner items (Dispatch precondition, routed, not counted): confirm the four surfaces stop naming a folder / instructing a delete (recommended); L5's "clears when no withheld copies are left" recommended to the successor.
ROUND 4 STATE: the shadow channel APPROVED round 4 on 0b76f0af with zero findings; the plugin channel's round-4 run was INVALIDATED (the orchestrator wrote an untracked file into the worktree mid-run, so its read-only check failed — the reviewer itself wrote nothing) and its two findings were adjudicated: (R4-A) the new unconditional pointer sentence is false for a LEGACY secret-revert-exhausted record written by a PRE-UPGRADE run that threw after writeLedger and before the step-20 print — PARKED under escalation (ii) as the Dispatch precondition's THIRD owner item (recommendation: accept as a named residual; the per-record stamp question registered with WP-quarantine-preserve-durability under Out of scope), with the two honest fixes and their costs stated; (R4-C) logbook §3.2's byte-identical claim corrected. Verify the third owner item is stated accurately and measured, that it is routed and not silently absorbed, that no byte-exact surface (the sentence, Table C, V5, the Q-clauses) changed since f198263a, and that nothing else moved. ROUND 3 FINDINGS (verify each is GENUINELY closed, not re-worded): (R3-A, converged) criterion 6's failure-injection detector faulted at the DIGEST rename, a step downstream of the first durable claim, so a placement of the record print just after writeLedger (before regenerateDigest) passed it — NOW the detector sits at the LEDGER boundary: fs.renameSync patched for the destination ending in transcript-ledger.json, the rename DELEGATED first so the final ledger is on disk, then an immediate throw; the assertion requires the announcing line and the record's own lines already in the captured output; criterion 6 is explicitly THREE-state (shipped ordering RED, after-writeLedger RED, step 17b GREEN), with the round-2 digest seam measured passing the wrong placement (logbook §3.1). (R3-C) criterion 10's idempotence rationale now states the real invariant (L7 adds no write, no state, no retry). Dropped with the reason recorded: splitting L7 into a sequenced S package (kept atomic — the pointer sentence is only true under the ordering). Named residual: V5 reads the LOCAL main ref; the orchestrator fetches before running it.
CHALLENGE THE FIXED POINT: (a) the ledger-boundary detector — read writeLedger in src/core/dream/ledger.js and steps 4, 5b, 17b, 18–20 in src/cli/dream.js: is there ANY durable write or pointer-bearing print that can precede the step-17b print on the refused or promoted-unpublished arm (the step-4 watermark migration write, the 5b quarantine-only path, the report file on the unpublished arm, git commits)? Is there a placement of the print that is WRONG under Table L row L7 but still passes the ledger-boundary detector — e.g. printed between two ledger writes, printed inside a try/catch that swallows, printed only on one of the two undelivered arms? Does "delegate then throw" leave any state that makes later tests in tests/unit/dream-pipeline.test.js flaky (the patched fs.renameSync is restored in finally — is the ledger left in a shape the next test could read)? (b) the three-state criterion as WRITTEN — can a sonnet implementer satisfy criterion 6's text with a test that is green on step 17b but does not actually fail on the after-writeLedger placement (for instance by asserting only that a fault occurred, or by faulting at the temp write rather than the rename)? Is the "fixture must carry no state/watermarks.json" precondition stated where the implementer will read it? (c) the spec's mirrors of the round-2 measurement — grep the spec for any surviving sentence that still describes the detector as faulting at the digest write or as "both directions"/two-state (Exact contracts, Table L row L7, criterion 6, Verification bullets, Definition of done, Mirrored Surface Checklist): any survivor is a finding. (d) the six declarations and V5 — unchanged since round 2; confirm nothing in the round-3 edits touched them (byte-compare against the round-2 revision f198263a if you can). (e) anything ELSE material a sonnet implementer reading only this spec would get wrong.
OUT OF SCOPE — file as scope objections in a separate routed section, do NOT count toward the verdict: the three Dispatch-precondition owner items (including the parked legacy-record residual — re-arguing the recommendation is a scope objection, not a finding), the owner-ruled `remediation: 'delete'` value, the successor's shelf/retention/durable-state questions, the adjudicated round-zero/1/2/3/4 dispositions (including the dropped split), any code change on this branch (there is none), and every file other than the two in scope.
REPORT: state what you EXECUTED (each command with exit status), then findings with file:line and a materiality band (A: a criterion a wrong implementation passes, silent wrong behaviour, or a user-facing sentence left false; B: caught downstream; C: hygiene), then the verdict. Review is READ-ONLY: git status --porcelain must be identical before and after.
