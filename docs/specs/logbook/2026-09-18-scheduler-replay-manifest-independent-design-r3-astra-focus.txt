Adversarial DESIGN review, round 3, of docs/specs/WP-scheduler-replay-manifest-independent.md (Draft). Rounds 1-2 raws and dispositions in docs/specs/logbook/2026-09-18-scheduler-replay-manifest-independent-design-r{1,2}-astra-raw.json and -design-review.md. Round 2 found (a) the discovered unload ran after the manifest reversers could delete its evidence, (b) an in-root symlink alias could count as coverage while deriving null argv. Fixes: D5 is two phases — D5a the UNLOAD phase before the manifest entry loop (manifest.js:765) so every discovered item is unloaded while its evidence is on disk; D5b the REMOVAL phase after the loop keeping recorded-entry priority, the removedSet skip and D11's separate deletion permission (removes only under R4 = unload-and-remove AND the item's remove flag); D6 restated per phase; reverse()'s unrecordedSchedules = the paths D5a unloaded. D10 gains condition (e): coverage requires deriveUnloadArgv(recorded lexical path, platform) deep-equal deriveUnloadArgv(candidate path, platform), null equal only to null; an alias deriving null or a different target leaves the candidate discovered. New rows S9/S10, ACs 12/13, two absence-shaped RED declarations (srm-unload-moved-after-loop anchored on the entry loop header; srm-alias-counts-as-coverage anchored on deriveUnloadArgv(entry.path, platform)).

Do NOT touch the real scheduler, ~/Library/LaunchAgents or ~/.wienerdog. Execute with mocked I/O: (1) D5a before the loop: an unload attempted on a file the manifest ALSO records as a scheduler-entry — is it now unloaded TWICE (once by D5a, once by the entry's own reverser), and is a double bootout/disable/delete-task harmless on each platform, or does the second attempt's failure now trip the existing failed-unload handling and abort an uninstall that was fine before? Check D10's suppression prevents the double (a covered candidate must not be in D5a's set) and what happens when coverage is decided per (a)-(e) but the entry's reverser then fails; (2) D5a on a dry run: no spawn may happen — confirm the plan/dry-run path still builds the disclosed set without executing D5a; (3) D10(e) — deriveUnloadArgv for each platform: is deep-equality of argv the right notion of "equivalent unload" (launchd: bootout by label vs by path; systemd: disable --now on unit name; schtasks: /Delete /TN) — can two different files derive the same argv (a stale copy and the live one) and thereby count as coverage for each other; can the candidate's own argv be null (a recognized basename that deriveUnloadArgv rejects) and then "null equals null" wrongly count a null alias as coverage of a null candidate; (4) the two new RED anchors: unique, and do the mutations correspond to the properties; (5) anything the reordering breaks in the existing consent argument (disclosed ⊇ acted; narrowing-only re-check) and in D9/D12; ACs now 14; cites; checklist; owner items unchanged, no approval claims. Report findings with file:line, band, LIGHT/HEAVY; verdict: approve / request-changes.
