Adversarial DESIGN review, round 6 (confirming), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-5 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..5}-astra-raw.json and -design-review.md. Round 5: a blanket "the disposer never throws" would have swallowed mechanics-deletion failures and broken uninstall retry. Fix: Table X row X13 — the no-throw promise covers the shelf levels (lstat/non-directory level, ENOTEMPTY/EEXIST, fold ambiguity → preservedQuarantine) and the pre-existing empty-core try/catch only; enumerating paths.state, any step-2 rmSync on a non-shelf child and the recursive rmSync over logs/, schedules/, secrets/ throw byte-for-byte as at 5b77865f; K4's deleter arm narrowed; Y10; AC 16 (EPERM on secrets/ throws, manifest + config present by existence and content, retry succeeds once cleared; the paths.state arm; a same-run shelf ENOTEMPTY that does not throw); RED quse-mechanics-failure-swallowed anchored on `const mechanics = [`; ten declarations over nine criteria.

Confirming round, final. Execute with mocked I/O (temp dirs): (1) X13's boundary: enumerate every fs call in X1 steps 0–4 and assign throw/preserve to each; confirm step 0's unlinkSync of a symlinked state (EPERM → throws? preserved?) has a stated bucket; confirm step 1's readdirSync failure (EACCES on state/) has one; confirm that a shelf-level failure followed by a mechanics failure in the same run reports the shelf AND propagates the mechanics error (order of effects, and does preservedQuarantine reach the caller when the function throws?); (2) uninstall.js after a thrown disposer: manifest and config intact (cite the exact lines that delete them and confirm they run only after a successful return); is the refusal message (W2) still printed on the retry path when the shelf is non-empty AND a mechanics error occurred; (3) the ten declarations: anchors unique where anchored; no declaration reddens another's sole test; the new quse-mechanics-failure-swallowed mutation actually restores the round-5 defect; (4) final consistency across Tables K/X/W/Y/B, ACs 1–17, the two mirror classes, Platform scope, owner items 1–3 (standing form, no approval claims), cites at both ends. If nothing about the product is wrong, say approve.
