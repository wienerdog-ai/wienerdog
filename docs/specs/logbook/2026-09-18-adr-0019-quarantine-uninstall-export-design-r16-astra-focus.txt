Adversarial DESIGN review, round 16 (confirming, final), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-15 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..15}-astra-raw.json and -design-review.md. Round 15: the chain-closure protected set over-protected a symlinked core. Fix: X17 (1a) two classes — (i) shelf subtrees protect the node and every descendant (containment both ways); (ii) chain nodes protect the node and its ancestors only (target must not equal or contain the node; no containment the other way); verified against rounds 11–14's measured cases; new Table W row W10 — when the live sweep returns non-empty preservedQuarantine the run stops before uninstall.js:424 (manifest and config.yaml left in place, reusing :428-432's shape); AC 2 symlinked-core arms; AC 19 for W10; RED quse-chain-node-over-protects and quse-manifest-deleted-after-preserve; twenty declarations over thirteen criteria.

Final confirming round. Apply X21's residual definition first: anything requiring an alias created after protected-set computation is INSIDE the residual — do not report it. Then, executing read-only against the tree (temp dirs only): (1) the two-class rule on the four measured alias cases (rounds 11–14) and on the symlinked-core layout — confirm each gets the intended outcome (preserve the shelf / remove ordinary mechanics) with NO regression among them; (2) W10 + the W2 refusal: two stop points now (pre-plan refusal; post-sweep preserve-stop) — same vocabulary, same exit code, both leave manifest + config; the retry path after a post-sweep stop re-enters the pre-plan gate and refuses with the count — confirm no state in which uninstall can neither complete nor refuse legibly; (3) twenty declarations: anchors unique; no cross-reddening; quse-chain-node-over-protects's mutation (treat chain nodes as subtrees) reddens exactly the symlinked-core removal arm; (4) whole-document consistency: Tables K/X/V/W/Y/B, ACs 1–19, both mirror classes, Platform scope, owner items 1–4 (standing form, no approval claims), cites at both ends; size — M or split (say). If nothing about the product is wrong outside the residual, say approve.
