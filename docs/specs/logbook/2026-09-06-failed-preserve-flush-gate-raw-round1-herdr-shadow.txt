# raw — hermetic Codex shadow (codex exec, read-only sandbox, CODEX_HOME=~/.codex-review-home, gpt-5.6-sol high) — fpdf1
# tip reviewed: d8d14372b979cacb173ca65fb1c41a23076f5149 (detached worktree) ; base main = b1d20ce0cd4f48c6600d6925f760ee9d1b279428 ; committed pre-adjudication
# codex exec rc=0 ; porcelain before/after: identical

{"target":"d8d14372b979cacb173ca65fb1c41a23076f5149","verdict":"needs-attention","executed":["git diff b1d20ce0cd4f48c6600d6925f760ee9d1b279428 HEAD --stat — exit 0","git diff b1d20ce0cd4f48c6600d6925f760ee9d1b279428 HEAD — exit 0; output inspected with the three changed files read in full","nl/rg inspections of validate.js, predecessor specs, dream-validate.test.js, quarantine-preserve-durability.proofs.json, and scripts/red-proofs.js — exit 0","Static count of RED declarations — exit 0: 64 proofs across 12 files; the three existing removal literals retain occurrences: 1","node scripts/mirror-walk.js --scope quarantine-failed-preserve-disposal-flush — exit 0","In-memory execution of V1-equivalent logic after replacing both mandated comments with WRONG COMMENT — exit 0, incorrectly PASS","git diff --check b1d20ce0cd4f48c6600d6925f760ee9d1b279428 HEAD — exit 0","npm test — exit 1 before tests ran: read-only sandbox denied mkdtemp with EPERM","npm run red-proofs -- --wp WP-quarantine-failed-preserve-disposal-flush — exit 1 before proofs ran: read-only sandbox denied mkdtemp with EPERM","npm run lint — exit 1: markdownlint dependency lookup required blocked network access; frontmatter check passed, but the full lint result is unavailable"],"summary":"Do not ship the spec yet. Placement, parent-directory selection, call-site gating, existing RED literals, and F7(a)'s dated routing are coherent, but the claimed win32 Z2 proof is visibly vacuous, canonical Z3 overstates downstream behavior, and the mandated code-comment mirrors are not pinned. The 455-line size is not independently blocking; the substantive drift caused by repeated prose is captured below.","findings":[{"title":"The prescribed win32 test never reaches Z2","file":"docs/specs/logbook/2026-09-06-failed-preserve-disposal-flush-design-gate-rounds.md","line_start":103,"line_end":127,"materiality_band":"B","confidence":0.99,"body":"Classification: criterion/machinery. FP-P1's own output shows every forced-win32 `post-commit-failure` case returning an object, for pristine, compliant, and ungated states. That means the Z2 failure/removal site was not reached: `flushPreservation` returns success before the artifact fsync on win32, so the spec's prescribed artifact-fsync fault (`WP...md:294-299`) cannot drive Z2 there. An ungated Z2 therefore also reports zero fsyncs in this probe, contradicting AC4's claim that V3 is independently falsifiable on both acts. V1 still lexically catches a missing conjunct, but the advertised second proof can pass vacuously.","recommendation":"For forced-win32 Z2, induce a post-flush verification failure such as `patchDestRead(..., 'corrupt')`, and assert that `quarantinePreserve` returned null and the destination removal occurred before checking the fsync count. Demonstrate that the ungated Z2 variant emits one qdir fsync, then update FP-P1 and the implementation note."},{"title":"Canonical Z3 falsely says every failure run publishes and commits","file":"docs/specs/WP-quarantine-failed-preserve-disposal-flush.md","line_start":232,"line_end":232,"materiality_band":"B","confidence":0.97,"body":"Classification: product/contract. Z3 applies to both call sites and both arms but unconditionally says the caller carries on and the run publishes and commits. That is true of the measured redacted-to-withheld fallback, not of a failed withheld preservation: `quarantinePreserve` returns null and the gate throws at validate.js:1442-1458, so that run does not publish or commit. This promises more than the conditional acceptance predicate and conflicts with O13's narrower interface claim.","recommendation":"Replace the sentence with the exact observable contract: the flush result does not change `quarantinePreserve`'s null/no-throw result; downstream behavior remains branch-specific—redacted failure can fall through, while withheld failure still takes the existing abort."},{"title":"The required verbatim code-comment mirrors can be wrong while every specified mechanical gate passes","file":"docs/specs/WP-quarantine-failed-preserve-disposal-flush.md","line_start":241,"line_end":245,"materiality_band":"C","confidence":0.99,"body":"Classification: mirror. These entries require each source comment to copy Z3's reason verbatim, but AC1/V1 checks only that intervening lines begin with `//`; the new RED proofs find only the flush-call lines, and the behavior tests cannot inspect comment text. I executed V1-equivalent logic on an in-memory compliant source with both comment blocks replaced by `// WRONG COMMENT`; it passed. Thus the whole-cell ignored-boolean explanation is not actually verified.","recommendation":"Have V1 match each complete three-line comment plus its call line byte-for-byte, or make each RED declaration's `find` cover the complete comment-and-call block. State explicitly which check owns the two comment mirrors."},{"title":"The owner-decision measurement summary is internally inconsistent","file":"docs/specs/logbook/2026-09-06-failed-preserve-disposal-flush-design-gate-rounds.md","line_start":151,"line_end":217,"materiality_band":"C","confidence":0.98,"body":"Classification: record. The recorded FP-P5 evidence contains one 2.5675 ms median, while section 0.4 summarizes the cost as 2.9–3.0 ms; the primary spec instead claims three medians, 2.57/2.95/3.04, two of which are absent from the repository record. FP-P6 is likewise described in the primary spec as 16 runs formed by 2 acts × 2 arms × 3 fault states, which is only 12; the log's compressed line apparently includes four pristine baselines but does not say so. These are factual defects in the evidence used to price O12/O13.","recommendation":"Record all three FP-P5 runs or narrow the claim to the one preserved run and use a range that includes it. Describe FP-P6 explicitly as 12 compliant fault cases plus four pristine baselines, and include the rows or an auditable aggregate."}]}