Adversarial DESIGN review of docs/specs/WP-quarantine-only-copy-shelf.md (Draft, S, pinned c05a575b), not of code. The question: what the redacted shelf (state/quarantine/redacted/, written at src/core/dream/validate.js:1416 by the redact arm; capped at REDACTED_RETENTION_CAP = 50 and pruned oldest-first by pruneRedactedOriginals at validate.js:1176, invoked once per run after promote() via the pruneRedacted closure at :1558-1560 / src/cli/dream.js:1102) owes a copy that is the ONLY surviving pre-scrub content of a note. The spec's central finding, Table O row O1: "only-copy" is not a partition of the shelf but the shelf's type — the one in-gate delete (:1489-1504) fires only on proven byte-identity, so every copy on the shelf is an only-copy and the prune is the only path in src/ that destroys one. From that it concludes candidate (a) — delete anyway, bounded — is what a count cap on such a shelf already means, and reduces the package to a test [OC-1] pinning row O1, one RED declaration, a clause in docs/runbooks/secret-incident.md and one in docs/GLOSSARY.md; src/ is deliberately absent from Deliverables. Owner items in the standing form: (1) delete anyway; (2) an overlapping-run eviction guard parked to a successor; (3) an unrecorded copy that no prune removes.

Execute against the code with mocked I/O (the retained probe /private/tmp/claude-501/-Users-gyulafeher-Documents-Claude-Projects-wienerdog/f170987d-e33b-4ab8-8cd7-66ae3b6be861/scratchpad/oc-probe.js drives makeGates({stateDir}).secret(...)/.pruneRedacted() on 50-run fixtures; use a temp state dir, never ~/.wienerdog): (1) is row O1 TRUE — enumerate every write and every delete on the redacted shelf across src/ (the redact arm, the in-gate byte-identity delete, quarantinePreserve's temp cleanup, the failed-scrub rollback at ~:1500, the prune, uninstall's disposeCoreMechanics, anything in doctor/warnings) and find any path that leaves a NON-only-copy on the shelf (e.g. a redaction whose scrub later fails and rolls back the vault note, so the shelf copy and the vault note are byte-identical again; a note redacted twice in two runs producing two shelf copies of successive states) — if such a copy exists, O1 is false and candidate (a)'s justification weakens; (2) the prune's selection: oldest-first by what key — mtime, name prefix date, ledger order — and can the ordering evict a copy from THIS run before an older one (the spec's owner item 2 says an overlapping-run eviction is real; characterise it precisely); (3) the claim that a warning line is "unbuyable" because composeWarnings is a pure function of the ledger (src/core/dream/warnings.js:21-26) — is there really no existing durable record (the dream report names each copy at creation; the ledger; run-evidence.jsonl) from which a "N only-copies were pruned this run" line could be rendered without a new record class; (4) the RED declaration expects TWO tests to redden ([OC-1] plus the existing "EP2 redact arm R8" at tests/unit/dream-validate.test.js:1594) — is that honest and stable, or does it couple this package's proof to a sibling's assertion in a way ADR-0042 discourages; (5) the reduction to test + docs: does a package whose deliverable is a test that pins a property and two doc clauses satisfy the template's Definition of done, and is the Security checklist honest that the shelf holds unredacted secrets at 0600 and that the cap bounds disclosure surface as well as disk; (6) cites at both ends, Mirrored Surface Checklist, no owner-approval claims. Report findings with file:line, band A/B/C, LIGHT/HEAVY. End with a verdict: approve / request-changes.
