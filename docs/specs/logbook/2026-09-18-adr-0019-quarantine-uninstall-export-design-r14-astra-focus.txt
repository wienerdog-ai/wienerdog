Adversarial DESIGN review, round 14 (confirming), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-13 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..13}-astra-raw.json and -design-review.md. Round 13: the `symlink` replay kind could unlink a link on a protected shelf's resolution chain. Fix: item (7) reverseSymlink's unlinkSync (manifest.js:314) joins the enumeration; Table X row X20 — no operation of this uninstall removes a link on the resolution chain of a protected shelf (disposer step 0b by X19, reverse()'s symlink kind by X20); a symlink entry is preserved-and-reported when its own target or any intermediate alias on the chain from a lexical anchor to its resolved anchor overlaps a protected target; Table V's column re-read as "the pre-dispatch guard applies" with each row naming its clause; `dir` exemption kept and proven (removes only a virtually-empty directory); AC 18 resolution-chain arm across reverse() + both disposer calls + retry, alias's survival asserted before the original's; RED quse-symlink-entry-breaks-chain anchored on the reverseSymlink dispatch line; seventeen declarations over eleven criteria; enumeration seven items, all gated.

Confirming round, final. Execute against the tree (temp dirs only): (1) the enumeration once more, now that every operation is named — check the ONE remaining kind not in it, `dir`: read reverseDir; confirm it uses rmdirSync (non-recursive) and that a `dir` entry naming an intermediate alias on a shelf's chain CANNOT remove it (rmdir on a symlink → ENOTDIR — confirm on POSIX and what Node does on Windows) — if rmdir can remove a symlink-to-dir on any platform the exemption is a hole; (2) X20's chain computation: the chain from lexical anchor to resolved anchor is computed how — component-by-component lstat/readlink walk? — and is a link OUTSIDE <core> and <claude> (e.g. in /tmp) on that chain protected too (it should be: removing it breaks reachability) — and does the guard then preserve a manifest entry naming a link outside every allowed root, which reverse() would have refused anyway (harmless double-guard — stated?); (3) seventeen declarations: anchors unique; the new one isolates as claimed; (4) final whole-document consistency: Tables K/X/V/W/Y/B, ACs 1–19, both mirror classes, Platform scope, owner items 1–3 (standing form, no approval claims), cites at both ends; size M still honest for a package with seventeen declarations and this much surface — say so or recommend the split the architect reserved. If nothing about the product is wrong, say approve.
