DESIGN REVIEW of a re-derived work-package spec; no product or test code has been written yet. Review ONLY docs/specs/WP-secret-sink-wiring-probes.md and docs/specs/logbook/2026-09-17-secret-sink-wiring-probes-round-zero.md. Exclude docs/marketing/, memory/research/ and userreports/ entirely. This work package is DIAGNOSTIC and tests-only: it writes characterization tests ("probes") that pin what each durable-output sink ACTUALLY does today with a secret-shaped value passed through the repo's redactOnly function, at nine call sites (src/core/alerts.js, src/core/run-evidence.js, src/core/transcripts, src/core/dream/brain.js, src/cli/run-job.js). It fixes nothing. Seven of the pinned behaviours are recorded as KNOWN DEFECTS and asserted in a named form so that a future fixer knows turning them red is the intended outcome: three "truncate" defects (a value straddling the 2000-character field cap leaks a partial head into alerts.jsonl / run-evidence.jsonl) and four "chunk" defects (a secret split across two stream chunks is redacted by neither per-chunk call, so the WHOLE credential lands contiguous in the durable log - a residual the code comments call owner-approved 2026-07-17 but describe as "partially redacted"). Challenge, EXECUTING the real functions with synthetic secret-shaped inputs where you can (use only the repo's own synthetic patterns; never a real secret): (1) is the inventory COMPLETE - find every caller of redactOnly and every writer of a durable state, log, evidence or alert file, and say whether a tenth site exists that the spec does not list; (2) is each measured behaviour in the spec TRUE - reproduce the truncate leak and the chunk leak and confirm which bytes reach the file; (3) can a defect-pinning probe pass VACUOUSLY - for example on an empty artifact, on a fixture whose two writes coalesce into one chunk so no boundary exists, or because the secret-shaped value does not match the pattern it claims to; the spec says defect assertions carry a positive-presence check and that the chunk fixture must force a boundary - is that enough; (4) do the probes risk making a future FIX harder or a regression invisible - a test that pins a defect is green while the product is broken: is the naming and the owner item sufficient that nobody reads green as safe; (5) do the probes or their fixtures write anything secret-shaped to a durable location outside a temp dir, or into CI logs; (6) can each RED-proof declaration (the mutation is applied to the SINK, not the test) discriminate, and does the literal declaration file shown load through scripts/red-proofs.js; (7) do the file:line citations resolve on this tree. Report findings about PRODUCT or SECURITY consequences in the main list; list findings about the spec's own verification machinery separately, marked as machinery. A risk the spec already names and prices as a residual or owner item is not a finding unless its statement is untrue. Owner items are recommendations with overrule costs by design. State what you executed versus what you only read.
