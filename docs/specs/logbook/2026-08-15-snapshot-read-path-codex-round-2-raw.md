# Adversarial design review — external round 2, after round 1's fixes

WP: WP-snapshot-read-path-hardening
Backend: gptsol (Codex subscription), one agent per gate run
Tree: `wp/snapshot-read-path-hardening` @ `dfac10b`; the reviewed spec and ruling
are byte-identical to `b0f18a0`, which the reviewer verified with
`git diff --exit-code`.
Read-only check: `git status --porcelain` empty before and after, byte-identical
(the reviewer ran and reported both).

Raw final output, byte-unchanged inside the fence, committed **before any
adjudication** per `docs/runbooks/codex-review.md` ("Rules"). One transport
artifact was decoded and nothing else was touched: the notification channel
delivered `&amp;&amp;` inside one `executed` string where the reviewer had run
`&&`. No finding text, verdict, confidence, status or line number was altered.

Round ≥ 2, so the reviewer was also required to verify each of round 1's
findings is genuinely fixed rather than re-worded; that verdict is the
`prior_findings_verification` array.

`````text
{"verdict":"needs-attention","summary":"Do not ship the spec yet: the bounded-read acceptance criterion still permits whole-source allocation, and the promised preservation of skip precedence contradicts the mandatory open-before-cap order.","executed":["git -C /Users/felho/dev/repos-to-learn-from/wienerdog status --porcelain (initial) — exit 0; stdout empty","git branch --show-current && git rev-parse HEAD && git diff --exit-code b0f18a0 -- reviewed spec and ruling — exit 0; confirmed wp/snapshot-read-path-hardening at dfac10bc742107748dab9c64686f78f60f9bf98e and reviewed files byte-identical to b0f18a0","wc -l plus grep -a coverage/comment scan plus CI-platform grep — exit 0; reproduced 205/411 line counts, six broker-wiring call sites, the binary-test mirror comment, and ubuntu/macos-only CI","find existing FIFO and Unix-socket paths under temporary/runtime directories — exit 0; found /private/tmp/wdfifo/pipe.md and /private/tmp/zeb_def_ipc_43872","node cap/flag/scanner-boundary probe — exit 0; Node v24.18.0 on darwin, MAX_FILE_BYTES == SCAN_MAX_BYTES == 262144, scanner accepts 262144 and reports oversized at 262145, and O_NOFOLLOW/O_NONBLOCK are present","node symlink/FIFO/socket/directory/character-device open probe with contract flags — exit 0; symlink failed ELOOP, FIFO/directory/character device opened and fstat reported non-file, socket failed before descriptor, and opening the symlink without O_NOFOLLOW followed it","node staged grow/swap/source-directory and current-precedence reproductions — exit 0; grow reported appears-to-contain-a-secret, leaf swap copied outside bytes, source-directory symlink copied outside bytes, and a statically over-cap unreadable file took the cap reason without a read","git show --stat and unified diff for b0f18a0 — exit 0; inspected the round-1 corrections","npm test -- --test-name-pattern \"vault-snapshot\" — exit 0; 120 tests passed","npm run lint — exit 0; markdown/frontmatter passed, shellcheck and PSScriptAnalyzer unavailable and skipped","node bounded-read allocation counterexample — exit 0; allocated a 120965360-byte source-sized Buffer while requesting and reading only 262145 bytes, which still produces the per-file-cap outcome","node whole-file-read probe — exit 0; read the 120965360-byte Node executable in full","node oversized Buffer decode probe — exit 0; 536870889-byte decode threw ERR_STRING_TOO_LONG","MANPAGER=cat man 2 close | col -b | grep around EINTR — exit 0; Darwin documents EINTR as a close failure","npm test — exit 0; full repository suite passed","node unguarded FIFO-open child probe — exit 0; child remained blocked after 500 ms and was killed","node over-cap mode-000 precedence probe — exit 0; current implementation returned the 262144-byte cap reason, while the contract-flag open failed EACCES","git -C /Users/felho/dev/repos-to-learn-from/wienerdog status --porcelain (final) — exit 0; stdout empty and byte-identical to initial status"],"prior_findings_verification":[{"id":1,"title":"The mandated finally starts after a descriptor can already need closing","status":"genuinely-fixed","evidence":"Table A lines 199-200 now start the descriptor scope immediately after every successful open, include fstat and its type refusal, map fstat failure to unreadable, and require every in-scope exit to traverse finally. The FIFO, directory, and character-device probe confirmed successful opens that then fail the descriptor type test, so the widened scope covers the formerly leaking path. The close posture now promises exactly one close invocation rather than guaranteed kernel release; Darwin's close(2) documentation confirmed EINTR, and close-call count is observable by substituting or monkeypatching fs.closeSync."},{"id":2,"title":"The large-file criterion does not distinguish bounded from unbounded reading","status":"partially-fixed","evidence":"Lines 350-358 now make requested and accumulated bytes observable at the read primitive and therefore exclude a whole-file read. They do not bound allocated capacity: the executed counterexample allocated a 120965360-byte Buffer, requested/read only 262145 bytes, and would return the required cap outcome. Thus reading is constrained, but the resource-allocation defect is not completely guarded."},{"id":3,"title":"The socket acceptance criterion contradicts open-failure precedence","status":"genuinely-fixed","evidence":"Table A line 193 and lines 334-339 now classify by the first failing operation. The probe confirmed a socket open fails before a descriptor and therefore maps to unreadable, while FIFO and directory opens reach fstat and map to the non-regular reason. A character device (/dev/null) also opened and reported isFile() false, which is covered by line 192's generic descriptor type rule even though it is not one of line 193's examples. Pre-existing non-regular objects remain rejected by lstat."}],"findings":[{"file":"/Users/felho/dev/repos-to-learn-from/wienerdog/docs/specs/WP-snapshot-read-path-hardening.md","line_start":350,"line_end":358,"confidence":1,"title":"The primitive bound still permits whole-source allocation","body":"The criterion limits bytes requested from the read primitive and bytes accumulated, but it does not limit the capacity allocated for source content. The executed counterexample allocated a Buffer sized to the 120,965,360-byte source, then requested and accumulated only MAX_FILE_BYTES + 1 bytes; it satisfies the criterion and produces the required cap skip while retaining an allocation proportional to the source. That contradicts Table A's claim that allocation stays bounded by the cap and leaves the resource-exhaustion class this WP exists to close able to pass acceptance.","recommendation":"Add a binary acceptance property that the total source-content buffer capacity allocated or retained for one candidate never exceeds MAX_FILE_BYTES + 1, regardless of fstat size or source size. Keep this separate from the existing requested/accumulated-byte and cap-outcome assertions."},{"file":"/Users/felho/dev/repos-to-learn-from/wienerdog/docs/specs/WP-snapshot-read-path-hardening.md","line_start":191,"line_end":204,"confidence":1,"title":"The new operation order cannot preserve today's skip precedence","body":"The spec mandates open, fstat, and read before cap decisions, but line 204 says a candidate failing several checks keeps the reason today's order produces, and line 198 says nothing observable changes. The executed mode-000 oversized-file case disproves that: current code rejects it on lstat size with the per-file-cap reason and never reads it; under the specified order, open fails EACCES first and line 191 requires unreadable. Read failures on an oversized file create the same conflict. An implementer cannot satisfy both contracts, and user-visible diagnostics or tests can legitimately choose opposite outcomes.","recommendation":"Explicitly define the changed crossover precedence: open, fstat, and read failures occur before cap reasons, while caps still precede gates. Amend the cost row, reason-vocabulary row, and acceptance criterion at lines 347-349 so they preserve the reason strings and the unaffected ordering, not every reason assignment produced by today's lstat-size-first implementation."}]}
`````
