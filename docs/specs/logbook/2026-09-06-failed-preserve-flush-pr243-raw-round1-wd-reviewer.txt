# raw — wd-reviewer (opus), PR gate round 1 — WP-quarantine-failed-preserve-disposal-flush, PR #243
# tip reviewed: 22aacfaceeb8c5ae7ddeb2c5a5172e2870547fe9 (detached copy, copied node_modules) ; base main = fdbeb9f2 ; committed pre-adjudication
# porcelain of the review checkout after the run: empty

## REQUEST-CHANGES
The implementation itself is right: the two inserted lines are byte-exact, correctly gated, correctly placed, and every acceptance criterion re-verified green on this tree. What blocks merge is the verification RECORD (a pasted exit code that is false) and one clause of criterion 4 the two forced-win32 tests do not actually assert. One finding is a spec bug for wd-architect.

Findings:
1. [criteria] B — PR body "V2 (--wp scoped)": the pasted rc=0 is false. The spec's literal V2 `npm run red-proofs -- --wp WP-quarantine-failed-preserve-disposal-flush` → RUN: FILTERED, rc=1 (the two new ids PROVEN); under V2's `set -e` the block aborts and V3–V5 never run. Fix: paste the UNFILTERED run — `npm run red-proofs` → 66 declared / 66 selected, RUN: PROVEN, rc=0, failed-preserve-tmp-flush-dropped=PROVEN, failed-preserve-dest-flush-dropped=PROVEN, all seven WP-quarantine-preserve-durability roll-ups PROVEN.
2. [criteria] B — tests/unit/dream-validate.test.js:3612-3614 (Z1 win32) and :3631-3633 (Z2 win32): criterion 4's mandatory reach precondition is not asserted — both tests assert only res === null and !existsSync(path); non-existence is not evidence of removal and neither observes the injected fault firing, so `tracer.events.length === 0` cannot distinguish "reached the act and issued no flush" from "never entered it". Empirically not vacuous today (probe: destCreated=true faultFired=1 rmSync(dest)=1 fsyncs=0 on both arms). Fix per act, before the zero-fsync assertion: observe the removal (traceRemovalThenFlush at :3479, assert an 'rm' event, as the criterion-2 tests do at :3530-3531) and make the fault report itself (wrap failCommit / patchDestRead in a counter, assert ≥1); nest the tracers or add an rm-only tracer so both do not patch fsyncSync/openSync blindly.
3. [contract] B — SPEC AT FAULT, route to wd-architect: docs/specs/WP-quarantine-failed-preserve-disposal-flush.md:411 prescribes the `--wp` form inside a `set -e` block; scripts/red-proofs.js rolls any (wp, criterion) pair with unselected declarations up as FILTERED and exits non-zero (rollUp, :2154); docs/specs/done/WP-dot-segment-denial.md:683-685 states the rule verbatim. V2 as written can never pass. V2 should be the bare `npm run red-proofs` with the expectation `66 declared / 66 selected, RUN: PROVEN` plus the two ids; criterion 5 and the checklist's V1–V3 bullet mirror it and move in the same edit.
4. [quality] C — tests :3446 and :3458 duplicate failDirOpen/failDirFsync nested at :2794/:2802 inside [QPD-2]; defensible (hoisting edits fenced test code) but belongs in "Decisions made" or a one-line comment naming the original.
5. [quality] C — tests :3459-3470: matchedFd never cleared when the descriptor closes (fd reuse, trap vii at :2719-2722); no live misattribution here; clear it after the throw or match by path.
6. [quality] C — PR body V3 note "2699 − 2681 = 18 new assertions" mixes total with pass count; base 2693 total / 2681 pass; branch 2699 / 2687 / 12 → +6 tests; the "no shipped assertion changed" claim is true (diff purely additive).

Verified rather than trusted: boundary clean (three Deliverables + spec; inbox untouched; no package changes; boundary-check rc 0). Contract: the two inserted lines byte-exact at validate.js:985-988 and :1025-1028, gated on the same boolean as their removal plus DURABILITY_AVAILABLE, boolean discarded, immediately before return null; whole-file diff 8 added lines; flushDir( occurs 4 times; qdir is the removed path's parent on both arms and both acts; flushDir catches its own failures so neither insert changes return or throw. No RED declaration re-targeted: all 8 pre-existing validate.js find literals at their declared occurrences (runner substring semantics). New declarations: ids, wp, suite, find at occurrences 1, shared testNamePattern `\[FPDF-Z[12]\]`, expectRed exactly that act's criterion-2 test; set equality confirmed by the per-proof lines. Decision 2 inside the contract (faulting qdir's own open/fsync is Z2's first named route); Decision 3 matches "one per act, per criteria 2, 3, 4"; the criterion-2 assertion is ORDERED (events.slice(rmIdx+1).some). V1 green and non-vacuous (red on base; red on a // WRONG COMMENT mutant). Six test names unique; process.platform and require.cache restored in finally (:3505-3516). Conventions: JSDoc; one conventional commit with both trailers; PR title matches DoD 2; spec Ready → In-Review as its only change. ADR-0031 drift: none; the only ADR-0031-shaped defect is finding 3 (V2 drifted from the runner's semantics).

Executed: boundary-check rc 0; V1 verbatim rc 0 ("V1 OK…"); V1 on base rc 1; V1 on a WRONG COMMENT mutant rc 1; V2 as written rc 1 (RUN: FILTERED); V2 unfiltered rc 0 (66/66, RUN: PROVEN, 97 PROVEN lines); V3 npm test rc 0 (2699/2687/0/12, all six [FPDF-Z…] pass); V4 lint rc 0; V5 scoped mirror-walk rc 0 (RESOLVED; 6 AMBIGUOUS references, never failing); forced-win32 Z2 reach probe rc 0.
git status --porcelain of the review checkout: empty.
