# raw — wd-reviewer (opus), PR gate round 1 — WP-dream-git-env-pinning, PR #234
# tip reviewed: cca7f16430b6c95772b96c3d335d429e28356211 (detached copy, copied node_modules) ; base main = d6a233ea ; committed pre-adjudication
# porcelain of the review checkout after the run: empty

## APPROVE

Executed (foreground, synchronous, in the read-only review checkout):
| step | command | rc | decisive line |
| boundary | node scripts/boundary-check.js docs/specs/WP-dream-git-env-pinning.md $(git diff --name-only d6a233ea...HEAD) | 0 | 8 changed files = the 7 Deliverables rows + the spec file; memory/lessons/inbox.md NOT touched; tests/unit/dream-pipeline.known-calls.js NOT touched |
| V1 | npm test | 0 | tests 2690 / pass 2678 / fail 0 / skipped 12 |
| V2 | node scripts/red-proofs.js | 0 | RUN: PROVEN; PROVEN git-env-inherits-git-dir (criterion 2), git-env-inherits-object-directory (3), git-env-inherits-config-count (4), dream-private-index-dropped (WP-show-slot-own-value-kind criterion 3); roll-ups PROVEN WP-dream-git-env-pinning criterion 2/3/4 |
| V3 | node scripts/red-proofs.js --wp WP-dream-git-env-pinning | 1 | RUN: FILTERED (the second exit shape the spec predicts); same three PROVEN per-proof lines and PROVEN on all three roll-ups; every FILTERED line belongs to another WP |
| V4 | id identity check | 0 | git-env-inherits-config-count,git-env-inherits-git-dir,git-env-inherits-object-directory |
| V5 | three grep -qF mirror greps | 0 | V5 OK |
| V6a | npm run lint | 0 | markdownlint 0 error(s); frontmatter check passed: 269 spec(s); lint passed |
| V6b | boundary-check | 0 | — |

Contract fidelity (read, not tested): buildGitEnv is key-by-key construction (const env = {}; no ...process.env, no delete); only non-node: require is ../paths; U1 PATH inherited; U2 HOME = getPaths().home at call time; U3 no XDG_CONFIG_HOME anywhere; U4 nine win32 keys carried when !== undefined; U4b USERPROFILE set from paths.home; U5 GIT_INDEX_FILE only when indexFile given. Seam: gitIn passes env: buildGitEnv(opts.indexFile); opts.env fully removed (all env occurrences in dream.js grepped: spawnGitPinned pass-through :182, brain :393, containment probe :768, run-token :812 — none a gitIn option); commitNamedSet withIndex passes indexFile: tmpIndex; indexEnv gone; spawnGitPinned byte-unchanged; the ninth shape (show HEAD:reports/warnings.md) is now pinned too. known-calls.js not in the diff; AC1's PRIVATE_VERBS matches the three private rows. ADR-0012: pure append :312-353, heading byte-exact, every checklist obligation discharged, the 2026-08-30 W1 pointer bullet untouched. Table W row W1: a single pure insertion of 1672 chars at offset 6439, line 541 the only changed line in the file; hook-residual sentences byte-intact; opening sentence byte-exact; all four required statements present; pipe count 5 before and 5 after; NEITHER SUPPRESSED NOR DETECTED occurs exactly once before and after (V5's third grep not self-satisfying). Conventions: no package changes; JSDoc only; two conventional commits with trailers; spec In-Review. ADR-0031 drift: none; one unregistered mirror (finding 1).

Findings:
1. [quality] Unregistered mirror of Table U row U4: the WIN32_CARRIED constant (src/core/dream/git-env.js:6-9). The checklist registers git-env.js's "own comments"; a data constant is not a comment and lies on the path AC1's fixture cannot execute. Materiality C. Fix: widen the checklist bullet (spec :658-661) to name WIN32_CARRIED as the executable mirror of row U4's key list. Contents currently correct (nine keys, same order). Registration hygiene, not a defect; no wd-architect round needed.
2. [contract] The testNamePattern addition to dream-private-index-dropped (tests/red-proofs/dream-pipeline.proofs.json:14) is within the spec (id/wp/criterion/why/expectRed unchanged; testNamePattern is none of them; five other declaration files use it) and does not narrow the proof (scripts/red-proofs.js:1602 refuses a proof if any declared identity was excluded by the pattern; V2's PROVEN is machine evidence all three declared tests ran and reddened). Given up: the incidental signal that no OTHER test reddens — and the test that now reddens outside the window is AC1 doing its job; the spec forbade adding AC1 to expectRed. Materiality C, informational; recorded under the implementer's Decision 2.
3. [quality] env.PATH assigned unguarded (git-env.js:29) while win32 keys are guarded (:33): with PATH absent an own key with value undefined; no behavioural consequence (child_process skips undefined values; a PATH-less run fails loudly at resolveExecutable/verifyPin anyway). Materiality C; no change requested.

Test design checked and sound: ENV_KEYS (:40-48) carries XDG_CONFIG_HOME, WIENERDOG_ENV_CANARY, GIT_DIR, GIT_OBJECT_DIRECTORY and the GIT_CONFIG_COUNT/KEY_0/VALUE_0 triple, saved/overwritten/restored around dream.run (:344-372); AC1 asserts the full map (PATH identical and equal to the inherited value, HOME === ctx.home, XDG undefined with one exported, GIT_INDEX_FILE the exact literal on exactly the three private verbs and undefined on the six others, canary absent, no other key) with non-vacuity; the HOME-unset case asserted directly against buildGitEnv() with process.env.HOME deleted/restored; AC5(a) selects the hook only via core.hooksPath in the temp home's global .gitconfig written after setup(); AC5(b) injects the identical hook via GIT_CONFIG_COUNT, asserts the run published and the hook did not fire; AC3's clone decoy strengthens the assertion (an unrelated decoy would abort at read-tree); all three RED finds target the live tail of buildGitEnv with occurrences: 1.

git status --porcelain of the review checkout: empty.
