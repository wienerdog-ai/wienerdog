DESIGN REVIEW ROUND 2 of a work-package spec; no product code has been written yet. Review ONLY: docs/specs/WP-dream-lock-stale-owner-loud.md and docs/specs/logbook/2026-09-17-dream-lock-stale-owner-design-review.md (round-1 raw for reference: docs/specs/logbook/2026-09-17-dream-lock-stale-owner-design-r1-raw.json). Exclude docs/marketing/, memory/research/ and userreports/ entirely. Background: src/core/dream/lock.js keeps an EXPIRED single-run dream lock when its recorded local PID still exists, and src/cli/dream.js exits 0 on that busy decline, so a lock left by an uncleanly-killed dream whose PID is later reused stops memory consolidation forever with no alert. PART ONE - verify each round-1 finding is GENUINELY fixed in substance, not re-worded: (R1-A1) "The pre-boot test is not proof that the probed owner is dead" - the revision REMOVES the boot-proof takeover entirely; no takeover decision changes; confirm no remnant of it survives anywhere in the spec, ACs, RED proofs or the ADR-0012 amendment block. (R1-B1) "A quiet decline prevents the hourly catch-up from observing the six-hour threshold" - the revision keeps the mechanism and corrects the claim: first loud result is the next scheduled daily run; check the five-step timeline against src/cli/run-job.js (last_success write, catchUp overdue test) and check the re-justified 6 h bound. (R1-B2) "The byte-exact recovery message is discarded before the durable alert and email" - the revision narrows the message's claimed surface to the per-run log and an attended terminal, and rewrites the recovery text as restart-then-delete; is that claim now true, is the instruction safe and actionable for a non-developer on macOS, Linux and Windows, and does the message interpolate anything from the lock file? (R1-B3) "A valid huge deadline defeats both takeover and loudness indefinitely" - the revision classifies a finite deadline more than max(24 h, 2 x the contender's timeoutMs) ahead of now as the existing owner-unknown; can a legitimate lock ever trip this, and can a crafted lock still evade BOTH this cap and the loud gate (for example a deadline just under the cap, re-evaluated every night as now advances)? PART TWO - attack what remains: does staleForMs on the busy result leak into any other consumer of acquireLock; can the loud gate fire on a legitimately live long-running dream; once loud, the hourly catch-up repeats the alert - is that priced; does the ADR-0012 amendment block contradict Table L of docs/specs/done/WP-dream-live-owner-lock.md or the 2026-09-15 amendment; does every AC and RED declaration discriminate; do all file:line citations resolve on this tree. Owner items are recommendations with overrule costs by design; disagreeing with one is a welcome finding, their being unratified is not. Report what you executed versus what you only read.
