Adversarial DESIGN review, round 7 (confirming), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-6 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..6}-astra-raw.json and -design-review.md. Round 6: the public `removed` array gained the intermediate rmdirs and changed uninstall's visible count. Fix: rmdir bookkeeping internal; Table X row X4 rewritten — paths.state appears at most once and only when the whole of <state> is gone (final rmdirSync succeeded, or step 0 unlinked a symlinked <state>); intermediate rmdirs never appear; when any shelf level was preserved, paths.state does not appear and preservedQuarantine carries the detail; both caller consumers cited (uninstall.js:471-473 sums, :337-341 renders); AC 7 compares the "Removed N item(s)" line and the full --dry-run plan for the empty-shelves fixture; AC 2 asserts removed holds paths.state exactly once and neither shelf path across all fixtures.

Confirming round, final. Execute with mocked I/O (temp dirs): (1) X4 against the baseline for the four fixtures (no shelves; both empty; redacted non-empty; quarantine non-empty with redacted empty): `removed` and the rendered line and the dry-run plan — identical to 5b77865f for the first two, and for the last two the baseline would have DELETED the shelf (so output legitimately differs: state absent from removed, a preservedQuarantine block present) — is that difference disclosed as intended in W6/AC 7 (byte-identical only when nothing was preserved)?; (2) dry-run: X1 must not rmdir anything in the plan phase — how does the plan predict "state will be removed" vs "state will be preserved" without acting (K2's inventory + the fold rule) and can the prediction disagree with the act (the TOCTOU residual — stated once)?; (3) the ten declarations still correspond; (4) final consistency across Tables K/X/W/Y/B, ACs 1–17, both mirror classes, Platform scope, owner items 1–3 (standing form, no approval claims), cites at both ends. If nothing about the product is wrong, say approve.
