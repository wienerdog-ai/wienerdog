# Raw gate output — Codex plugin adversarial-review, round 2, docs/wp-quarantine-banner-location at 44aa930d (base main = 8302ce8e). Committed BEFORE adjudication. Focus text reproduced at the end.

# Codex Adversarial Review

Target: branch diff against main
Verdict: needs-attention

NO-SHIP: Round-1 fixes A/B/D hold, but V5 still lets a wrong canonical-row implementation pass. The four report outcomes and both derivation proofs are otherwise coherent; the six inlined mutations each match once and parse. Executed: initial status/name/stat check (0; status empty); spec reads via sed ×3 and nl ×2 (all 0); logbook reads ×2 (both 0); source/test/runner inspection via rg/nl ×12 (all 0); branch diff (0); exact extracted V5 on untouched tree (1, expected RED); an earlier hand-reconstructed V5 command (1, discarded due reviewer quoting error); in-memory declaration validation (0; six finds unique, markers valid/absent, six node --check results 0); initial heredoc form of that validator (1, sandbox denied temporary heredoc); filtered test-run attempt (1, sandbox denied mkdtemp before tests); git diff --check plus final status (0; status empty and identical).

Findings:
- [high] [A] V5 still permits arbitrary edits inside the three amended rows (docs/specs/WP-quarantine-banner-location.md:977-979)
  V5 checks that each current fourth cell ends with its extracted clause, then uses numstat 3/3 as proof that nothing else changed. Numstat counts lines, not bytes: an implementer can alter any pre-clause content in Q1, Q2, or Q9 while appending the correct clause. The result remains three added and three deleted lines, and all suffix checks pass. This directly violates acceptance criterion 8's “no other edit” requirement and silently permits corruption of the canonical Done-spec rows. The recorded extra-line rehearsal does not exercise this same-line defeat.
  Recommendation: For each row, read its exact base version with git show main:$Q and require the current row to equal that base row with precisely one expected clause inserted before the final ` |`. This byte comparison should replace—or supplement—the numstat-only assertion.

Next steps:
- Fix V5's same-line blind spot and rerun the compliant, extra-line, and altered-prefix-on-Q1/Q2/Q9 states.
- Routed, not counted: obtain the dispatch confirmation that L1–L4 stop naming a folder and stop instructing deletion; recommendation remains confirm.
- Routed, not counted: L5's mixed-shelf clearing claim remains false and should go to the designated quarantine durability/shelf successor.

---- focus text ----
SCOPE: docs/specs/WP-quarantine-banner-location.md (Draft, 1108 lines) and docs/specs/logbook/2026-09-05-quarantine-banner-location-design-gate-rounds.md, as changed relative to main. ROUND 2 — the closing confirmation. The WP: four ledger-derived user-facing surfaces (ledger.js's exhausted banner L1 and summary line L2, warnings.js's line L3, doctor.js's quarantineReport line L4) stop naming `state/quarantine/` and stop instructing a delete; ONE exported pointer constant (L0, PRESERVED_COPIES_POINTER in ledger.js) is imported by L3/L4; the digest's own banner L5 is NOT changed; dated additive clauses on rows Q1/Q2/Q9 of the Done spec WP-secret-fence-ep2-redact-arm (V5 checks placement and byte-exactness); six RED proofs in two inlined declaration files (Table C) — three shelf-claim proofs and three derivation proofs. Owner items (Dispatch precondition, routed, not counted): confirm the four surfaces stop naming a folder / instructing a delete (recommended); L5's "this notice clears when no withheld copies are left" is false in the mixed-shelf state — recommended to the successor WP-quarantine-preserve-durability.
ROUND 1 FINDINGS (verify each is GENUINELY fixed, not re-worded): (R1-A, converged) the pointer sentence named "each dream report", which does not exist on the refused arm. Fix claimed: "…the dream run that withheld them names each copy and its folder, in its dream report or in the output it printed." with a measured four-arm table (published / unpublished / fallback / refused). (R1-B, converged) V2's lexical single-author check was defeated by an unused import + a sentence composed from two literals split inside the prefix. Fix claimed: a runtime DERIVATION PROOF — a marker appended inside L0's literal must move every carrier; identities pin each carrier's rendered string against a HAND-WRITTEN literal, so a composed (non-deriving) carrier stays green under the mutation and evaluateRed's "declared identity did not fail" is the detection; V2 shrunk to the contiguous-retype case. (R1-C, plugin) V5 accepted marker-only amendments. Fix claimed: clauses extracted from the spec by structural key, cell-4 suffix must equal the whole clause, Done-spec numstat exactly 3/3. (R1-D, shadow) the declarations were not writable from the implementer's reading set. Fix claimed: two complete declaration files inlined with five byte-exact source forms, every find unique, every mutated file node --check'ed.
CHALLENGE THE FIXED POINT: (a) the four-arm table — read promote.js's return outcomes and dream.js's rendering around row G11 and confirm all four arms are named and that on EVERY arm the destination the sentence names actually carries the record (the `fallback` arm publishes the enforcement section — does it always include the copy lines? the `promoted`-unpublished arm — is the printed output the only place?); any fifth arm (an error thrown before the record renders; a crash after the copy is preserved but before the report is written)? (b) the derivation proof — read scripts/red-proofs.js evaluateRed and the two inlined declaration files: under the L0 mutation, do EXACTLY the declared identities fail and does every OTHER test in each suite stay green (the six existing pins in four files were updated to the new sentence — do any of them ALSO redden under the L0 mutation, creating undeclared own-body failures? they would, unless they are in a different suite or are the declared identities themselves — check per file and per testNamePattern); does the doctor declaration's testNamePattern select exactly the intended test through the CLI spawn; is "a composed carrier stays green" a detection the runner actually reports as a FAILED proof (not ERROR)? (c) the derivation proof's own vacuity — could an implementer pin the carriers against the imported constant instead of hand-written literals (the spec forbids it; is that forbidding VERIFIED by any step, or only stated)? (d) V5 — extract it from the fenced block and run it on the untouched tree (red); reason about the compliant state; can the structural key mis-select a clause if two clauses share a key phrase; is the 3/3 numstat gate blind to a change inside one of the three lines beyond the clause? (e) the six declarations — for each: find occurs exactly once on the FIXED tree (apply the Exact-contracts fix in memory and count), replace contains marker, marker absent from the pristine file, mutated file parses; (f) the pointer sentence's own truth under L5 — does the new sentence stay true when the digest's L5 banner (unchanged) and the pointer appear in the same digest, and does anything the pointer says contradict L5's parenthetical? (g) Deliverables — does the implementer touch anything not listed (digest.js must NOT be touched — confirm no criterion requires it); is size M honest at 1108 lines, one atomic WP?
OUT OF SCOPE — file as scope objections in a separate routed section, do NOT count toward the verdict: the two Dispatch-precondition owner items, the owner-ruled `remediation: 'delete'` value, the successor's shelf/retention/durable-state questions, the adjudicated round-zero/round-1 dispositions, any code change, and every file other than the two in scope.
REPORT: state what you EXECUTED (each command with exit status), then findings with file:line and a materiality band (A: a criterion a wrong implementation passes, silent wrong behaviour, or a user-facing sentence left false; B: caught downstream; C: hygiene), then the verdict. Review is READ-ONLY: git status --porcelain must be identical before and after.
