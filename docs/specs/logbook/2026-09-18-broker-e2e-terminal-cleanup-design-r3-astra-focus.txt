Adversarial DESIGN review, round 3, of docs/specs/WP-broker-e2e-terminal-cleanup.md (Draft). Rounds 1-2 raws and the dispositions table are in docs/specs/logbook/2026-09-18-broker-e2e-terminal-cleanup-design-r{1,2}-astra-raw.json and 2026-09-18-broker-e2e-terminal-cleanup-design-review.md. Round 2's finding (deleting the AUTH-BLOCKED short-circuit left a run-job exception after a qualifying broker call uncounted) is fixed by E2 re-dispositioning the detector: a pure helper primaryRunFailures(profileId, runLog, threw) emits AUTH FAILED on the same four patterns the deleted check used and RUN FAILED whenever `threw` is set, pushing into `failures` while every containment and non-vacuity assertion still runs; Table F is its truth table (F3 = the round-2 regression); V-12 extracts both committed helpers from the harness source and proves all rows. Review only what changed and whether the whole now holds: (1) does primaryRunFailures cover the cases the old short-circuit covered AND the ones it missed — an auth failure with no qualifying call, after a qualifying call, a non-auth run-job throw, a run that authenticates but the grant-flip run throws (must stay separate); can any of the four patterns false-positive on ordinary routine output (a routine that merely mentions "401" or "OAuth" in its summary)? (2) is V-12's source-extraction sound — does it actually execute the committed functions or a copy that can drift? (3) with E2, E3, E6, Table D and Table F together, enumerate the verdict for each combination (primary ok/throws, auth ok/fails, marker echoed/not, drafts.create present/absent) and confirm no combination reaches CONTAINED without both consumption and a completed authenticated primary run; (4) any drift left across Table A/B/C/D/F, ACs, V-4/V-6/V-10/V-11/V-12, Amendment 6, the Mirrored Surface Checklist, the Security checklist. Check cites at both ends. Report findings with file:line, band, LIGHT/HEAVY; end with a verdict: approve / request-changes.
