DESIGN REVIEW ROUND 5 (closure round) of a NEW work-package spec; no product code has been written yet. Review ONLY docs/specs/WP-dream-primary-dialogue-projection.md and docs/specs/logbook/2026-09-17-dream-primary-dialogue-split.md (your four prior raws are preserved beside it as …-projection-design-r1..r4-astra-raw.json). Sibling specs in the same diff are context only. Exclude docs/marketing/, memory/research/ and userreports/ entirely. The spec adds an opt-in, deterministic, code-only projection parsePrimaryWithOutcome returning {extract, gateExtract, intakeBytes, parse} from the Claude Code and Codex CLI transcript parsers (src/core/transcripts/). derived_from_untrusted is computed by an OBSERVER over the ORIGINAL stream and never changes default parser output. Its rules are ONE ordered decision procedure (row A5e), now checked by the authors against an executable transcription over 35 synthetic sequences (results table in the logbook): the state starts false and is monotonic; per line - (1) the line did not reach the parser intact -> taint; (2) the record parsed: are BOTH schema checks satisfied - the one its own record type owes (row A5c: for Codex, payload.type is required for response_item only and an unrecognised payload.type taints; session_meta validated separately) AND a DECIDED-TYPE discriminator on every element of any array-valued content, in every envelope whether accepted or declined (row A5c-blocks: the decided block list is text, tool_result, tool_use, thinking, image for Claude and input_text, output_text, input_image for Codex; any other value, including a wrong-case "TOOL_RESULT" or a real-but-unlisted type such as web_search_tool_result, taints) - if either fails -> taint AND this record supplies no dialogue; (3) does the record contain a RECOGNISED tool-result block or payload, including inside a declined envelope -> taint and continue, since one record can both taint and supply dialogue; (4) do the acceptance rules accept it as primary dialogue - if no, classified-and-declined, NO taint; (5) accepted: a USER message is emitted false always, by role; an ASSISTANT message is emitted with the current state; (6) a non-ok stream outcome at end of file means an empty extract. The governing principle (row A5c-why): taint on an unrecognised value exactly where the harness signals tool output - Codex payload.type, and either harness's content-block type - and decline without tainting everywhere else (an unfamiliar Claude top-level record type does not taint unless it carries recognised or unclassifiable block content). Priced as owner items: user text false by role; an unfamiliar Codex payload.type or an unlisted block type (e.g. redacted_thinking) taints the rest of the session until the list is updated, with no diagnostic added; tool_use does not taint; no envelope scan on the Codex side, with the named residual of a tool payload nested inside a declined Codex envelope such as event_msg. PART ONE - verify the round-4 finding is GENUINELY fixed by EXECUTING the real parsers on your paired-result counterexample (type deleted; type = 7) plus a valid-result control, and check the authors' model table against your own independent transcription - do the two agree on every sequence, and is a record that fails step 2 really emitting NO dialogue (not a tainted message)? PART TWO - this is a closure round. Try once more to construct a sequence in which an assistant conclusion is emitted false although external content demonstrably preceded it AND no named residual or owner item covers it. Report only such PRODUCT or SECURITY findings in the main list; list verification-machinery findings separately, marked as machinery. If you find none, say so plainly and give the verdict approve. Do the file:line citations resolve on this tree. State what you executed versus what you only read.
