DESIGN REVIEW ROUND 4 of a NEW work-package spec; no product code has been written yet. Review ONLY docs/specs/WP-dream-primary-dialogue-projection.md and docs/specs/logbook/2026-09-17-dream-primary-dialogue-split.md (your three prior raws are preserved beside it as …-projection-design-r1/-r2/-r3-astra-raw.json). Sibling specs in the same diff are context only (the collection spec was approved at round 3 and is on hold only because it depends on this one). Exclude docs/marketing/, memory/research/ and userreports/ entirely. The spec adds an opt-in, deterministic, code-only projection parsePrimaryWithOutcome returning {extract, gateExtract, intakeBytes, parse} from the Claude Code and Codex CLI transcript parsers (src/core/transcripts/). derived_from_untrusted is computed by an OBSERVER over the ORIGINAL stream, never changing default parser output. The taint rules are now ONE ORDERED DECISION PROCEDURE (row A5e): the state starts false and is monotonic; for each line in order - (1) the line did not reach the parser intact (over-long line, JSON depth over the limit, JSON.parse threw, budget exhausted) -> taint; (2) the record parsed: is the discriminator check its OWN SCHEMA owes satisfied (row A5c - for Codex, payload.type is required for response_item records only; session_meta is validated separately; an unrecognised Codex payload.type taints) - if not -> taint; (3) does the record contain a RECOGNISED tool-result block or payload (row A5d for a Claude message.content block, INCLUDING inside a declined envelope such as an attachment, an unfamiliar top-level type, an assistant record or an isMeta user record; TOOL_OUTPUT_TYPES for a Codex response_item) - if yes -> taint, and continue, because one record can both taint and supply dialogue; (4) do the acceptance rules accept it as primary dialogue - if no it is classified-and-declined, NO taint; (5) if accepted, a USER message is emitted false always, by role, and an ASSISTANT message is emitted with the current state; (6) at end of file a non-ok stream outcome means an empty extract. Two decisions to attack: tool_use does NOT taint (a request carries no external content; the spec measured 1,901 tool_use blocks in 3,789 sampled assistant records); and there is NO envelope scan on the Codex side because Codex signals tool output by payload.type on a top-level response_item - with a NAMED residual, a tool payload nested inside a declined Codex envelope such as event_msg, routed to a runbook and put to an owner item. PART ONE - verify the round-3 findings are GENUINELY fixed, EXECUTING the real parsers and streaming reader on synthetic inputs as before: (P3-1) a normal Codex session_meta header followed by a tool-free final answer must yield false, and the header must not taint; walk EVERY Codex top-level record type the policy recognises against row A5c; (P3-2) all four tool-bearing declined Claude envelopes must taint permanently while leaving raw parser output unchanged, and the benign controls (an attachment without tool blocks, an unfamiliar top-level type without tool content) must NOT taint. Then attack the PROCEDURE as a whole: construct a sequence in which an assistant conclusion is emitted false although external content demonstrably preceded it and the spec names no residual covering it - consider step ordering (3 before 5), a record that taints AND is accepted, sidechain and subagent records, cap and redaction boundaries, the newest-N message cap dropping the tainting record while keeping a later assistant message, and a Claude user record whose array content mixes a tool_result block with text. PART TWO - closure: report only findings about PRODUCT or SECURITY behaviour that the spec does not already name as a residual or owner item; list verification-machinery findings separately, marked as machinery. Do the file:line citations resolve on this tree. State what you executed versus what you only read.
