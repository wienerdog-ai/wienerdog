Adversarial DESIGN review of docs/specs/WP-scheduler-replay-manifest-independent.md (Draft, M, pinned c05a575b), not of code. It closes ADR-0041's residual R-stripped-manifest-orphan (a schedule file our generators wrote that the install manifest no longer records — e.g. after a stripped manifest — survives `wienerdog uninstall`, which replays the manifest's entry list only: src/core/manifest.js reverse() :717 with schedulerOpts :751-758; disposeCoreMechanics :1133 never touches ~/Library/LaunchAgents). Design: Table R recognition = enumerate OUR generators' fully-anchored basename shapes (never a forbidden set; withinSchedulerRoot's `.*` patterns stay the CONTAINMENT gate, not recognition); Table D discovery once pre-plan, disclosed as its own block in the pre-confirm plan (src/cli/uninstall.js:353-354) and --dry-run, passed by value into both reverse() calls (`unrecordedSchedules`, default []), re-checked at act time NARROWING only; argv re-derived by deriveUnloadArgv (ADR-0027) and spawned through schedulerSpawn; a leftover file is classified STANDALONE so ADR-0038's manifest-narrowing rule does not decide it; owner item 1 = unload-only vs unload-AND-remove (recommendation unload-and-remove; the stated overrule cost is that on darwin launchd reloads ~/Library/LaunchAgents at each GUI login and would re-register the booted-out plist against a deleted core — flagged by the spec as documentation, not measured); owner item 2 = strike the residual from owner-signed ADR-0041 by a dated amendment (row marked CLOSED in place; Status/OWNER-SIGNED lines untouched, pinned by greps).

Reason over the code and execute with mocked I/O where useful; do NOT touch the real scheduler, ~/Library/LaunchAgents or ~/.wienerdog. Verify: (1) Table R against src/scheduler/generators.js — construct the exact filenames/labels the three generators emit (launchd plist, systemd .timer/.service, schtasks .xml) for the default and for a user-supplied job name at the edge of the allowed charset, and find any generator output the regexes reject or any near-miss they accept (case, unicode, a trailing dot, a name containing `wienerdog` in the middle); (2) the consent argument: disclosed set ⊇ acted set — trace both reverse() call sites in uninstall.js and the act-time re-check; can the set widen between disclosure and act (a file appearing after discovery), and does "narrowing only" hold under the actual code order; is the widened set correctly excluded from the manifest byte-compare that guards recorded entries, and can an attacker who can write inside our roots make uninstall act outside them (withinSchedulerRoot realpath containment; the lstat regular-file requirement for symlinks — is it in the right place, before derivation); (3) the darwin claim that a booted-out but not removed plist is re-registered at next GUI login — assess from launchd's documented behaviour (RunAtLoad / bootstrap semantics; `launchctl bootout` vs a plist that remains on disk) and say whether the spec's characterisation and its "documentation, not measured" flag are honest; (4) does the package's classification of the leftover as STANDALONE really keep it outside ADR-0038's D rule, or is deleting a file the manifest does not list exactly the "widen a deletion" the ADR forbids — read ADR-0038 and rule; (5) the five RED declarations: are the four measured anchors unique and do the mutations correspond to the properties (drop the widened set; skip disclosure; skip containment; widen past narrowing), and is the fifth (new code) declarable at all pre-implementation; (6) sizing and the interaction with WP-adr-0019-quarantine-uninstall-export (same two files; serialized after this) — anything this design forecloses for it; (7) cites at both ends, Mirrored Surface Checklist, Security checklist, no owner-approval claims. Report findings with file:line, band A/B/C, LIGHT/HEAVY. End with a verdict: approve / request-changes.
