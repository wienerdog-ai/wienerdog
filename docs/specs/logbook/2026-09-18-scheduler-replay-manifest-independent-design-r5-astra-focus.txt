Adversarial DESIGN review, round 5 (confirming), of docs/specs/WP-scheduler-replay-manifest-independent.md (Draft, M). Rounds 1-4 raws and dispositions in docs/specs/logbook/2026-09-18-scheduler-replay-manifest-independent-design-r{1,2,3,4}-astra-raw.json and -design-review.md. Round 4 found that tests/integration/uninstall-core-e2e.test.js inherits XDG_CONFIG_HOME so the widened deletion could remove a developer's real systemd timer. Fix: Table D row D14 — a DISCOVERY root must be `contains`-inside paths.home or paths.core (LaunchAgents is home-derived; the Windows XML root is path.join(paths.core,'schedules'), never APPDATA; the systemd user dir is the only ambient-movable root); reverse()'s own schedulerRoots and withinSchedulerRoot untouched; the integration test added to Deliverables with the literal edit `XDG_CONFIG_HOME: path.join(root, '.config'),` in tempEnv(); named residual R-external-xdg-root-undiscovered (an install whose XDG_CONFIG_HOME genuinely points outside $HOME has its unrecorded units undiscovered — narrower than today); RED srm-external-root-discovered anchored on `gen.systemdUserDir(paths.home, process.env), // $XDG_CONFIG_HOME||~/.config + /systemd/user` (unique); ACs 15/16; Security row stating the contains guard, not the authority predicate, is the proof (realSchedulerAuthority gates schedulerSpawn, not fs.rmSync).

Confirming round — review ONLY the round-4 fix and regressions. Execute read-only against the tree (never the real scheduler or ~/.wienerdog): (1) D14's containment: `contains(paths.home, root)` with realpath on both sides? a home that is itself a symlink; an XDG_CONFIG_HOME inside $HOME via a symlink pointing outside; XDG_CONFIG_HOME relative (the spec says "$XDG_CONFIG_HOME||~/.config" — the XDG spec ignores relative values; does gen.systemdUserDir?); (2) the residual: is "narrower than today" true — today reverse() replays recorded entries via schedulerRoots which DOES include the ambient XDG root, so recorded entries there still reverse; only discovery is narrowed — confirm and confirm the row says exactly that; (3) the test edit: does adding XDG_CONFIG_HOME to tempEnv() change any other assertion in uninstall-core-e2e (systemd unit paths asserted elsewhere in that file?), and are there OTHER tests reaching reverse()/discovery with a redirected HOME but inherited XDG (grep tests/ for tempEnv-like builders, HOME overrides, and `manifestLib.reverse(`); (4) the RED anchor and mutation correspond to the property; ACs 15/16 non-vacuous (15 requires the same files under an inside-HOME XDG root ARE discovered — is that fixture buildable without touching real roots?); (5) cites, checklist, Security checklist, owner items unchanged with no approval claims. If nothing about the product is wrong, say approve; findings with file:line, band, LIGHT/HEAVY.
