Adversarial DESIGN review, round 11 (confirming), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-10 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..10}-astra-raw.json and -design-review.md. Round 10: the replay guard read an absent shelf as a resolution failure. Fix: Table X row X17 — one resolution rule over every path the package resolves: lexical anchors always retained; resolved anchors derived through the nearest validated existing ancestor with the missing suffix appended lexically; ENOENT/ENOTDIR = absence (no anchor, nothing reported, no entry skipped); any other code = unanswerable (entry preserved and reported); (3) and (4) must stay distinguishable; X11/K3/K4 unchanged; AC 18 gains three absence arms; RED quse-absent-shelf-read-as-unanswerable; Y6 and the Security manifest bullet re-worded; fourteen declarations over eleven criteria; convergence note: resolution semantics uniform across X11/X12/X16 under X17.

Confirming round, final. Execute against the tree (temp dirs only): (1) X17(2) "nearest validated existing ancestor + lexical suffix": with <state> absent AND <core> a symlink, the resolved anchor is <resolved core>/state/quarantine — confirm the guard then catches a forged entry naming either the lexical or the resolved form; with <state> present but `quarantine` absent, a forged {kind:'file', path:'<state>/quarantine/x'} whose target does not exist — the file reverser today: ENOENT → skip; under X16 → guard fires first? either outcome preserves — stated?; (2) the (3)/(4) distinguishability requirement: is there a test that fails if an implementer writes `catch { preserve }` (collapsing both) — the new RED's mutation does that; confirm its expectRed is exactly the absence arm and nothing else; (3) fourteen declarations: anchors unique; no cross-reddening; (4) final consistency across Tables K/X/V/W/Y/B, ACs 1–19, both mirror classes, Platform scope, owner items 1–3 (standing form, no approval claims), cites at both ends; and a whole-document read for any remaining pre-X17 vocabulary ("cannot be answered", "resolution fails" without the ENOENT carve-out). If nothing about the product is wrong, say approve.
