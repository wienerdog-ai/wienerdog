Adversarial DESIGN review, round 15 (confirming, final), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-14 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..14}-astra-raw.json and -design-review.md. Round 14: the protected set did not include intermediate links' locations. Fix: X17 (1a) — the protected set is the CLOSURE of each shelf's resolution chain (each link's location, each intermediate target, the final resolved target), computed component-by-component with lstat/readlink; X16/X18/X19/X20 all test against it; AC 1 two-hop arm across both disposer calls and a retry; RED quse-protected-set-is-anchors-only. The family is BOUNDED — Table X row X21: in scope = links at any depth inside the closure as computed at protected-set-computation time; out of scope = named residual R-alias-outside-closure (an alias created after computation that redirects a chain component; same user's own act on 0700 files; closing it needs a filesystem transaction no platform offers and a resident process ADR-0004 forbids); owner item 4 = refuse whenever any symlink exists under <core>, recommendation no, cost stated. Binding: a further alias finding inside the residual's definition is CLOSED by citing the residual.

This is the final confirming round. Two questions only: (A) Is the closure definition (1a) correct and computable — for each shelf lexical anchor, the walk resolves component by component; a component that is a link adds its location and follows; a loop (ELOOP) or a component that fails to lstat for a non-ENOENT reason = unanswerable → preserve everything the walk has collected plus the lexical anchor; absent component = stop, the closure ends at the last existing component + lexical suffix — confirm this is exactly what X17 now says, and that the closure is computed ONCE before any mutation (0a) and reused by reverse()'s guard and both disposer sweeps in the same process (uninstall.js runs reverse() then the disposer twice — is the set computed once per uninstall run, or once per call? if per call, the second call recomputes through a possibly-changed chain — is that inside the residual?). (B) Is X21's residual definition precise enough that a reviewer can decide, for any alias finding, whether it is inside or outside — apply it to the four alias shapes rounds 11–14 found and to one new one of your choosing; if your new one is inside the residual, say so and do NOT report it as a finding. Then: eighteen declarations — anchors unique, no cross-reddening; whole-document consistency (Tables K/X/V/W/Y/B, ACs 1–19, both mirror classes, Platform scope, owner items 1–4 in the standing form, no approval claims, cites at both ends); size — M or split. If nothing about the product is wrong outside the residual, say approve.
