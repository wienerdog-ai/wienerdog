PR GATE ROUND 1 — PR #202 — wd-reviewer (spec-fidelity gate, claude subagent), tip eb5a682d. Verdict REQUEST-CHANGES. Relayed verbatim from the agent's final message.

# REQUEST-CHANGES

Worktree left untouched (clean, still `wp/audit-c-close-disposition` @ `eb5a682d`). All probes ran in `mktemp -d` fixtures or a throwaway `--shared` clone, since deleted.

## What I executed (all from `/Users/gyulafeher/Documents/Claude_Projects/wienerdog-wt/audit-c-close-disposition`, `main == origin/main == d8e4afb6`)

| Command | Exit | Result |
|---|---|---|
| `git diff --numstat origin/main...HEAD` | 0 | 8 files; `docs/HANDOVER.md` = `1 1` |
| `node scripts/boundary-check.js docs/specs/WP-audit-c-close-disposition.md $CHANGED` (under **bash**) | **0** | no offenders |
| `node scripts/boundary-check.js` (no args) | 1 | usage — AC6's mandatory-argument property confirmed |
| V1 (D1 a/b/c) | 0 | output byte-identical to the pasted logbook/PR text |
| V2 (a) | 0 | `pinned set = 9 shapes, none of: add commit clean reset status stash` |
| V2 (b) | 0 | same SHA `47d05ff6403c8e6c3cf635ea6eb9263738432773` |
| V3, V4, V5 | 0, 0, 0 | all three byte-identical to pasted output |
| **V6 in full, `RULING=E1`, on committed bytes** | **0** | `V6 OK (ruling E1)`; frontmatter 267/4; lint 0 issues |
| `node scripts/check-frontmatter.js` | 0 | passed |
| `npm run lint` | 0 | markdownlint 630 files / 0 issues, shellcheck, PSScriptAnalyzer, frontmatter |
| V6 violating-state probes A/B/C (clone) | 1 | HANDOVER cell mutated → red; stub `REQUIRED VERIFICATION` removed → red; stub `Draft`→`Ready` → red |
| V6 violating-state probe D (stray `src/STRAY.js`, clone) | **0 — masked** | see finding 2 |

Byte-verifications (python diff, all exact): Table D header + rows D1–D5 in the logbook are **byte-identical** to the spec's; Table E's retired-mechanism paragraph is **verbatim**; both stubs' frontmatter blocks are **verbatim** against the spec's fenced YAML; both stubs' seven body items and their `REQUIRED VERIFICATION` bullets are byte-identical to the spec's prose except item 1 of each, which only adds the qualifier "of `WP-audit-c-close-disposition`" to the "Reproduce with V1 (b)/(c)" pointer (an improvement, not drift). The owner ruling quote in logbook §4 is **byte-identical** to `git show origin/docs/owner-rulings-2026-09-02:docs/specs/logbook/2026-09-02-owner-rulings-stub-queue.md` lines 50–67 after stripping `> ` (verified after `git fetch origin docs/owner-rulings-2026-09-02`); the ruling is item 3, option (i) QUEUED, currency first — matches the Dispatch precondition and both stubs' queue statements.

**Boundary: clean.** 8 changed files = 3 Deliverables (`docs/specs/logbook/2026-09-02-audit-group-c-disposition.md`, `docs/specs/WP-dot-segment-denial.md`, `docs/specs/WP-instruction-basename-currency.md`, `docs/HANDOVER.md`) + the spec itself (always allowed) + 3 pre-existing round-5 logbook/raw files under the boundary-free `docs/specs/logbook/` prefix. `docs/specs/WP-dream-git-env-pinning.md` is correctly absent from the diff and its `93072b1d` amendment is present and checked by V6 (c). No stray file.

## Findings

1. **[criteria] band B — fix.** `docs/specs/WP-audit-c-close-disposition.md:508-513` (Verification steps preamble) and `:717` (Definition of done item 1) require V6 to be observed in **three** states — "the deliverable-ABSENT state **as well as** compliant and violating" — with "the state exercised, the command, and its output" reported. `docs/runbooks/spec-authoring.md:32-37` states the same rule independently ("absent → red, compliant → green, violating → red"). PR #202's body reports only the ABSENT state (`FAIL: expected exactly 1 disposition logbook entry, found 0`, rc=1) and the compliant state (`V6 OK`, rc=0). No violating-state run is reported. **Fix:** in a throwaway clone of the branch, commit one deliberate violation and paste V6's output and rc. Any of these three works and I verified each reddens: (a) change `**Open — four residuals**` to `**Open — three residuals**` in `docs/HANDOVER.md:27` → `FAIL: Table E row E1 occurs 0 times in HANDOVER, want exactly 1`; (b) truncate `docs/specs/WP-dot-segment-denial.md` at `## REQUIRED VERIFICATION` → `FAIL: … carries no REQUIRED VERIFICATION section`; (c) set `status: Ready` in `docs/specs/WP-instruction-basename-currency.md:4` → `FAIL: … is absent or not Draft`. Do **not** commit the mutation to `wp/audit-c-close-disposition`.

2. **[contract] band B — SPEC BUG, route to wd-architect. Not the implementer's to fix.** `docs/specs/WP-audit-c-close-disposition.md:670-679` — the V6 block has no `set -e` and the last three commands (`node scripts/check-frontmatter.js`, `node scripts/boundary-check.js …`, `npm run lint`) carry no `|| { …; exit 1; }`, unlike every check above them. The script's exit code is `echo "V6 OK"`'s. **Measured** in a clone at `eb5a682d` with an extra `src/STRAY.js` committed: boundary-check printed `Files outside the spec's Deliverables table: src/STRAY.js` and V6 still printed `V6 OK (ruling E1)` and **exited 0**. This is a mirror drift against AC6 (`:487-492`), which asserts "V6 passes end to end … **including** `node scripts/check-frontmatter.js`, `npm run lint`, and `node scripts/boundary-check.js …`" — V6's green does not, in fact, assert any of the three. It is also exactly the false-green class ADR-0042 was written for ("a declared proof that stops proving"). **Recommended spec fix:** append `|| { echo "FAIL: <gate>"; exit 1; }` to each of the three, or put `set -euo pipefail` at the top of the V6 block. **Disposition for this PR: residual, not a re-implementation** — I ran all three gates individually and they are green (exit 0 each), and the implementer pasted them separately in the PR body, so the substance holds; only the spec's proof needs hardening.

3. **[quality] band C — fix (one sentence).** PR #202 body, under the V5 block: "*(Full V1–V5 command text and this pasted output also live in the logbook entry per its exact contract, item (3)…)*". The logbook (`docs/specs/logbook/2026-09-02-audit-group-c-disposition.md:62-110`) carries the **output only**, no command text. The spec's exact contract (`:161-163`) requires only "the command output pasted for each of V1–V5", so the deliverable is correct — the PR body's claim about it is not. Reword to "Full V1–V5 output also lives in the logbook entry…".

4. **[quality] band C — residual (note in the PR body, do not edit the stubs).** `docs/specs/WP-dot-segment-denial.md:19` and `docs/specs/WP-instruction-basename-currency.md:19` cite `docs/specs/logbook/2026-09-02-owner-rulings-stub-queue.md` with no note that it is not in the tree — it lives only on `docs/owner-rulings-2026-09-02` (PR #201, unmerged). The disposition logbook does flag this (`:114-116`); the two stubs do not, so if #201 stalls, two Draft specs carry a dead path. Cheapest disposition is a line under "Discovered issues" recording the ordering dependency (merge #201 before or with #202); editing the stubs to add "(PR #201)" is also acceptable and stays inside the Deliverables boundary.

**Nothing found under [convention].** Conventional commits on all four implementation commits with the correct `(WP-audit-c-close-disposition)` scope; PR title matches Definition of done item 2 byte-for-byte; PR template fully filled including `Generated-by: claude-sonnet-5`, "Decisions made", and "Discovered issues" carrying the parked ADR-0029 question as the spec's Implementation notes require; spec `status:` flipped to `In-Review`; docs-only, nothing in `src/` or `tests/`; `<!-- markdownlint-disable -->` in the logbook matches the established convention (45 of 228 logbook entries); unchecked `- [ ]` acceptance boxes match the repo convention (0 ticked across `docs/specs/done/`). AC1, AC2, AC2b, AC3, AC3b, AC4, AC5 and the idempotence N/A all hold on committed bytes; AC5's literal property verified directly — exactly one `harness-refusal` line in the logbook (`:18`), it contains `non-load-bearing`, and neither stub nor `docs/HANDOVER.md` contains the phrase.

## Contract-density / closed-contract drift check (ADR-0031)

No contract-dense inline prose needing extraction, and no promotion of a mirror to primary: Table D is reproduced into the logbook byte-identically under the spec's own declared ownership transfer (`:161-166`), and Table E row E1 reaches `docs/HANDOVER.md:27` byte-identically, enforced by V6 (d)'s `grep -cFx`. No already-settled canonical contract is silently reinterpreted — D1's "*the four names it ENUMERATES*" wording, D2 (b)'s owner and channel-set obligation, D4's withdrawn "nothing new is owed", the 2026-08-31 `reference-transaction` non-reopening, and E2-as-pointer all survive verbatim into the logbook, and the two successor stubs carry the class-proof and dated-inventory obligations exactly as the spec's required-verification clauses state them. Round 4's `F1–F4` residue is fully gone (zero hits across all four deliverables). The **one** drift is finding 2: acceptance criterion AC6 has fallen out of agreement with the V6 command it names, since V6's exit code does not carry the three gates AC6 credits it with. That is a criterion-vs-verification mirror, and the fix belongs in the spec's canonical V6 block, not in a deliverable — hence the wd-architect routing. This is round 1 of implementation review (rounds 1–5 were design-gate rounds on the spec), so no escalation threshold is reached.
