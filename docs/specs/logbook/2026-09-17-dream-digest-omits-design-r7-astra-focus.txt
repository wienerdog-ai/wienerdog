DESIGN REVIEW ROUND 7 of a work-package spec that was re-opened after going Ready; a partial implementation exists on another branch and is NOT in this diff. Review ONLY docs/specs/WP-dream-digest-omits-own-job-alerts.md and the 2026-09-17 dream-digest-omits logbook entries under docs/specs/logbook/ (prior raws are beside them, including -design-r4/-r5/-r6-astra-raw.json). Exclude docs/marketing/, memory/research/ and userreports/ entirely. THE DESIGN as it now stands: the existing step-19 digest render in src/cli/dream.js stays exactly as on main - UNFILTERED. ONE additional FILTERED render is the LAST STATEMENT OF THE LOCKED BODY: after the inner try/finally that tears down the workspace has completed, and BEFORE the outer finally that cleans scratch and releases the dream lock. It is reached only when the body and the workspace teardown succeeded, and it runs while this process still holds the dream lock, using the ledger this run holds. There is NO recovery mechanism. The filter omits an own-job alert record only if ALL THREE hold: WIENERDOG_JOB resolves via findJob to run: builtin:dream; a valid supervisor-minted WIENERDOG_DREAM_RUN_TOKEN is present; the record's `at` is STRICTLY EARLIER than this process's start instant (missing, empty, unparseable or equal means SHOWN). Hiding a GENUINE unresolved alert is the worst outcome; showing a stale one is merely today's behaviour. PART ONE - verify the round-6 findings are GENUINELY fixed, by EXECUTING production run() control flow and function bodies with mocked I/O as before: (R6-1) "the unlocked render can erase a newer quarantine warning" - the render is now under the lock; can any second dream interleave, and is the closure's ledger current at that point; (R6-2) "ordinary concurrent writers can trigger false job failures" - dream-to-dream overlap is gone; the spec now WITHDRAWS the temp-substitution prerequisite, states that an attended `sync` (which takes no dream lock) publishing between this render's rename and its post-rename lstat makes writeFilePrivate throw with no attacker, that this exposure is PRE-EXISTING for step 19 today, that this WP adds one more window per fully successful supervised run, and gives the measured consequence (exit 1, last_success unchanged, last_status error, clearAlerts skipped, a job-exited-1 alert); priced as owner item O4 - is every part of that true; (R6-3, machinery) Deliverables, AC6, AC7 and the mirrors are reconciled with the design, and "exactly two writes" is withdrawn in favour of "one additional write against main's count for the same fixture" - is any instruction left that would steer an implementer back to filtering at step 19, arming recovery, or rendering after the lock is released. Also check the re-derived exit inventory (it now includes dream.js:740) and the two named residuals: (1) cleanScratch is a bare fs.rmSync with force that can still throw EACCES/EBUSY/EPERM after the filtered render, so a run whose work succeeded exits 1 with the filtered digest on disk; (2) the concurrent-sync case above. PART TWO - closure: report only findings about PRODUCT behaviour a user or consuming process would observe that the spec does not already name as a residual or owner item; list verification-machinery findings separately, marked as machinery. Do the file:line citations resolve on this tree. State what you executed versus what you only read.
