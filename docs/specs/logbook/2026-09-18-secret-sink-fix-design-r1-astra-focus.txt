Adversarial DESIGN review of three chained work-package specs and one ADR, not of code: docs/specs/WP-secret-sink-redact-before-truncate.md (S), docs/specs/WP-secret-stream-safe-cut-redactor.md (M), docs/specs/WP-secret-sink-chunk-fix.md (M, depends on both), docs/adr/0043-safe-cut-stream-redaction.md, with measurements in docs/specs/logbook/2026-09-18-secret-sink-fix-design-measurements.md. They fix seven KNOWN DEFECTS pinned by docs/specs/done/WP-secret-sink-wiring-probes.md (Table S; probe tests in tests/unit/dream-brain.test.js and tests/unit/scheduler-runjob.test.js): a value redacted per stream chunk by redactOnly (src/core/secret-scan.js) at four tee sites (src/core/dream/brain.js ~:496-520, src/cli/run-job.js ~:1040-1060) is missed when a match straddles two chunks; and three sites truncate a field to 2000 chars before scanning, leaving a 24-char head.

This is a malformed-input robustness and correctness review of the DESIGN. Reason over the code and execute the production functions with mocked I/O where useful; do not construct exploit payloads. Verify: (1) Truncation: `redactOnly(v).slice(0, CAP)` — is it complete? Can a redaction token itself be split by the cut in a way that leaves a misleading fragment, and does the spec say what a field over SCAN_MAX_BYTES becomes? (2) The safe-cut criterion in the stream redactor: three positively-enumerated cut conditions (line-terminated; the line does not end in an open key binder; no open PEM block) with STREAM_REGION_MAX = 32768 as the only forced cut — enumerate the rule families in secret-scan.js (SENSITIVE_KEYS, SEP, the PEM rule, Bearer, JSON value, extended assignment, keyword-bound entropy) and find any family that can still match across an accepted cut; check the ×4 < SCAN_MAX_BYTES sizing argument against the actual constants; check what happens to a line longer than STREAM_REGION_MAX with no newline (a forced cut inside a possible match — is the residual risk stated?). (3) Held-back bytes: flush on 'end' AND before the derived values (stderrTail/stdoutHead/stdoutTotalLen in brain.js's exit handler; run-job's failure line) are read — trace the actual event ordering in Node child_process (can 'exit' fire before the stream 'end'? can 'close' be the right event?) and check the spec's chosen events keep per-stream byte order and lose nothing on a child that is killed. (4) The seven probe conversions and their RED declarations: is the inverse-mutation design sound (does converting a defect probe to the safe form keep it non-vacuous — i.e. does the sleep 0.3 boundary still provably form)? (5) The ADR: does it state the residual honestly, and does anything claim owner approval (it must not; the Status form is "ACCEPTED under standing authorization — owner signature pending")? (6) Chain coherence: Deliverables, depends_on, contract-table ids, the order the three must land in. Check cites at both ends. Report each finding with file:line, what the spec says, what the code says, band A/B/C and LIGHT/HEAVY. End with a verdict per spec and for the ADR: approve / request-changes.
