Adversarial DESIGN review, round 22 (confirming, final), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M; to be split into A = the gate and B = the deletion-side contracts after the gate closes). Rounds 1-21 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..21}-astra-raw.json and -design-review.md. Round 21: W10 blocked forever on a cleared shelfGuarded path. Fix: W10 counts only OUTSTANDING entries — each shelfGuarded path lstat'ed at decision time and classified with X17's vocabulary (absent = not outstanding; unanswerable or existing = outstanding); scoped to shelfGuarded alone (the other two inputs are current by construction); AC 19 remedy-terminates arm; RED quse-w10-blocks-on-absent-guarded-path; the Security checklist names three failure directions (destroying text; stranding a credential; refusing forever); 26 declarations over 13 criteria; ~1,950 lines.

Final confirming round. Apply the three named residuals first — anything inside is CLOSED. Then, read-only: (1) the three failure directions as a checklist over the whole design: for each of W3 (pre-plan refusal), X22 (set-level abort), X13 (mechanics propagation), W10 (retention stop) — can any of them fire on an install with NO shelf content and NO error, i.e. refuse or block an ordinary uninstall — walk the ordinary-install path and the symlinked-core path once more against the FINAL text; (2) the W10 outstanding filter's TOCTOU (lstat at decision time, then :424 deletes the manifest — a file preserved between the two is R-post-ledger-preserve — confirm the residual covers it and that W10's row says so); (3) 26 declarations: anchors unique; no cross-reddening; (4) whole-cell re-read of W10, X22, X17, X19; Tables K/X/V/W/Y/B, ACs 1–19, both mirror classes, Platform scope, owner items 1–4 (standing form, no approval claims), cites at both ends. If nothing about the product is wrong outside the residuals, say approve.
