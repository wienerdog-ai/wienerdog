# Raw gate output — Codex plugin adversarial-review, round 3, docs/wp-dot-segment-denial at 2eba621f (base main = 29c61d03). Committed BEFORE adjudication. Focus text reproduced at the end.

# Codex Adversarial Review

Target: branch diff against main
Verdict: needs-attention

NO-SHIP. Three band-A grading holes remain. The reference predicate, Table E verdicts, six-test RED partition, signals, and one-line roll-up are otherwise correct. All requested mutants failed: ASCII-only 3184/5000; length≤16 3396/5000; length>1 5000/5000 but Table E 34/38; NFKC 4955/5000; z∪D 2464/5000; fitted-18 B1 1010/2000. V2 extracted as written parsed and printed its seed/precheck, then exited 1 because the read-only sandbox denied mkdtemp; the full mutant replay therefore ran in memory. Scope objections: none counted. Initial and final git status were identical and clean.
Executed commands (exit): `git status --porcelain=v1` initial/final (0/0); structural `rg` searches over both scoped documents (0, except the deliberate forbidden-character no-match check: 1); `nl -ba … | sed -n …` reads covering the complete WP, relevant logbook ranges, four production modules, and `scripts/red-proofs.js` (all 0); `sed -n '827,976p' … | zsh` exact V2 extraction (1, EPERM on mkdtemp after successful parsing); `git diff --check main -- <scoped files>` (0); `git diff --stat main -- <scoped files>` (0); `git diff --unified=4 a3985def HEAD -- <WP>` (0); `git diff --name-only main` (0); two `git show a3985def:…` comparisons (0); `wc -l <scoped files>` (0); heredoc-based in-memory replay attempt (1, sandbox denied shell temporary file); pipe-fed in-memory six-mutant replay (0); N=200 precheck probe (0); N=200-versus-1000 wrong-predicate probe (0); seed-selection probes and searches (all 0); reference/coercion edge probe (0); synthetic `rollUp` verification (0).

Findings:
- [high] [A] V2 permits fewer than 1000 draws per enforcement point (docs/specs/WP-dot-segment-denial.md:858-866)
  F3 says `WD_HELD_OUT_N` may only raise the 1000-draw floor, but V2 assigns the environment value without validating it. N=200 passed the generator precondition for all three tested seeds (63–69 distinct first characters and an allowed dot share). An in-alphabet wrong B1 predicate then scored 400/400 at N=200 but 1998/2000 at the required N=1000. Thus a wrong implementation can pass the written verification by weakening its sample.
  Recommendation: Reject N unless it is a finite integer ≥1000 before drawing. Add a self-check proving `WD_HELD_OUT_N=999` exits nonzero.
- [high] [A] Qualifying evidence may use an adversarially selected seed (docs/specs/WP-dot-segment-denial.md:840)
  The command accepts `WD_HELD_OUT_SEED`, but no acceptance rule requires an unset seed for the PR run. A B1 predicate that incorrectly admits dot-prefixed segments whose second code point is `漢` scored 2000/2000 with seed 78 while its precheck passed at 112/112 and 0.509; seed 79 exposed it at 1990/2000. Table D and E contain no such input, so seed 78 makes all specified V2 oracles green. F7 honestly disclaims unsampled points, but it does not disclose that the implementer can choose the sample and defeat the stated probability argument.
  Recommendation: Require the qualifying PR run to begin with `WD_HELD_OUT_SEED` unset and mark supplied-seed runs as reproduction-only. Put that guard directly in the fenced verification command.
- [high] [A] Pruning removed grading of inferLayout’s distinct reports_dir path (docs/specs/WP-dot-segment-denial.md:897-907)
  At a3985def the implementation notes explicitly required “Grade this key” for `<reports-top>/dreams`. The current V2 B3 loop always creates only `*-projects` and inspects only `projects_dir`; Table E covers projects, inbox, and daily, but never reports. A wrong producer that validates picked directories while allowing `.myreports/dreams` through therefore passes every specified B3 oracle, emits a prohibited value during adopt, and leaves the reader to discard it silently. This contradicts the logbook’s assertion that pruning deleted no contract.
  Recommendation: Restore explicit T4 reference-equality grading for a generated `*reports` directory and its joined `<name>/dreams` result. Prefer cycling B3 draws across every producer keyword.

Next steps:
- Close all three band-A holes, then rerun the round-3 confirmation with an unset seed and the enforced N≥1000 floor.

---- focus text ----
SCOPE: docs/specs/WP-dot-segment-denial.md (Draft, 1076 lines) and docs/specs/logbook/2026-09-04-dot-segment-denial-design-gate-rounds.md, as changed relative to main. ROUND 3 — the closing confirmation after a circuit-breaker redesign. The WP: the class rule "no dot-prefixed segment is admitted or emitted" at three enforcement points (makeAdmit loop placed LAST; isSafeRelativePath in layout.js with layout-infer.js's copy deleted; thereby inferLayout/adopt --yes). Grading is now EQUALITY with a one-line reference predicate (Table A row A11: `[...seg][0] === '.'` after each point's documented pre-step — promote.js's fold at B1; each `/`-segment of coerceScalar(raw).value at B2; each segment of the picked directory name after pick's trim at B3) over a seeded full-alphabet generator (112 code points, lengths 1–65, ≥1000 draws per point, seed printed, first-character coverage precondition ≥ 60), plus Table D (29 handoff paths, the product obligation) and Table E (fixed boundary rows incl. bare `.`, `..`, empty, the trims, the look-alikes as LEGAL, a non-ASCII tail and a 36-char name as refusals), two RED proofs over six named test identities partitioned at the B1 / B2-B3 seam.
ROUND 2 FINDINGS (verify each is GENUINELY closed): the ASCII-only and length-capped predicates scored 686/686 on the old sample-based oracles; the redesign replaced the sampled families with reference equality. Verify: build both mutants plus `length > 1`, NFKC, the z ∪ Table D fitted matcher, and a fitted 18-name list in memory and score them against V2 AS WRITTEN (extract from the fenced block; if the sandbox denies temp writes run in memory and say so) — each must fail somewhere; the spec claims `length > 1` passes the reference grading (5000/5000) and is caught only by Table E — confirm and judge whether that dependency is stated honestly (Table F row F7).
CHALLENGE THE FIXED POINT: (a) the reference predicate — is `[...seg][0] === '.'` after the stated pre-steps EXACTLY the ruled property and nothing more or less: what about an empty segment (`[...''][0]` is undefined → false → legal: is an empty segment legal at each point, or already refused elsewhere?), a segment that is only `.` or `..` (true → refused: correct at B1? and at B2 where `.` was a valid reports_dir?), a fold that changes the first code point (NFC composition of a leading combining sequence; `toLowerCase` of a first character that lowercases to a multi-code-point string), B2's `coerceScalar` quoting/trim rules, B3's `pick` trim — for each point, is the reference applied to exactly the string the production predicate sees? (b) the generator — can the distribution be gamed: is the seed derivation such that an implementer can pick a seed (WD_HELD_OUT_SEED) that avoids a defect, and does the spec require an UNSET seed on the PR run; is the first-character coverage precondition sufficient to catch a degenerate generator; can a mutant that agrees with the reference on all 112 code points but differs on one outside the alphabet exist (a code point the alphabet does not contain — is that the honestly stated residual)? (c) Table E — for each row, is the expected verdict right per point under the code on this tree; any row a CORRECT implementation would fail? (d) Table C / the six identities under the new grading — read scripts/red-proofs.js evaluateRed/rollUp; under C1 exactly {T1,T2,T3}, under C2 exactly {T4,T5}, T6 green, band-marker signals matchable, one rollUp line for criterion 1 naming both ids? (e) V2's shell block — the spec says the block's guard now rejects `$`, backtick and `"` inside the JS; extract and run it; does it run as written? (f) the pruned Implementation notes — was any contract deleted by the pruning (compare against the round-1 tip a3985def: `git show a3985def:docs/specs/WP-dot-segment-denial.md`)? (g) Deliverables, ADR-0004, size M, one atomic WP — still honest at 1076 lines?
OUT OF SCOPE — file as scope objections in a separate routed section, do NOT count toward the verdict: the Dispatch-precondition item (silent fallback), the 2026-08-05 ruling's class shape, the basename list, NFKC normalisation, the spec-authoring boundary objection (adjudicated in round 2: dropped as a category error, restated machinery pruned), the adjudicated round-1/2 dispositions, any code change, and every file other than the two in scope.
REPORT: state what you EXECUTED (each command with exit status), then findings with file:line and a materiality band (A: a criterion a wrong implementation passes, silent wrong behaviour, or an over-denial encoded into an oracle; B: caught downstream; C: hygiene), then the verdict. Review is READ-ONLY: git status --porcelain must be identical before and after.
