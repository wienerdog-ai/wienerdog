Adversarial DESIGN review, round 7 (confirming), of docs/specs/WP-scheduler-replay-manifest-independent.md (Draft, M). Rounds 1-6 raws and dispositions in docs/specs/logbook/2026-09-18-scheduler-replay-manifest-independent-design-r{1..6}-astra-raw.json and -design-review.md. Round 6: candidate resolution still swallowed errors via withinSchedulerRoot/contains. Fix: D15 is now ONE rule over every path discovery resolves — each root, each candidate, D12's vault path: ENOENT/ENOTDIR = absent; any other code = unreadable → D9 abort before mutation; only a successful resolution may classify external/contained; discovery calls neither withinSchedulerRoot nor contains (its own error-surfacing resolver), both helpers byte-unchanged for their existing callers; stated exception: the act-time re-check D6 inside reverse() runs after D9's abort has passed, so a resolution failure there drops the path (failing can only preserve). AC 17 has four outcomes plus the unresolvable-vault case; S12 covers any path in discovery; the convergence note freezes the resolution semantics.

Confirming round, final: execute read-only against the tree, never the real scheduler or ~/.wienerdog. (1) Trace the full discovery pipeline as specified end-to-end for one root: resolve root (D15) → readdirSync (D9) → per entry: basename regex (R2) → lstat regular file → resolve candidate (D15) → contained-in-root and not-in-vault (D14/D12) → emit; find any step that can still throw, swallow, or misclassify — including readdirSync returning an entry that vanishes before lstat (ENOENT on lstat: absent, fine?), a candidate that is a hardlink, a case-insensitive filesystem making two names resolve equal (macOS default APFS is case-insensitive — can `ai.wienerdog.Dream.plist` and `ai.wienerdog.dream.plist` both exist? does R2's charset exclude uppercase, and does the generator ever emit uppercase?); (2) the D6 exception: at act time after D9 passed, a candidate that now fails to resolve is dropped — but its UNLOAD was attempted in D5a before or after that drop? if D5a acts on the discovered list and D6 re-checks only for removal, a job unloaded whose file then fails to resolve is unloaded-not-removed: is that the stated outcome and disclosed? (3) any remaining inconsistency across the now-18 ACs, D1–D15, R1–R8, S1–S12, the RED table, the Mirrored Surface Checklist, Security checklist; owner items 1–2 unchanged, standing form, no approval claims; cites at both ends. If nothing about the product is wrong, say approve.
