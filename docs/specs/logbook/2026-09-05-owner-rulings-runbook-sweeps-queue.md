---
date: 2026-09-05
title: "Rulings: the runbook-sweeps queue and the evening session's merge authorization"
related_wps: [WP-process-runbook-sweeps]
---

# Rulings: the runbook-sweeps queue (2026-09-05, evening session)

**Two instructions from the owner govern this session.** Both are quoted
verbatim so this record stands on its own.

At the session's resume, after a context clear:

```text
I have just cleared context. Please read the handoff document and resume your
work. Remember to follow the playbook of the project, including the double
gated reviews and the subagent delegations.
```

And, after the orchestrator reported that merge authorization had not been
restated for this session:

```text
I hereby authorize you to perform merges in this session too.
```

**What the first instruction carries.** "Resume your work" continues the queue
the handover names (`docs/HANDOVER.md`, status pass #5: `WP-process-runbook-sweeps`
first) under the process the 2026-09-05 rulings record settled and the handover
restates: *the maturing architect records a recommendation with the cost of
overruling it, and the session may dispatch under that recommendation, the owner
reversing any of them by dated amendment.* The wording around each item below is
the orchestrator's; the process decision is not, and none of the items is a
direct ruling — a direct ruling would be recorded as such, apart from these.

**What the second instruction carries.** Merges are authorized for this session
only; the next session needs it restated, as `docs/HANDOVER.md`'s status passes
have recorded for every session since 2026-09-02.

## The four owner items of `WP-process-runbook-sweeps`

The spec's "Dispatch precondition — owner items" carries four placement/scope
calls. Each is dispatched under its recommendation; the owner may reverse any by
dated amendment at the stated cost.

1. **O1 — R14 ("enumerate your own good") lands in `.claude/agents/wd-architect.md`.**
   Recommendation adopted. Overrule cost as the spec states it: one Deliverables
   row swapped and one sentinel path changed.
2. **O2 — R15 ("judge the whole cell, never the grep window") lands in
   `.claude/agents/wd-reviewer.md`.** Recommendation adopted. Overrule cost: one
   Deliverables row and one sentinel path.
3. **O3 — no retroactive provenance lines on already-bound text.** Recommendation
   adopted. Overrule cost: roughly six lines across `codex-review.md` plus one
   sentinel each.
4. **O4 — R19 ("declined owner grants surface loudly") is not landed.**
   Recommendation adopted: a mechanical sweep of both source documents returned
   zero hits, so the rule has no paid-for-by provenance. Overrule cost: the owner
   supplies the incident and the WP reopens with one Table B row, one sentence
   and one sentinel.

## Round-zero executor findings

The seven coherence findings and the one template note were dispositioned by the
orchestrator under the same standing process (all LIGHT: citations, mirrors, one
criterion without a runnable form) and are recorded, with what changed, in
`2026-09-05-process-runbook-sweeps-design-gate-rounds.md`.
