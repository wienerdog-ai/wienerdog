Adversarial DESIGN review, round 17 (confirming, final), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-16 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..16}-astra-raw.json and -design-review.md. Round 16: W10 covered only the first sweep; X17/X19 contradicted for an absent-shelf symlinked state. Fixes: W10's check now sits immediately before uninstall.js:424 and consults every sweep's preservedQuarantine plus a fresh quarantineInventory read; the :467 call is not moved (its job is removing the now-empty core, which needs manifest and config gone at :463-466); the window is narrowed, and the two remainders are NAMED in Table W row W11 — R-post-ledger-preserve and R-post-uninstall-preserve — leftovers not losses (one rm -rf; both need a dream already running though scheduler entries were reversed earlier); W8 names the preserved directory literally; AC 9 after-the-first-sweep arm; RED quse-w10-checks-first-sweep-only. X17 (1a) distinguishes HYPOTHETICAL anchors (absent shelf; class (ii) only; never trigger X19) from EXISTING chains; X19 retains a state alias only when an existing shelf's chain passes through it; a shelf created after the unlink is still protected because validate.js:953's recursive mkdir recreates the path and the live disposer holds no snapshot across calls; AC 14 asserts completion on that layout; twenty-one declarations over thirteen criteria.

Final confirming round. Apply X21 and W11 first: findings inside R-alias-outside-closure, R-post-ledger-preserve or R-post-uninstall-preserve are CLOSED — do not report them. Then, read-only (temp dirs): (1) the claim "a shelf created after the unlink is still protected": with <state> unlinked, validate.js:953 mkdir -p recreates <core>/state/quarantine as a REAL directory (not the alias) — so the copy lands inside the core, the next sweep (:467) finds it, preserves it, W10's pre-:424 check has already passed → this is R-post-ledger-preserve (named) — confirm the spec says exactly that and does not also claim it is protected as if by the alias; (2) W11's two residuals: "both require a dream already running" — the dream lock and the scheduler reversal: is a dream started BEFORE uninstall's scheduler reversal the only way, and can `wienerdog dream` be invoked by hand during uninstall (yes; same-user) — stated; (3) twenty-one declarations: anchors unique, no cross-reddening; (4) whole-document consistency: Tables K/X/V/W/Y/B, ACs 1–19, both mirror classes, Platform scope, owner items 1–4 (standing form, no approval claims), cites at both ends; size verdict (M vs split). If nothing about the product is wrong outside the three named residuals, say approve.
