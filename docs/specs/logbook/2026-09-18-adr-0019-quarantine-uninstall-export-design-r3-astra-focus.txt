Adversarial DESIGN review, round 3 (confirming), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-2 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1,2}-astra-raw.json and -design-review.md. Round 2 found the child-by-child sweep followed a symlinked state directory. Fix: Table X row X10 — every classification is lstatSync-based (statSync/isDir at manifest.js:175-181 never decides enumeration or deletion); X1 step 0: a symlinked paths.state is unlinkSync'd and never descended (reproduces 5b77865f where rmSync(state) removed only the link; same treatment disposeCoreMechanics gives a symlinked core at :1164-1165); X1 step 3: a shelf root that lstat reports as anything but a directory is preserved, never followed/removed/climbed past (consistent with K2); Y9; AC 14 (external dir's files byte-unchanged, link removed, core emptied, shelf-root arms); RED quse-symlinked-state-descended anchored on `if (!isDir(dir)) continue;`.

Confirming round. Execute with mocked I/O (temp dirs only; never ~/.wienerdog): (1) X1 step 0 "unlink the symlink at state, never descend" — with `state` unlinked, steps 1–4 have nothing to enumerate: confirm the algorithm terminates cleanly and reports (was the shelf inside the external target? it was never inspected — so a user whose state/ was a symlink to a directory that DOES contain quarantine/ files loses nothing but also gets no refusal — is that stated as the outcome, and is it consistent with W1 (the plan phase's K2 inventory: does the inventory also refuse to follow a symlinked state, so plan and act agree)?); (2) lstat everywhere: enumerate every path the package classifies (state, each direct child, quarantine, redacted, the entries K2 counts) and confirm each is lstat'd — a directory child of state that is a symlink to elsewhere: step 2's rmSync recursive unlinks the link (Node does not follow) — stated; (3) TOCTOU between the lstat and the rmdirSync/rmSync on the same path (a race that swaps a dir for a symlink) — is it acknowledged as a residual bounded by the user's own account (same-user, 0700), or ignored; (4) the RED anchor `if (!isDir(dir)) continue;` — unique; and does the mutation (using the statSync-based gate) actually restore the round-2 defect under the new algorithm; (5) final consistency: Tables K/X/W/Y/B, ACs 1–15, checklist, Security, owner items 1–3 unchanged in the standing form, no approval claims, cites at both ends. If nothing about the product is wrong, say approve.
