Adversarial DESIGN review, round 10 (confirming), of docs/specs/WP-scheduler-replay-manifest-independent.md (Draft, M). Rounds 1-9 raws and dispositions in docs/specs/logbook/2026-09-18-scheduler-replay-manifest-independent-design-r{1..9}-astra-raw.json and -design-review.md. Round 9: a disclosed file vanishing before the unload dropped the unload while the registration persisted. Fix: phase D5a is filesystem-free — input is the disclosed list, argv re-derived from path.basename by deriveUnloadArgv (zero fs uses), every disclosed item unloaded unconditionally; D5b owns the act-time re-check and governs removal only (absence = nothing to remove; any other failure = skip with a notice; a preserved plist prints R9's warning); D15 has exactly one abort site left — D9 in discovery; consent: unload acted set == disclosed set, removal acted set ⊆ disclosed; AC 18; S13; convergence note: the unload phase is filesystem-free.

Confirming round, final. Read-only against the tree; never the real scheduler or ~/.wienerdog. (1) With D5a filesystem-free, enumerate what can now be unloaded that should not: a disclosed candidate whose basename was recognized at discovery but which the user replaced during the prompt with a DIFFERENT job of the same name — the unload argv targets the label/name, so the user's replacement job gets unloaded: is that acceptable (it is our namespace; disclosed by name) and stated? (2) D5b's "any other failure = skip with a notice": confirm this is the removal phase only and cannot leave a live registration (the unload already happened in D5a) — so R-failed-unload-style outcomes are bounded to "file left behind, job unloaded"; and for a launchd plist left behind → R9's warning. (3) D9 as the sole abort site: is the dry-run/plan path guaranteed to run D1+D9 identically to the live path so the disclosed list the user consents to is the one D5a acts on (same process? recomputed? if recomputed, can it differ)? (4) The chokepoint count assertions in ACs 14/18: with double unload for recorded entries, is the expected count per fixture stated exactly (D5a once + recorded reverser once = 2 per recorded scheduler-entry; 1 per unrecorded)? (5) Final consistency: D1–D15, R1–R9, S1–S13, ACs 1–19, RED table (which declarations remain and are their anchors still unique at c05a575b), Mirrored Surface Checklist, Security checklist, owner items 1–3 standing form, no approval claims, cites at both ends. If nothing about the product is wrong, say approve.
