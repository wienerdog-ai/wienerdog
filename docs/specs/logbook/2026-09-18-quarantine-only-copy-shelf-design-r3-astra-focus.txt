Adversarial DESIGN review, round 3, of docs/specs/WP-quarantine-only-copy-shelf.md (Draft, S). Rounds 1-2 raws and dispositions in docs/specs/logbook/2026-09-18-quarantine-only-copy-shelf-design-r{1,2}-astra-raw.json and -design-review.md. Round 2 found class C over-broad. Fix: C is split — C1 = identical === true at validate.js:1498 AND the best-effort rmSync at :1500 threw (proven byte-identical twin on the unbounded withheld shelf; true duplicate; NOT on the preservation record since the else never ran); C2 = identical === false with preserved non-null (comparison read threw, or buffers differ; kept AND recorded at :1502-1503; the shelf copy may be the only copy of that version; treated exactly as A and B). The spec now names the round-1 inversion explicitly (the gate DELETES proven-identical copies at :1499 and KEEPS unproven ones at :1502, so surviving fall-through files are overwhelmingly potential only-copies — the code's own comment at :1478-1481). O2: the prune destroys a sole-surviving version on A, B and C2; only C1 is lossless. O5: a floor would be A+B+C2 ≈ never prune, needing a never-persisted "was identity ever proven" boolean. O8 / owner item 1: what is signed is bounded loss with a rare housekeeping case. New fixture-level tests [OC-2] (buffers differ) and [OC-3] (comparison read throws) drive the fall-through with the withheld preserve succeeding via the existing patchFs, asserting the shelf copy survives, is recorded with location 'quarantine/redacted', and coexists with a twin whose equivalence was never proven; neither asserts the prune ([OC-1] does). Deliverables: three tests in one file, one RED declaration, two doc clauses, no src/.

Execute against the code with mocked I/O (temp state dir only): (1) is the A/B/C1/C2 partition now exhaustive over every exit of :1416-:1504 — in particular: identical === true and the rmSync SUCCEEDS (the file is gone — not on the shelf, fine); preserved === null (the withheld preserve itself failed — what happens to the redacted copy then, and which class); the comparison read throwing for the REDACTED copy vs for the withheld copy; (2) the claim "C1 is not on the preservation record" — verify against :1489-1504 and say whether that makes C1 invisible to reports/warnings.md and the dream report, and whether the doc clause's "may be the only copy" is honest for a reader who cannot tell C1 from C2; (3) [OC-2]/[OC-3]: are they non-vacuous — can the withheld preserve "succeed" via patchFs in a way that leaves the fixture in a state the real gate never produces, so the assertion pins a fake? does either accidentally become a second reddening test for [OC-1]'s mutation (the expectRed must stay one entry)? (4) size: three tests + one declaration + two doc clauses with no src/ — still S, and does the Definition of done hold; (5) owner items 1-3 in the standing form, no approval claims; cites at both ends; template sections intact; Table O intact. Report findings with file:line, band, LIGHT/HEAVY; verdict: approve / request-changes.
