Adversarial DESIGN review, round 8 (confirming), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-7 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..7}-astra-raw.json and -design-review.md. Round 7: dry-run could not plan without the syscalls the live deleter forbids. Fix: Table X row X15 — dryRun: true is a READ-ONLY PLANNER running the same quarantineInventory (X12 fold, X11 three-level validation), no mutating fs call, `removed` = predicted removals at X4's granularity; the no-emptiness-test rule exempted for the planner with its reason; live arm untouched and snapshot-free; X2/K5/K7/X4/W5 re-scoped to "live arm"; named residual R-dry-run-prediction-drift with direction (live can only preserve more, never delete unnamed); AC 17 (three shelf states on both surfaces, zero mutating fs calls counted through the seam); RED quse-dry-run-plans-by-assuming; eleven declarations over ten criteria.

Confirming round, final. Execute with mocked I/O (temp dirs): (1) X15's planner and the pre-confirm plan: is the interactive (non-dry-run) pre-confirm plan ALSO produced by the read-only planner (uninstall.js:353-354 builds it with dryRun:true today — confirm) so that the user consents to a prediction, then the live arm acts snapshot-free — and is the one place where they can diverge (a shelf filled during the prompt) reported by the live refusal in the same vocabulary as the plan; (2) the R-dry-run-prediction-drift direction claim: enumerate the live arm's outcomes vs the predicted ones — is there ANY case where live removes something the plan did not name (a shelf empty at plan time and still empty at act: removed as predicted; a shelf that becomes empty during the prompt: plan said preserve, live removes it — that IS deleting something the plan did not name, though it is an empty directory; is that acknowledged or does the direction claim need "no shelf FILE" qualification)?; (3) the eleven declarations still correspond; (4) final consistency across Tables K/X/W/Y/B, ACs 1–18, both mirror classes, Platform scope, owner items 1–3 (standing form, no approval claims), cites at both ends. If nothing about the product is wrong, say approve.
