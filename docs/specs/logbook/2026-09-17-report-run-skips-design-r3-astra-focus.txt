DESIGN REVIEW (confirming round, fresh reviewer) of a re-derived work-package spec; no product code has been written yet. Review ONLY docs/specs/WP-dream-report-run-skips.md and the 2026-09-17 report-run-skips logbook entries under docs/specs/logbook/. Exclude docs/marketing/, memory/research/ and userreports/ entirely. Two earlier rounds by a different model are preserved as docs/specs/logbook/2026-09-17-report-run-skips-design-r1-raw.json and -r2-raw.json with dispositions in the design-review entry; do not assume they were right. The spec makes the nightly dream's vault report state, in plain language for a non-developer, how many sessions a run could not consolidate, as six integer counts composed inside src/core/dream/promote.js composeRecord via a new optional promote({runSkips}) argument. The collector (src/core/dream/scratch.js collectExtracts) has five disjoint exclusion arms: quarantine, read-deferred, oversized (memoized, not retried until the file, the app version or the limit changes), capacity-deferred, deadline-deferred. Challenge: (1) is every byte-exact sentence the report will print TRUE against the real collector and ledger code; (2) is the coverage boundary stated truthfully - only a real, non-dry run that admits at least one session and reaches promote() writes a report; zero-admission runs throw or return idle; (3) can a count be lost, double-counted, or fed by untrusted or non-integer input that reaches rendered markdown; (4) the pointer to reports/warnings.md renders only when a quarantine count is non-zero, and the spec names as a residual that the file may be stale when the refresh failed - is that residual stated truthfully; (5) the code-owned heading follows the same (absent) ownership rule as the three shipped code-owned headings - verify that claim in src/core/dream/promote.js; (6) can each acceptance criterion and RED-proof declaration discriminate a correct implementation from a plausible wrong one; (7) do the file:line citations resolve on this tree. Report findings about PRODUCT behaviour a user would observe in the main list; list findings about the spec's own verification machinery separately, marked as machinery. A risk the spec already names and prices as a residual or owner item is not a finding unless its statement is untrue. Owner items are recommendations with overrule costs by design. State what you executed versus what you only read.
