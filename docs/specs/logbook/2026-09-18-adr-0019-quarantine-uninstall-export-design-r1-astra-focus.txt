Adversarial DESIGN review of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M, pinned 5b77865f), not of code. Context: `wienerdog uninstall` disposes the core via disposeCoreMechanics (src/core/manifest.js:1133-1148, recursive rmSync of paths.state with no carve-out), which today destroys state/quarantine/ and state/quarantine/redacted/ — the two shelves holding UNREDACTED user text preserved by the secret gate (src/core/dream/validate.js:1439 withheld, :1416 redacted); ADR-0019's invariant ("uninstall disposes core mechanics", :52-54) has been false in spirit since those shelves exist and nothing recorded it. The package's decisions: owner item 1 — AMEND ADR-0019 (carve-out for state/quarantine/**), amendment drafted with the standing-authorization Status line, opening with an erratum; re-obtainability not sensitivity is the criterion, so secrets/, logs/, schedules/ stay disposable; owner item 2 (Table W W1/W2) — REFUSE-AND-REPORT: when a shelf is non-empty, uninstall refuses, prints the count/bytes and the literal rm -rf the user can run, writes NO copy anywhere, and `--yes` does not proceed (a refusal is not a prompt; uninstall.js:349-351); a fixed HOME location and a user-chosen path are weighed as overrule paths with Table Y row Y3 the binding export contract if overruled; named residual R-unclearable-shelf; owner item 3 — no --discard-quarantine flag; mechanism (Table X X5–X7) — an UNCONDITIONAL carve-out in disposeCoreMechanics, not a manifest "preserved kind" (both only narrow deletion under ADR-0038; the manifest route refused because it would make the dream gate a writer of install-manifest.json, put user-text survival behind an unauthenticated file, and fail silently); composition with WP-scheduler-replay-manifest-independent (Ready; same two files) in Table W row W7 (shelf gate before its D9; disjoint in manifest.js); depends_on WP-quarantine-only-copy-shelf (shared GLOSSARY/secret-incident sentences).

Reason over the code and execute with mocked I/O; never run uninstall outside a temp HOME, never touch ~/.wienerdog. Verify: (1) the refuse-and-report decision's consequences: an uninstall that cannot complete while a shelf is non-empty — is that acceptable for a knowledge-worker user (the shelf fills whenever the gate withholds/redacts; the only exit is a shell command printed by the tool); does it interact with the scheduler package's D9 abort and R9 warning (two abort sites now — order, and can one mask the other's disclosure); what does `wienerdog uninstall --dry-run` show; does adopt/re-install after a refused uninstall work; (2) the unconditional carve-out: trace disposeCoreMechanics and every other path that removes paths.state (uninstall, adopt reset, tests' temp roots, `wienerdog update`?) — does the carve-out leave state/quarantine/ inside a removed core dir (parent gone, child kept — is that even possible with recursive rmSync; the spec must say how the carve-out is implemented: skip-list inside the recursive walk, or move-aside), and what the residual directory tree looks like after uninstall (an orphan ~/.wienerdog/state/quarantine/ with 0700 perms — disclosed?); (3) the concurrent-dream window X3 the carve-out is said to earn — is that argument sound (uninstall while the dream runs — is that even permitted today; the dream lock?); (4) ADR-0038: both routes narrow deletion — confirm the ADR's rules N and D as cited; (5) the six RED declarations — anchors and mutations correspond to properties; the two on new code disclosed; (6) the Security checklist — the refusal message prints counts and a path, never contents; the shelves' 0600/0700 untouched; (7) composition row W7 — every scheduler row named exists and the ordering claim (shelf gate before D9) is consistent with that spec's D9 "abort before the plan is printed"; (8) cites at both ends, checklist, owner items in the standing form, no approval claims. Report findings with file:line, band A/B/C, LIGHT/HEAVY. End with a verdict: approve / request-changes.
