Adversarial DESIGN review, round 5 (confirming), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-4 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..4}-astra-raw.json and -design-review.md. Round 4: a case-insensitive filesystem let a `Quarantine` shelf be deleted by the byte-equal filter. Fix: Table X row X12 — shelf identity by ASCII case fold on every platform; step 2 excludes every child folding to `quarantine`, preserving-and-reporting a fold-equal-but-not-byte-equal child (which makes <state> non-empty so step 3's ENOTEMPTY stops the climb); K1 uses the same fold for the inventory; the byte-exact lowercase path stays the removal target; Y4 rewritten; the Security-checklist untrusted-identifier bullet corrected and a "Security-checklist bullets that restate a contract" mirror class registered; nine declarations over eight criteria.

Confirming round, final. Execute with mocked I/O (temp dirs): (1) X12 on a case-SENSITIVE volume (Linux): a user directory literally named `Quarantine` under state/ that is NOT our shelf is now preserved-and-reported rather than deleted — deletes strictly less, uninstall then refuses under ENOTEMPTY with a message naming it — is that outcome stated as the accepted cost and does the refusal text make sense to the user? (2) K1/K2 with two fold-equal entries present (`quarantine` and `Quarantine` both exist on Linux): which is the inventory root, is the other counted as a user entry, and is the act-phase consistent; (3) the same question for `redacted` under quarantine (does X12 fold that level too?); (4) unicode: is the "ASCII fold only, our names are pure ASCII" argument complete — could a preserve path ever be created with a non-ASCII variant (no: our code writes the literal), and is an attacker-created `quarantıne` (dotless i) irrelevant because it is not fold-equal and not our shelf — stated?; (5) the nine declarations: anchors unique where anchored; no declaration reddens another's sole test; (6) final consistency across Tables K/X/W/Y/B, ACs 1–16, the new mirror class, Platform scope (case-insensitivity per platform stated), owner items 1–3 (standing form, no approval claims), cites at both ends. If nothing about the product is wrong, say approve.
