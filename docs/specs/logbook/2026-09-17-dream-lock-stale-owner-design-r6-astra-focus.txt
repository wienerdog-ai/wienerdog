DESIGN REVIEW ROUND 6 of a work-package spec; no product code has been written yet. Review ONLY docs/specs/WP-dream-lock-stale-owner-loud.md and the 2026-09-17 dream-lock-stale-owner / dream-lock-silent-stall logbook entries under docs/specs/logbook/ (prior raws are beside them: -design-r1..r4-raw.json from another model and -design-r5-astra-raw.json). Exclude docs/marketing/, memory/research/ and userreports/ entirely. Background: src/core/dream/lock.js keeps an EXPIRED single-run dream lock when its recorded local PID still exists, and src/cli/dream.js exits 0 on that busy decline, so the supervisor records success; a lock left by an uncleanly-killed dream whose PID is later reused stops memory consolidation indefinitely with no alert. The spec does exactly three things: (a) a busy decline more than 6 hours past the lock's deadline throws (exit 1 - the existing alert and email path) with a byte-exact plain-language message; (b) a finite deadline more than an absolute 24 hours ahead of now is classified as the existing owner-unknown; (c) an exit-code doc comment is corrected. No takeover decision changes. PART ONE - verify the round-5 findings are GENUINELY fixed, executing the real scheduler/watchdog/lock functions where you can: (A-1) "Scheduled dreams can legitimately exceed the loud threshold" - the watchdog claim is now qualified to the default configuration, scheduled overruns are priced beside attended ones (the consequence is an alert, never a takeover), and the byte-exact message NO LONGER contains any deletion instruction or start-time heuristic: it says to restart, that Wienerdog normally clears the lock itself at its next run, and that if the message keeps appearing the lock is the named file, removing it is only safe while no dream is running, Wienerdog cannot tell the user that, so someone should check before it is deleted rather than deleting it on a guess. Is every sentence of that message TRUE, is it safe, and does any remnant of the old instruction survive in Table S, the owner items, the ADR amendment block or the worked timelines? (A-2) "[Machinery] The declared proof suite cannot exercise S4" - S4's executable coverage moved into tests/unit/dream-pipeline.test.js with its own declaration file, one file per suite; check against scripts/red-proofs.js that this can reach PROVEN. PART TWO - closure: report only findings about PRODUCT behaviour a user or consuming process would observe that the spec does not already name as a residual or owner item; list verification-machinery findings separately, marked as machinery. Do the file:line citations resolve on this tree. State what you executed versus what you only read.
