# Raw gate output — Codex plugin adversarial-review, round 3, docs/wp-instruction-basename-currency at f070c888 (base main). Committed BEFORE adjudication. Focus text reproduced at the end.

# Codex Adversarial Review

Target: branch diff against main
Verdict: needs-attention

NO-SHIP. The exact Round-2 attacks are closed: skeleton-less inventory, NOT-A-DATE cells, and the negated obligation all fail. However, new in-memory mutants prove V3 can certify inconsistent or impossible dates and malformed fenced output, while V4 can certify an obligation that exists only inside a code example. Deliverables, anchoring/Table D, ADR-0004, and size S otherwise remain coherent. The pre/post worktree status was identical: `?? node_modules`.

Findings:
- [high] [Band A] V3 certifies stale and impossible inventory dates (docs/specs/WP-instruction-basename-currency.md:631-644)
  V3 selects only the first non-`--write` argument, shape-checks it lexically, and performs substitution without validating the template's required date sites. The post-substitution placeholder check is tautological. Running the exact extracted script in memory returned rc=0 for: `2026-99-99`; two date arguments; zero placeholders with a static-date document; and a 2027 render whose first Fetched cell remained `2026-09-04`. The latter directly violates the ONE-DATE rule while V3 reports byte identity. A document retaining literal `@DATE@` did fail, so that narrower case is closed. The current block contains 11 occurrences—header, nine Fetched cells, and explanatory prose—none structurally validated.
  Recommendation: Before substitution, require exactly one date argument, validate it as a real calendar date via parse-and-round-trip, and assert the semantic template sites: one Current-as-of placeholder and one placeholder in every DENY row's Fetched cell. Then substitute and compare.
- [high] [Band A] V4 mistakes numbered text inside a code fence for a release step (docs/specs/WP-instruction-basename-currency.md:681-685)
  V4 filters raw lines with `/^[0-9]+\. /` but does not account for Markdown context. The exact script returned rc=0 when the runbook consisted solely of a fenced `text` example containing `7. <canonical sentence>`. There was no actionable runbook step, yet criterion 4 passed. Unnumbered, bulleted, indented-continuation, duplicate, and negated mutants correctly failed.
  Recommendation: Verify the exact insertion into the expected top-level numbered-list context and reject matches inside fenced blocks or comments. Stronger: compare the complete runbook against its base content plus the one permitted exact insertion, matching the whole-artifact circuit-breaker design.
- [medium] [Band A] V3 permits an internal fence that corrupts the generated inventory (docs/specs/WP-instruction-basename-currency.md:637-642)
  The wrapper check validates only the lines immediately after BEGIN and before END. An in-memory canonical block containing an additional standalone ``` line rendered and compared with rc=0. In the generated document that delimiter opens a code fence and turns the remaining inventory into malformed Markdown. Duplicate sentinels, a missing outer fence, and CRLF sentinels correctly failed, but nested/early fence closure remains silently accepted.
  Recommendation: Reject any standalone fence delimiter within the extracted payload, or use a representation whose delimiter cannot occur in the rendered content and validate that invariant before `--write`.
- [low] [Band C] The reported byte counts are UTF-16 code-unit counts (docs/specs/logbook/2026-09-04-instruction-basename-currency-design-gate-rounds.md:512-515)
  Both checks use JavaScript string `.length` while describing the result as bytes. Measured values are 525 UTF-8 bytes for the claimed 523-byte release sentence and 13,855 bytes for the claimed 13,779-byte inventory. String equality still catches the exercised valid-UTF-8 mutations, but the byte-exact evidence and logbook measurements are factually inaccurate.
  Recommendation: Compare Buffers or explicitly encode the rendering, and report `Buffer.byteLength(value, 'utf8')`; update the recorded byte counts.

Next steps:
- Executed `git status --porcelain=v1 && git diff --unified=80 main -- <two scoped files>`: exit 0; initial status `?? node_modules`.
- Executed the fenced V3 via `awk '/^# V3 —/{p=1} /^# V4 —/{p=0} p' ... | zsh`: exit 1, expected missing inventory.
- Executed the fenced V4 via `awk '/^# V4 —/{p=1} /^# V5 —/{p=0} p' ... | zsh`: exit 1, expected zero canonical runbook lines.
- Executed scoped `rg`, `nl`, and `sed` inspections: exit 0; the runbook-content `rg` returned exit 1 because none of the obligation text exists yet.
- A heredoc mutant harness returned exit 1 because the sandbox denied its temporary file. The replacement no-write VM harness initially returned exit 1 due to selecting the commented example; the corrected exact-script in-memory harness returned exit 0 and produced the mutant results cited above.
- Executed the exact R2-A in-memory mutants: harness exit 0; skeleton-less document rc=1 and all-NOT-A-DATE document rc=1. The R2-B negated-step mutant also returned rc=1.
- Executed `git diff --check`, changed-file inspection, placeholder count, and byte measurement: exit 0.
- Executed final `git status --porcelain=v1`: exit 0, still exactly `?? node_modules`; review made no filesystem changes.
- V3 `--write` behavior was tested against virtual in-memory files: it silently overwrites an existing different generated inventory and exits 0. This is consistent with the file's generated-only contract, so it is not counted as a finding.
- Scope objections excluded from the verdict: none.

---- focus text ----
SCOPE: docs/specs/WP-instruction-basename-currency.md (Draft, 737 lines) and docs/specs/logbook/2026-09-04-instruction-basename-currency-design-gate-rounds.md, as changed relative to main. ROUND 3 — the closing confirmation after a circuit-breaker redesign. The WP: bring the instruction-file basename deny-list in src/core/dream/promote.js (INSTRUCTION_BASENAMES, four names) current; ship docs/instruction-file-inventory.md as a byte-exact RENDERING of one sentinel-delimited canonical block in the spec (single @DATE@ placeholder; produced by V3's script with --write, verified by the same script without it); a fixed 523-byte release-runbook step byte-matched by V4; tests with a hand-written nine-name literal (criterion 7) and a red-proofs declaration targeting it (Table D). Owner rulings already given: the 2026-08-05 ruling keeps this a NAME LIST with a named residual; dot-prefixed paths are a CLASS rule owned by WP-dot-segment-denial.
ROUND 2 FINDINGS (verify each is GENUINELY closed): (R2-A, converged) V3 compared only table regions, so a skeleton-less/undated inventory and NOT-A-DATE cells passed. (R2-B, converged) V4 was five substrings, so a step negating the obligation passed. The redesign deleted both mechanisms in favour of whole-artifact byte compares.
CHALLENGE THE FIXED POINT — every item below is about whether a WRONG implementation can pass: (a) the sentinel extraction — the canonical block sits between <!-- BEGIN-CANONICAL-INVENTORY --> / <!-- END-CANONICAL-INVENTORY --> inside a markdown fence; what does V3's script do if a sentinel appears twice, if the fence is missing or nested, if the block contains a line that looks like a fence close, if the spec has CRLF? Does a mis-extraction fail RED or GREEN? (b) the @DATE@ substitution — where must the placeholder appear, is it validated as appearing at all, can a document with the placeholder literally unreplaced pass, is the date validation (^\d{4}-\d{2}-\d{2}$ and >= 2026-09-04) sufficient (2026-99-99?), and does --write refuse to overwrite an existing different file or silently clobber? (c) the ONE-DATE rule — every Fetched cell = Current as of; is there any row whose citation was fetched on a different date today, and does the rule survive a future partial re-fetch honestly (a row that cannot be confirmed must change the block — is that stated as an obligation somewhere the maintainer reads)? (d) V4's exact-line match — the 523-byte sentence: is the numbered-line shape ("N. " prefix?) pinned so that an unnumbered or bulleted copy fails, and can the sentence appear inside another step's continuation lines and pass? Does the runbook's existing text already contain any of it? (e) the anchoring chain — criterion 7's literal → shipped document DENY basenames (half a) → rendering byte-equal to the spec block → spec text the implementer may touch only to flip status. Is every link of that chain actually verified by a runnable step, and where does Table D's proof sit in it? Does reverting INSTRUCTION_BASENAMES to four names on the CORRECT finished tree produce an own-body ERR_ASSERTION of criterion 7's test and PROVEN, and does a shrunken tree fail at BASELINE? (f) Deliverables — does the redesign require touching any file not listed (e.g. does the implementer need to run V3 --write, which writes docs/instruction-file-inventory.md, a listed deliverable — fine — but anything else)? (g) Table A/B/C being MOVED into the block — did any spec-internal reference to "Table A/B/C" or their rows dangle after the move (grep them)? Does the Mirrored Surface Checklist still register every mirror that exists and none that no longer does? (h) ADR-0004 and size S still honest after the redesign?
OUT OF SCOPE — file as scope objections in a separate routed section, do NOT count toward the verdict: the two owner items in ## Dispatch precondition (denying replit.md and AGENT.md; the obligation's owner being a role), the 2026-08-05 list-with-residual ruling, the dot-segment class, the adjudicated round-1/round-2 dispositions, any code change (nothing in src/ or tests/ changes on this branch), and every file other than the two in scope.
REPORT: state what you EXECUTED (each command with exit status) — extract V3 and V4 from the fenced blocks and run them as written on this untouched tree, and in memory against mutants where the sandbox denies temp writes — then findings with file:line and a materiality band (A: a criterion a wrong implementation passes, or silent wrong behaviour; B: caught downstream; C: hygiene), then the verdict. Review is READ-ONLY: git status --porcelain must be identical before and after.
