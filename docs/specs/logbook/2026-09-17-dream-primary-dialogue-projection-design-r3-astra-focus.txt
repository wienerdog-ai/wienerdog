DESIGN REVIEW ROUND 3 of a NEW work-package spec; no product code has been written yet. Review ONLY docs/specs/WP-dream-primary-dialogue-projection.md and docs/specs/logbook/2026-09-17-dream-primary-dialogue-split.md (your round-1 and round-2 raws are preserved beside it as …-projection-design-r1-astra-raw.json and -r2-astra-raw.json). Sibling specs in the same diff are context only. Exclude docs/marketing/, memory/research/ and userreports/ entirely. The spec adds an opt-in, deterministic, code-only projection parsePrimaryWithOutcome returning {extract, gateExtract, intakeBytes, parse} from the Claude Code and Codex CLI transcript parsers (src/core/transcripts/). derived_from_untrusted is computed over the ORIGINAL stream before projection: verified user text is false by role (today's rule, with the residual priced as an owner item); assistant text is governed by a monotonic, never-resetting taint state that becomes true at the first tool output OR the first GAP. PART ONE - verify the round-2 findings are GENUINELY fixed, EXECUTING the real parsers and streaming reader on synthetic inputs: (P2-1) "Valid JSON without type discriminators escapes the gap rule" - row A5a now distinguishes CLASSIFIED-and-declined records (a top-level type that is a non-empty string which the raw policy declines; isMeta; an empty assistant join; a Codex payload.type that is a non-empty string AND is in the list this repository has decided about - message, the five TOOL_OUTPUT_TYPES, and reasoning, custom_tool_call, function_call, agent_message; a content-block type that is a non-empty string which the acceptance rules decline, e.g. image, thinking, input_image) from UNCLASSIFIABLE records, which taint: a discriminator that is absent, not a string, or empty at the record, payload or content-block level; a Codex payload that is absent or not a plain object; and a Codex payload.type OUTSIDE the decided list. The spec states an ASYMMETRY and its reason: Codex signals tool output at payload.type so an unfamiliar Codex payload type taints, whereas an unfamiliar Claude top-level type does NOT taint because Claude carries tool output as a tool_result block inside a user record and tainting on unfamiliar Claude top-level types would taint nearly every session (it cites an observed `attachment` type at 3,992 of 16,218 records); the Codex behaviour is put to an owner item and no diagnostic is added. Attack this: can a Claude record of an unfamiliar top-level type carry tool output or external content that then reaches an assistant conclusion marked false; can a Claude tool_result block appear anywhere other than inside a user record; is the decided Codex list complete against src/core/transcripts/codex.js; does any of the three levels still let a structurally damaged record vanish without tainting? (P2-m1) the mirror sweep - is ANY surface still promising an unchanged or identical admitted set unconditionally, or defining false for USER records as requiring no preceding tool output or gap? (P2-m2) AC4a now asserts exactly one streamLines invocation / one transcript open in addition to budget-debit equality, with a deliberate second-read mutation that must redden. PART TWO - closure: report only findings about PRODUCT or SECURITY behaviour that the spec does not already name as a residual or owner item; list verification-machinery findings separately, marked as machinery. Do the file:line citations resolve on this tree. State what you executed versus what you only read.
