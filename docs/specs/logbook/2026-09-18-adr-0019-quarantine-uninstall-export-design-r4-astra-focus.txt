Adversarial DESIGN review, round 4 (confirming), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-3 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1,2,3}-astra-raw.json and -design-review.md. Round 3: the bottom-up sweep touched external/redacted through a symlinked quarantine. Fix: Table X row X11 — validation runs TOP-DOWN (lstat <state> → quarantine → redacted, stopping at the first level that is not a real directory: that level and below not accessed/lstat'ed/removed), removal runs BOTTOM-UP over the validated prefix only; AC 14 extended with a direct-sweep regression asserted via existsSync/lstatSync (not bytes); second RED quse-shelf-validated-leaf-first; eight declarations over seven criteria.

Confirming round, final. Execute with mocked I/O (temp dirs only): (1) X11 end-to-end on the seven shapes: all real dirs; state symlink; quarantine symlink with external redacted; redacted symlink; redacted a regular file; quarantine missing but redacted somehow present (impossible — say so); a shelf root replaced between validation and rmdir (TOCTOU — acknowledged as a same-user residual?); confirm for each which paths are touched and that nothing outside <state> is ever removed; (2) the plan-phase inventory (K2) and the act-phase sweep (X1/X11) classify the three levels identically, so plan and refusal agree except for what changed during the prompt (stated?); (3) AC 14's assertions are non-vacuous for the empty-external-dir case; (4) the eight declarations: anchors unique where anchored, mutations correspond, no two declarations redden each other's sole test; (5) final consistency across Tables K/X/W/Y/B, ACs 1–15, checklist, Security, Platform scope, owner items 1–3 (standing form, no approval claims), cites at both ends. If nothing about the product is wrong, say approve.
