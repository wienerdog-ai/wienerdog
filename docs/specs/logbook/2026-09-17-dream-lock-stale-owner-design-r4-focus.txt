DESIGN REVIEW ROUND 4 (closure round) of a work-package spec; no product code has been written yet. Review ONLY: docs/specs/WP-dream-lock-stale-owner-loud.md and docs/specs/logbook/2026-09-17-dream-lock-stale-owner-design-review.md (prior raws for reference: the -design-r1/-r2/-r3-raw.json files beside it). Exclude docs/marketing/, memory/research/ and userreports/ entirely. Background: src/core/dream/lock.js keeps an EXPIRED single-run dream lock when its recorded local PID still exists, and src/cli/dream.js exits 0 on that busy decline, so a lock left by an uncleanly-killed dream whose PID is later reused stops memory consolidation forever with no alert. The spec does exactly three things: a busy decline more than 6 h past the lock's deadline throws (exit 1, existing alert path) with a byte-exact plain-language message; a finite deadline more than an absolute 24 h ahead of now is classified as the existing owner-unknown; the exit-code doc comment is corrected. No takeover decision changes. PART ONE - verify each round-3 finding is GENUINELY fixed in substance: (R3-A1) "A repeated message does not prove this is still the dead owner's lock" - the identity claim is withdrawn everywhere, and the message now requires the user to first make sure no dream is running at that moment (none they started in a terminal, none Wienerdog started on its own in the last half hour) before deleting the file; the residual is restated as resting on that human check and priced in an owner item, with an attended conditional-recovery command routed onward, not designed here. Is any remnant of the identity claim left, and is the residual stated truthfully? (R3-B1) "Success watermarking stretches the worst-case silence to 54 hours and three lost runs" - every ~30 h / two-nights statement is replaced by the sampling-bound maximum of 54 h / three scheduled runs, separated from the realistic one-night case, with the 21:30 / exactly-24 h / exactly-6 h walk as a worked example; the comparison stays strict because >= moves the maximum by one minute only. Check the arithmetic and check no stale bound survives in Table S, the owner items, the timeline or the ADR amendment block. PART TWO - closure: report ONLY findings about PRODUCT behaviour a user or consuming process would observe that the spec does not already name as a residual or an owner item. A risk the spec already names and prices is not a finding unless the statement of it is untrue. List verification-machinery findings separately, marked as machinery. Do all file:line citations resolve on this tree. Report what you executed versus what you only read.
