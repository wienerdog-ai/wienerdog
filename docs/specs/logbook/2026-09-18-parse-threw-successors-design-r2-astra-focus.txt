Adversarial DESIGN review, round 2, of three work-package specs (Draft): docs/specs/WP-doctor-recognizes-parse-threw.md (S), docs/specs/WP-transcript-parsers-harden-text-values.md (M), and the NEW companion docs/specs/WP-ledger-retry-parse-threw-on-upgrade.md (M, depends on the transcripts one). Round-1 raw and dispositions: docs/specs/logbook/2026-09-18-parse-threw-successors-design-r1-astra-raw.json and 2026-09-18-parse-threw-successors-design-review.md. Round 1's finding: no recovery for sessions quarantined `parse-threw` (a reason shipped in v0.14.0 with unhardened parsers) once the parsers are hardened — ledger.selectState() skips unchanged quarantined files forever. The fix: a companion package whose mechanism is to DELETE the `parse-threw` quarantine records once per app-version upgrade so selectState's existing no-record path re-selects them (no skip rule changes, no record field), with a one-shot gate and a dry-run guard modelled on migrateFromWatermarks at src/cli/dream.js:709-711, and the transcripts spec gaining Table E / AC9 / owner item 2 (release ordering).

Focus, executing against the code with mocked I/O where useful: (1) the companion: where is the "once per upgrade" state persisted, and is it really persisted — the spec declares a RED proof `rpt-gate-not-persisted` for an in-memory-only gate; trace readLedger (`src/core/dream/ledger.js:139-157`, hard-pins version:1) and writeLedger (`:160-177`, serializes an explicit literal dropping unknown keys) and say whether the chosen state location survives a ledger rewrite, a crash between delete and gate-write (is a crash-loop or a repeated sweep possible?), a downgrade, and a dry run; does deleting records interact with anything else that reads them (reports/warnings.md GROUPS, doctor counts, the banner decay) — a warning that disappears then reappears if the file still fails; is "re-quarantine inputs that still fail" guaranteed by construction (the parse-threw boundary is untouched)? (2) release ordering (owner item 2): if the companion lands in a release AFTER the hardening, records accumulated between the two releases are also swept — is the gate keyed so that ANY later upgrade sweeps once, or only the specific version pair? (3) the transcripts spec's claim that its parse output stays byte-identical — does adding the companion change that? (4) doctor spec unchanged since round 1 — confirm nothing regressed. (5) cites at both ends, checklists, Security checklists, no owner-approval claims. Report findings with file:line, band A/B/C, LIGHT/HEAVY. End with a verdict per spec: approve / request-changes.
