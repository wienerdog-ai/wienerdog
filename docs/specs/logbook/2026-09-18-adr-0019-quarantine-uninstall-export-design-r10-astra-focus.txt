Adversarial DESIGN review, round 10 (confirming), of docs/specs/WP-adr-0019-quarantine-uninstall-export.md (Draft, M). Rounds 1-9 raws and dispositions in docs/specs/logbook/2026-09-18-adr-0019-quarantine-uninstall-export-design-r{1..9}-astra-raw.json and -design-review.md. Round 9: the replay guard covered targets below a shelf but not a recursive delete of a shelf ancestor. Fix: X16 symmetric — no manifest-driven deletion may cover a shelf path from below or from above; new Table V enumerates the recursive deleters read from the shipped code: vendored-tree (manifest.js:593) and copied-skill (:645) only; `dir` deliberately excluded because init records {kind:'dir', path:'<state>'} and guarding it would add a skipped line to an ordinary uninstall (breaking W6/AC 7 byte-identity); a verification step requires exactly three `recursive: true` hits in manifest.js (:593, :645, :1148 = disposeCoreMechanics under X1); AC 18 gains the from-above arm (app→state symlink + forged vendored-tree; copied-skill; a {kind:'dir'} negative control) counted through the seam; RED quse-reverse-guard-from-below-only anchored on `if (!sameResolvedDir(entry.path, appRoot)) {`; thirteen declarations over eleven criteria.

Confirming round, final. Execute against the tree (temp dirs only): (1) the `dir` exclusion: read the `dir` reverser in reverse() — is it non-recursive (rmdirSync / only-if-empty) so that it can never delete a shelf FILE, and can it remove the shelf ROOT directory when empty (harmless) — confirm and confirm the spec states exactly that; is there any other kind whose reverser removes a directory tree through a helper that does not literally spell `recursive: true` (e.g. a call into disposeCoreMechanics, a rimraf-like helper, a rename-aside) — the grep count is only as good as that; (2) sameResolvedDir at the app-root check — with app → state, does the guard's "contains a shelf root" test run on the RESOLVED target so the symlinked app case is caught, and does the negative control ({kind:'dir'}) really produce zero skipped lines on the empty-shelf fixture; (3) the thirteen declarations: anchors unique; from-below-only vs both-dropped mutations isolate as claimed; (4) final consistency across Tables K/X/V/W/Y/B, ACs 1–19, both mirror classes, Platform scope, owner items 1–3 (standing form, no approval claims), cites at both ends. If nothing about the product is wrong, say approve.
