# raw — hermetic Codex shadow (codex exec, read-only sandbox, CODEX_HOME=~/.codex-review-home, gpt-5.6-sol high) — vs1
# tip reviewed: e96f761d47ba1b1c70c0f8f7433b6c598818e043 (detached worktree) ; base main = 8358655d41f997e8da05a39ec6b2851d05785480 ; committed pre-adjudication
# codex exec rc=0 ; porcelain before/after: identical

{"target":"e96f761d47ba1b1c70c0f8f7433b6c598818e043","verdict":"needs-attention","executed":["git status --short --branch; git rev-parse HEAD; git diff 8358655d41f997e8da05a39ec6b2851d05785480 HEAD --stat; git diff ... -- <three reviewed docs>: rc 0 (confirmed detached target and three-file diff)","Full/targeted reads and rg sweeps across all named specs, source files, tests, RED declarations, runner, Table U, Table W, and ADR-0012: rc 0","rg caller/requirer sweep for assertGitRepo, assertCleanTree, restoreVaultToHead, and dream/validate: rc 0; excluding validate.js found no src callers for the latter two (rg rc 1)","Raw git probes: nested docs directory rev-parse and show-toplevel rc 0; /tmp without GIT_DIR rc 128; /tmp with GIT_DIR pointing at this worktree rc 0","Spec V4 on the untouched source: rc 0, count 4","In-memory adversarial V4 probe adding a fifth multiline git() call: rc 0 and reported actualGitInvocations=5, v4Count=4, v4WouldPass=true","Spec V3 identity check: rc 1 as expected because the implementation declaration is absent on this docs-only branch","npm test: rc 1 before tests ran; sandbox denied mkdtemp with EPERM","node scripts/red-proofs.js: rc 1 before proofs ran; sandbox creation failed with EPERM","npm run lint: rc 1; markdownlint dependency lookup failed because network access was unavailable; frontmatter check passed","node scripts/boundary-check.js against the reviewed diff: rc 0","git diff --check 8358655d41f997e8da05a39ec6b2851d05785480 HEAD: rc 0"],"summary":"Do not ship this design yet. Its W1 standing-trigger disposition is not supported by the canonical wording, and the claimed mechanical mirror demonstrably false-greens forbidden call-shape growth. Several canonical/current-state claims also duplicate or contradict the repository evidence.","findings":[{"title":"[product/contract] The spec narrows W1(c)(i)'s standing trigger without authority","file":"docs/specs/WP-dream-git-env-validate-seam.md","line_start":165,"line_end":180,"materiality_band":"B","confidence":0.94,"body":"Table W says \"any change to what validate.js spawns\" falsifies the election; it does not limit that trigger to argv or call-site count. This WP changes the environment handed to spawnPinnedSync from process.env to buildGitEnv(), which is part of the spawn operation. The nearby tenth-shape discussion describes the required response—close the seam and bring the shape to the owner—but does not redefine \"any change\" as argv-only. O4 authorizes extending the constructed environment; this finding does not challenge O4. It challenges the unapproved conclusion that O4 leaves the prior standing trigger untouched. The implementation could pass every proposed gate while the default-deny contract remains undisposed.","recommendation":"Either follow W1(c)(i) as written and route seam closure/tenth-shape admission, or obtain a dated amendment explicitly limiting the trigger to argv/call-site changes and explaining why environment-only narrowing preserves the stale-stat safety measurement. Update all registered W1/Table J mirrors accordingly."},{"title":"[criterion/machinery] V4 can pass with a fifth git call and with changed spawned argv","file":"docs/specs/WP-dream-git-env-validate-seam.md","line_start":368,"line_end":383,"materiality_band":"B","confidence":1,"body":"V4 counts only the exact text `git(vaultDir, [`. A fifth call formatted as `git(\\n  vaultDir, [...])` is invisible. I executed an in-memory mutation with exactly that fifth call: the simulated file had five invocations while all four literals remained present, the count remained four, and V4 passed. V4 also does not inspect git()'s own `args: ['-C', vaultDir, ...args]` construction, so changing the actual prefix handed to spawnPinnedSync—or adding a direct spawnPinnedSync call—also escapes it. AC5 therefore does not mechanically establish the invariant it claims.","recommendation":"Replace the textual count with a structural check that recognizes calls independent of formatting and rejects additional direct spawn sites. Also capture spawnPinnedSync options through the suite's existing require-cache collaborator substitution and assert the four complete spawned argv arrays, not merely the arguments passed into git(). Prove red cases for multiline addition, direct spawn addition, and wrapper-prefix mutation."},{"title":"[mirror] The spec restates Table U's channel set while declaring it citation-only","file":"docs/specs/WP-dream-git-env-validate-seam.md","line_start":69,"line_end":74,"materiality_band":"C","confidence":0.99,"body":"This bullet enumerates PATH, HOME, the nine win32-carried keys, USERPROFILE, and GIT_INDEX_FILE behavior. That is a partial restatement of the channel set whose canonical Table U the spec repeatedly says must be cited and never restated. Because this copy is not registered as a Table U mirror, a later canonical change can leave the successor spec stale while its coherence checks remain green.","recommendation":"Replace the copied channel inventory with citations to Table U's applicable rows. Retain only this WP's local call contract—buildGitEnv() is invoked with no argument—without reproducing the resulting key set."},{"title":"[record] The claimed two-place git reachability inventory omits promote.js","file":"docs/specs/WP-dream-git-env-validate-seam.md","line_start":34,"line_end":41,"materiality_band":"C","confidence":0.99,"body":"The claim that a dream run makes git calls from two places is false. The same spec later acknowledges src/core/dream/promote.js's spawnGitForMerge, and canonical Table W W1(c)(ii) identifies that merge-file invocation as a dream-path spawn outside the pipeline seam. The relevant validate.js reachability conclusion remains valid, but the purported whole-run inventory is not.","recommendation":"State that there are two spawn surfaces relevant to this WP, or list all three modules and explicitly mark promote.js's already-constructed merge spawn as out of scope."},{"title":"[record] Table J's “exported for tests only” assertion is false on the reviewed tree","file":"docs/specs/WP-dream-git-env-validate-seam.md","line_start":160,"line_end":160,"materiality_band":"C","confidence":0.99,"body":"A repository-wide test search found no test that calls or imports assertCleanTree; only stale prose/comments mention it. The source export block says tests use it to build fixtures, but the current test suite does not. The no-src-caller portion of J2 is correct, while the stated reason the export exists is not.","recommendation":"Describe the measured state narrowly: assertCleanTree is exported and has no in-repo caller. Remove the tests-only rationale from Table J, and either authorize correction of the stale export-block comment in this WP or route that comment as a discovered issue."},{"title":"[mirror] The owner queue is an unregistered duplicate of J0/J5","file":"docs/specs/logbook/2026-09-05-owner-rulings-git-env-pinning-queue.md","line_start":143,"line_end":182,"materiality_band":"C","confidence":0.96,"body":"The append says O4/O5 live in one place and are cited rather than restated, but the following entries restate J0's construction rule and J5's nested-vault behavior. The primary spec's “every surface” Mirrored Surface Checklist does not register this queue entry, so Table J and all listed mirrors could move while the adopted-owner record silently disagrees.","recommendation":"Either reduce the queue entries to true citations, or register this append as an external Table J mirror and specify the dated-amendment procedure required when J0 or J5 changes."}]}