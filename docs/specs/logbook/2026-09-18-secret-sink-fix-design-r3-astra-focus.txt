Adversarial DESIGN review, round 3, of docs/specs/WP-secret-sink-chunk-fix.md (M) with its dependencies docs/specs/WP-secret-sink-redact-before-truncate.md, docs/specs/WP-secret-stream-safe-cut-redactor.md and docs/adr/0043-safe-cut-stream-redaction.md (all Draft). Rounds 1-2 raws and dispositions: docs/specs/logbook/2026-09-18-secret-sink-fix-design-r{1,2}-astra-raw.json and 2026-09-18-secret-sink-fix-design-review.md. Round 2's finding: the shutdown() call in the finally at src/cli/dream.js:479 missed the pidfile-failure path (:410-447 throws before the watchdog try at :461). Fix: the whole post-spawn body of runBrainWithWatchdog (:405 through the close of the reap finally at :539) is wrapped in a NEW OUTER try { … } finally { shutdown(); } — three literal edits: `const { child, done, shutdown } = spawnBrain({`, `try {` after the `});` at :403, `} finally { shutdown(); }` after the reap finally's closing brace at :539; the reap finally is untouched and runs first (inner before outer); AC2b adds the caller-level regression reusing the pidfile-failure harness at tests/integration/dream.test.js:1554-1610 (Deliverables now nine paths); the churn gate is `git diff -w` ≤12/−2 plus three structural greps; the Current-state bullet that claimed both throw paths unwind through the reap finally was corrected.

Verify by applying the three edits in memory to src/cli/dream.js and tracing every settle path — normal completion, watchdog rejection at ~:457, pidfile write failure with reap success and with reap failure (:434/:442 throws), a throw from inside the reap finally itself, an exception from shutdown() — and confirm shutdown() runs exactly once on each, before :958's logStream.end(), after the reap verdict, with no change to the returned { sawUnknownCommand, reap } and no scope change; check that an exception thrown by the inner finally does not skip the outer finally and that shutdown() being idempotent makes a double call harmless; check the churn gate actually bounds what it claims (can a reindent hide a semantic edit under `git diff -w`?) and that the three greps are unambiguous in the file. Then confirm nothing regressed in the sibling specs or the ADR since round 2 (they were not re-opened). Cites at both ends. Report findings with file:line, band, LIGHT/HEAVY; verdict per spec and the ADR: approve / request-changes.
