# Raw gate output — Codex plugin adversarial-review, round 2, docs/wp-instruction-basename-currency at 0eb96a96 (base main). Committed BEFORE adjudication. Focus text reproduced at the end.

# Codex Adversarial Review

Target: branch diff against main
Verdict: needs-attention

No-ship: two Band-A bypasses let core deliverables pass without being delivered. Executed commands: (1) initial `git status --porcelain=v1` plus scoped diff, exit 0; (2) targeted `rg` and numbered spec excerpt, exit 0; (3) numbered lines 275–674, exit 0; (4) production predicate/layout and RED-runner inspection, exit 0; (5) BASELINE/RED/CONTROL inspection, exit 0; (6) here-doc probes plus `mktemp -d`, exit 1 because the sandbox denied temporary writes; (7) production `makeAdmit(defaultLayout())` spot-check and Table-A count, exit 0—nine names, and GitHub/Cursor/Claude rows matched Table B; (8) exact fenced V3 command, exit 1 as expected because the prospective inventory does not exist; (9) exact V4 against a negated synthetic step, exit 0, demonstrating the bypass; (10) first in-memory V3 harness attempt, exit 1 due shell interpretation of backticks; (11) corrected in-memory V3 harness, exit 0—dropped/reworded/removed/duplicate cases red and Fetched changes green, but a document containing only the three table regions also green; (12) current runbook plus V4, exit 1 as expected with zero matching steps; (13) scoped `git diff --check` plus final status, exit 0. Initial and final status were identical: `?? node_modules`. RED-proof semantics themselves are sound provided criterion 7 retains its fixed nine-name literal: BASELINE requires that exact test to pass, RED requires an own-body `ERR_ASSERTION`, and CONTROL reruns pristine. The literal is edited in the same test file as other fixtures, but V3 independently anchors it to the spec’s nine-row Table A; shrinking both oracles would require also violating the explicit spec contract.

Findings:
- [high] [Band A] V4 accepts a runbook step that explicitly negates the maintenance obligation (docs/specs/WP-instruction-basename-currency.md:595-622)
  Criterion 4 defines success solely as the presence of five substrings. I executed the exact V4 verifier against: `For a MINOR release, the release maintainer MUST NOT re-fetch docs/instruction-file-inventory.md; any source and tests MUST NOT be updated in the same pull request.` It returned `V4 OK`. Thus the standing obligation—the central mechanism intended to prevent recurrence—can be absent or inverted while criterion 4 passes, leaving the inventory silently stale.
  Recommendation: Fix the normative step text in Table C and require that exact affirmative clause, or compare the complete numbered step against a prescribed template. Token presence cannot establish obligation semantics.
- [high] [Band A] V3 does not prove the required inventory skeleton or dated-residual declaration (docs/specs/WP-instruction-basename-currency.md:542-592)
  Criterion 1 says the document follows the complete skeleton and claims V3 proves it, but V3 compares only the content under the three table headings. An in-memory execution of its exact region logic passed a synthetic document containing only those headings and copied table regions—no `# Instruction-file inventory`, `Current as of` date, canonical-source statement, or named-residual opening. It also accepts arbitrary heading suffixes. Consequently an undated inventory that omits the explicit residual can pass V1–V3 and the new criterion-7 test.
  Recommendation: Extend V3 to compare the entire fixed skeleton, with narrowly normalized placeholders for the document date and Table-A Fetched cells. Require exact section headings and validate the substituted dates rather than limiting comparison to table regions.

Next steps:
- Strengthen V3 and V4, then rerun the same omission, negation, row-drop, duplicate, heading-removal, Fetched-date, CRLF, and baseline/RED/control mutants.
- Keep the nine-name criterion-7 literal independent of inventory parsing and retain the current Table-D targeting.

---- focus text ----
SCOPE: docs/specs/WP-instruction-basename-currency.md (Draft, 674 lines) and docs/specs/logbook/2026-09-04-instruction-basename-currency-design-gate-rounds.md, as changed relative to main. ROUND 2 — the closing confirmation of a design loop. The WP: bring the instruction-file basename deny-list in src/core/dream/promote.js (INSTRUCTION_BASENAMES, four names) current with a dated inventory shipped as docs/instruction-file-inventory.md (Tables A/B/C copied verbatim within a mechanical copy boundary), a standing obligation in docs/runbooks/release.md (Table C, five literal tokens), tests plus a red-proofs declaration (Table D). Owner rulings already given: the 2026-08-05 ruling keeps this a NAME LIST with a named residual; dot-prefixed paths are a CLASS rule owned by WP-dot-segment-denial.
ROUND 1 FINDINGS (verify each is GENUINELY fixed, not re-worded): (R1-A, converged) the shipped inventory, the constant and the fixtures were mutually validating mirrors, so a consistently omitted row (drop QWEN.md from the document, the constant and the fixtures together) passed criteria 2-3, V1, V2 and the RED proof. Fix claimed: acceptance criterion 7 — a test carrying a HAND-WRITTEN literal expected set of nine basenames asserting set equality against the inventory parse; V3 — a spec-vs-document region compare over Tables A/B/C (region = heading to last pipe-line) allowing only the Fetched-cell substitution and rejecting duplicates; Table D's RED proof retargeted from criterion 2 to criterion 7 so a shrunken tree fails at BASELINE. (R1-B, plugin) Table B's copied preamble said HANDOFF rows "are denied instead" by a rule not yet in force. Fix claimed: ASSIGNED-not-protected wording plus a measured fifth column "Refused independently of the dot-segment rule" on all 24 rows. (R1-C, converged) the runbook check grepped only the pathname. Fix claimed: V4 isolates exactly one numbered step by the path (zero or two matches fail) and asserts Table C's five literal tokens on that step's text; criterion 4 rewritten.
CHALLENGE THE NEW MECHANISMS: (a) criterion 7's literal set — can the SAME accident that shrinks the document/constant/fixtures also shrink the literal (is it in a file the implementer edits for other reasons; is there any instruction telling the implementer to derive it)? Is nine the right count against Table A on this tip? (b) V3's region compare — extract the exact command from the fenced block and run it against a temp copy of the spec's own tables as a synthetic shipped document (the sandbox may deny temp writes; if so, reason from the script and say so): does a dropped row, a reworded cell, a removed heading, a duplicate row go red, and does a Fetched-date change stay green? Does the "heading to last pipe-line" boundary have an edge case (a table followed by another pipe-line paragraph, a trailing blank, CRLF, a cell containing an escaped pipe)? (c) Table D retarget — read scripts/red-proofs.js's BASELINE/RED/CONTROL semantics and judge: with expectRed naming criterion 7's test, on the CORRECT finished tree does the four-name revert produce an own-body ERR_ASSERTION failure of exactly that test (PROVEN), and on a shrunken tree does BASELINE fail (non-PROVEN)? Any way both are true of a wrong tree? (d) V4 — token assertions on one numbered step: can a step that contains all five tokens but obliges nothing pass (the tokens present, the obligation absent)? Is "exactly one match" fragile against the runbook's existing text? (e) Table B's fifth column — spot-check three cells against the production predicate (makeAdmit with defaultLayout()) for the mechanism claimed. (f) Anything ELSE material: a way the implementer following only this spec ships a wrong deny-list, a stale inventory, or an unreachable name.
OUT OF SCOPE — file as scope objections in a separate routed section, do NOT count toward the verdict: the two owner items in ## Dispatch precondition (denying replit.md and AGENT.md; the obligation's owner being a role), the 2026-08-05 list-with-residual ruling, the dot-segment class, the closed round-1 dispositions themselves (adjudicated), any code change (nothing in src/ or tests/ changes on this branch), and every file other than the two in scope.
REPORT: state what you EXECUTED (each command with exit status), then findings with file:line and a materiality band (A: a criterion a wrong implementation passes, or silent wrong behaviour; B: caught downstream; C: hygiene), then the verdict. Review is READ-ONLY: git status --porcelain must be identical before and after.
