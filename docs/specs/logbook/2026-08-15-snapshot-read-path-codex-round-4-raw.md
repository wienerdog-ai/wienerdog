# Adversarial design review — external round 4, the declared last round on the recurring families

WP: WP-snapshot-read-path-hardening
Backend: gptsol (Codex subscription), one agent per gate run
Tree: `wp/snapshot-read-path-hardening` @ `eb0749f`; the reviewed spec and ruling
are byte-identical to `57eb54c`, which the reviewer verified with
`git diff --exit-code`.
Read-only check: `git status --porcelain` empty before and after, byte-identical
(the reviewer ran and reported both).

Raw final output, byte-unchanged inside the fence, committed **before any
adjudication** per `docs/runbooks/codex-review.md` ("Rules"). No transport
artifact needed decoding in this one.

This round was told, in its focus text, that three consecutive rounds had landed
in the same two families, that the circuit-breaker had already fired once, and
that whatever it found in those families would be carried as a named residual
rather than patched again — and it was asked to say plainly whether a finding
would actually harm a user or is a precision gap. Its summary answers that
question directly.

`````text
{"verdict":"needs-attention","summary":"Needs disposition before Ready, not another product patch: the read-path contract is otherwise sound, but its exact reason-set claim includes a file-count reason that no valid current plan can emit. This is a precision/testability gap suitable for the declared named-residual stop condition, not a user-harming refusal defect.","executed":["git -C /Users/felho/dev/repos-to-learn-from/wienerdog status --porcelain (initial) — exit 0; stdout empty","git -C /Users/felho/dev/repos-to-learn-from/wienerdog branch --show-current — exit 0; wp/snapshot-read-path-hardening","git -C /Users/felho/dev/repos-to-learn-from/wienerdog rev-parse HEAD — exit 0; eb0749f9457a26f05b0e9aec23f4fae442245f3c","git -C /Users/felho/dev/repos-to-learn-from/wienerdog rev-parse '57eb54c^{commit}' — exit 0; 57eb54cc7f090d6adf31c08d276333ebaa836c21","git -C /Users/felho/dev/repos-to-learn-from/wienerdog rev-parse '4606e36^{commit}' — exit 0; 4606e3643e37ad9b19606b32d50de85791d5f560","git diff --exit-code 57eb54c HEAD for the reviewed spec and ruling — exit 0; byte-identical","git ls-tree/grep for the scope ruling — exit 0","git ls-tree/grep for snapshot review records — exit 0","wc -l on vault-snapshot.js and vault-snapshot.test.js — exit 0; 205 and 411","grep -a makeVaultSnapshot in broker-wiring.test.js — exit 0; six call sites","grep CI platform matrix — exit 0; ubuntu-latest and macos-latest, no Windows leg","git diff --exit-code main...HEAD -- src tests — exit 0; implementation and tests unchanged","node limits/flags/plan probe — exit 0; MAX_FILES=32, MAX_TOTAL_BYTES=2097152, MAX_FILE_BYTES=SCAN_MAX_BYTES=262144, O_NOFOLLOW/O_NONBLOCK present, plan maxima 1/14/0","grep documented snapshot/symlink surfaces — exit 0; reproduced the cited threat-model, skill and source surfaces","npm test -- --test-name-pattern \"vault-snapshot\" — exit 0; 120 passed","npm run lint — exit 0; markdown/frontmatter passed; unavailable shellcheck and PSScriptAnalyzer layers skipped","node file-count-cap reachability probe — exit 0; valid plans expose at most 14 candidates versus MAX_FILES=32","git diff 4606e36..57eb54c for the spec/review record — exit 0; inspected all round-3 corrections","node current grow/shrink/zero-byte probe — exit 0; grow→secret skip, shrink→old per-file-cap skip, zero-byte file copied empty","node partial-read/EOF probe — exit 0; observed reads 3 then 0","node symlink/FIFO/open probe, first form — exit 1; Node rejected mixed CommonJS require and top-level await","node symlink/FIFO/open probe, corrected async wrapper — exit 0; O_NOFOLLOW→ELOOP, unguarded symlink followed, nonblocking FIFO opened as non-file, unguarded FIFO blocked, directory/device opened as non-files, socket open failed","node grow/swap/source-directory-symlink probe — exit 0; grow→secret skip, leaf swap copied outside bytes, symlinked source directory copied external bytes","node current refusal-precedence probe — exit 0; over-cap mode-000 and staged read failure both returned the per-file-cap reason, with zero read calls; leaf symlink returned the non-regular reason","node allocation/scanner/bounded-read probe — exit 0; 120965360-byte source, 262145-byte bounded allocation, a view retained the full backing store, a copy retained 262145 bytes, scanner accepted 262144 and rejected 262145","node oversized UTF-8 decode probe — exit 0; 536870889-byte Buffer threw ERR_STRING_TOO_LONG","man 2 close | grep EINTR — exit 0; Darwin documents EINTR","npm test — exit 0; 2002 tests, 1993 passed, 9 skipped, 0 failed","grep full-suite summary from captured command output — exit 0","node concrete-reason enumeration — exit 0; eight easy fixtures reproduced, while static comparison identified total-cap and file-count reasons for separate reachability checks","node total-cap reachability probe — exit 0; reproduced exceeds the 2097152-byte total cap","grep verification/definition-of-done assertions — exit 0; found the no-new-step statement and stale two-new-assertions wording","grep file-count contract, plans and tests — exit 0; confirmed slice-before-check and no plan above 14 candidates","git -C /Users/felho/dev/repos-to-learn-from/wienerdog status --porcelain (final) — exit 0; stdout empty and byte-identical to initial status"],"prior_findings_verification":[{"id":"3-1","title":"Allocated capacity still has no testable unit or scope","status":"genuinely-fixed","evidence":"The revised mechanism at lines 196 and 387-399 now requires the read-stage buffer itself to be allocated at MAX_FILE_BYTES+1, forbids sizing from source/fstat, and permits onward transfer only as that bounded buffer or a copy of its filled prefix, never a larger-backed view. The executed probe distinguished all cases: a 262145-byte view retained a 120965360-byte backing store, while Buffer.from of that prefix retained only 262145 bytes. The unchanged gate representations are honestly carved out and remain functions of an input already capped at 262144 bytes."},{"id":"3-2","title":"Table C denies a read-failure crossover it explicitly creates","status":"genuinely-fixed","evidence":"Table C now separates changed precedence at rows 3, 4 and 6 from the new row-5 refusal, and the acceptance criterion cites the whole table while explicitly exercising open-failure and read-failure crossovers. The current-state probe reproduced both old assignments: over-cap+mode-000 and over-cap+staged-read-failure each returned the per-file-cap reason, with the latter never read. The revised ladder consistently assigns unreadable before rows 7-9; rows 1-2 and 7-10 remain internally consistent."},{"id":"3-3","title":"The exact reason-set criterion has the wrong cardinality","status":"partially-fixed","evidence":"Table C now correctly lists ten source-level literals/forms and correctly interpolates 262144, 32 and 2097152; the three provenance substitutions are also complete. However, the executed plan probe proved that the preserved plans expose at most 1, 14 and 0 candidates, while MAX_FILES is 32 and fileCount advances at most once per candidate. Therefore `exceeds the 32-file cap` cannot occur for any valid current routine, although the total-cap reason was separately reproduced. The document has fixed the code-vocabulary count, but not its claim that all ten are concrete current outcomes."}],"findings":[{"file":"/Users/felho/dev/repos-to-learn-from/wienerdog/docs/specs/WP-snapshot-read-path-hardening.md","line_start":240,"line_end":248,"confidence":1,"title":"The ten-string set includes an unreachable current outcome","body":"Table C calls all ten entries concrete strings enumerated against today's code, and the acceptance criterion requires that exact ten-string set. But the preserved plans select at most 1 candidate for daily-digest and 14 for weekly-review, while MAX_FILES is 32; the check at row 8 therefore cannot fire through any valid makeVaultSnapshot call. The executed probes separately reproduced the total-cap string and confirmed that only the file-count string is dormant. This does not misroute or harm a user today, but it makes the exact runtime-reason criterion unassertable without an invalid plan, source inspection, or an artificial seam. It is a precision/testability gap in the recurring reason-set family and can be carried as the declared named residual.","recommendation":"Distinguish the ten code-level reason literals from the nine outcomes reachable under the frozen current plans, and identify the file-count literal as dormant. Make acceptance assert the preserved vocabulary separately from reachable runtime outputs; do not alter MAX_FILES or the plans merely to make the string reachable. If the surface is frozen, record this exact distinction as the named residual."}]}
`````
