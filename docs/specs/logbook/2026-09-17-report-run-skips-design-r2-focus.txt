DESIGN REVIEW ROUND 2 of a re-derived work-package spec; no product code has been written yet. Review ONLY: docs/specs/WP-dream-report-run-skips.md and the logbook entries docs/specs/logbook/2026-09-17-report-run-skips-rederived.md and the 2026-09-17 report-run-skips design-review entry (round-1 raw for reference: docs/specs/logbook/2026-09-17-report-run-skips-design-r1-raw.json). Exclude docs/marketing/, memory/research/ and userreports/ entirely. The spec makes the nightly dream's vault report state, in plain language, how many sessions a run could not consolidate, as six integer counts composed inside src/core/dream/promote.js composeRecord via a new optional promote({runSkips}) argument. PART ONE - verify each round-1 finding is GENUINELY fixed in substance, not re-worded: (F1) "Quarantine counts disappear whenever no session is admitted" - disposition was to correct the false coverage-boundary claim, not grow the product: the spec must now state exactly which paths write a report, and for each zero-admission path what is and is NOT carried today, with an owner item pricing durable zero-admission accounting. Is every sentence of that now true against src/cli/dream.js? (F2) "The oversized-session promise omits real retry conditions" - the byte-exact bullet now names three invalidators (session change, Wienerdog update, the limit raised past the measured size) and says re-measurement may still find it too big. Is it exactly true against src/core/dream/scratch.js and the ledger memo rules, and still plain language? (F3) "Acceptance criterion 5 requires an impossible collector state" - now two runs. Are both satisfiable, and do they still prove disjointness? (F4) "The warnings pointer can target a missing or stale file" - disposition: named residual plus owner item, because refreshWarnings runs before promote() only when there is a NEW quarantine, so a confirmed-current flag would delete the pointer from the standing-quarantine case. Verify that ordering claim against src/cli/dream.js. (F5) "The code-owned heading is not reserved from brain-authored content" - disposition: mirror the existing rule for the shipped code-owned headings, which is that there is none (they appear only where emitted; nothing scans, strips or dedupes); behaviour pinned by two fixtures and routed onward. Verify that claim against src/core/dream/promote.js. (F6) "The inline integers-only gate accepts plausible coercion bugs" - now a full expected-string comparison, an unsafe-integer case and a float-coercion RED mutation. Check it discriminates. PART TWO - closure round: report only findings about PRODUCT behaviour a user would observe (an untrue sentence in the report, a lost or double-counted count, a pointer to the wrong place, untrusted input reaching markdown). List findings about the spec's own verification machinery separately, marked as machinery. Do all file:line citations resolve on this tree. Owner items are recommendations with overrule costs by design; disagreeing with one is a welcome finding, their being unratified is not. Report what you executed versus what you only read.
