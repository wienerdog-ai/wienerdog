# Raw gate output — Codex plugin adversarial-review, round 1, docs/wp-dot-segment-denial at 4f02c5f3 (base main = 29c61d03). Committed BEFORE adjudication. Focus text reproduced at the end.

# Codex Adversarial Review

Target: branch diff against main
Verdict: needs-attention

NO SHIP: the verification admits at least one simple non-class implementation, and C1’s expected RED surface is underspecified. Table D otherwise maps all 17 HANDOFF rows to 29 paths correctly; the adopt conjunction, permission boundary, ADR-0004, and size M are sound. Worktree was clean before and after. Out-of-scope objections: none.

Executed: initial status/diff/source inspection (rc=0); RED-runner/inventory inspection (rc=0); proof/test inspection (rc=0); vault-write and cited-line inspection (rc=0); V2 extraction inspection (rc=0); exact fenced V2 (rc=1: sandbox denied mkdtemp before grading); first in-memory adaptation (rc=1: adapter mismatch); corrected in-memory V2 (rc=1 as expected: admit 0/312 | reader 0/234 | producer 1/7 | over 12/12 | handoff 36/87); five-path makeAdmit spot-check, Unicode NFC scan, and status (rc=0); numbered spec inspection (rc=0); existing-test coverage search (rc=0); heredoc matcher probe (rc=1: sandbox denied shell temp file), followed by diff checks/status (rc=0); corrected in-memory fitted-matcher probe (rc=0: held-out 104/104, Table-D DOT segments 13/13, while `.`, `.ordinary`, and `.vscode` escaped); final git status --porcelain=v1 (rc=0, empty).

Findings:
- [high] [A] The class proof never grades the bare-dot boundary and still accepts a fitted non-class matcher (docs/specs/WP-dot-segment-denial.md:464-478)
  Every generated segment has a non-empty `z`-led suffix, every Table D path has a longer dot name, and the adopt case uses `.projects`. Meanwhile the existing report test is instructed to remove `.`, `./reports`, and `reports/./dreams`. Therefore an implementation using `seg.length > 1 && seg.startsWith('.')` at both validators passes criteria 1–5, including both RED mutations, while `readVaultLayout` still honors the explicitly forbidden value `reports_dir: .`. More generally, an in-memory probe confirmed that a matcher combining the documented z-family with Table D’s 13 relevant segment names covers 104/104 generated segments and 13/13 oracle segments while missing `.ordinary` and `.vscode`; criterion 3 does not bound the acknowledged family-fit residual when the two matchers are combined.
  Recommendation: Add mandatory non-generated boundary cases for `.`, `./reports`, `reports/./dreams`, short and long non-z dot names, and dot-prefixed basenames at B1/B2. Require those assertions to redden under C1/C2, and add a discriminating proof against the combined generator-family-plus-Table-D matcher rather than treating Table D alone as sufficient.
- [medium] [B] C1 omits a second failure surface that its mutation necessarily creates (docs/specs/WP-dot-segment-denial.md:345-358)
  Deleting the promotion loop does not only redden the B1 held-out assertions: it also makes all 17 formerly admitted Table D paths fail criterion 3 at three depths. The RED runner requires the observed own-body failure set to equal `expectRed` exactly. If the independent oracle is implemented as its own test—as its independent-criterion framing naturally suggests—C1 will be rejected for an undeclared failure, although the implementation is correct. The current text names only the held-out assertions and does not require coalescing both assertion groups into one test identity or declaring both.
  Recommendation: Specify distinct test identities and require C1’s `expectRed` to include both the B1 held-out test and the Table D oracle test. Keep C2 similarly exhaustive after adding the missing reader boundary cases.

Next steps:
- Revise the class grading and C1 expected-failure contract, then rerun the design gate.
- Run the literal V2 again in an environment that permits its self-cleaning temporary directories.

---- focus text ----
SCOPE: docs/specs/WP-dot-segment-denial.md (Draft, 830 lines) and docs/specs/logbook/2026-09-04-dot-segment-denial-design-gate-rounds.md, as changed relative to main (run: git diff main...HEAD --stat). Design review of a work package handed to a sonnet implementer who reads ONLY the spec and CLAUDE.md. The WP: the 2026-08-05 audit ruling's item 1 — "no write to any path with a dot-prefixed segment", a CLASS rule — enforced at THREE points: makeAdmit clause (c) in src/core/dream/promote.js (a new loop placed LAST, after clause (a), so no existing refusal string moves), isSafeRelativePath in src/core/layout.js (one new clause, exported, and the copied duplicate in src/core/layout-infer.js deleted in favour of the shared one), and thereby inferLayout / adopt --yes (the producer). A held-out proof (generated dot-prefixed segments, seeded per run, anti-leakage-checked against the sources) grades all three points; a hand-transcribed Table D of the 29 concrete paths the sibling WP handed off is an independent oracle; two RED proofs (Table C) revert each source change to the shipped enumeration. Owner rulings already given: the basename deny-list stays a list (WP-instruction-basename-currency, Done) and is not reopened; the reader's per-key silent fallback stays silent (Dispatch precondition, recommended).
CHALLENGE — every item is about whether a WRONG implementation can pass, or a RIGHT one over-denies: (a) the property "no segment beginning with `.` at any depth in any case" — is there an input shape at any of the three points that still slips through: a segment that is exactly `.` or `..`, a Windows-style separator, a trailing-dot or leading-whitespace segment, a Unicode dot look-alike, a NFD-composed leading character, a layout value with a leading `./`, an absolute value, an empty segment (`a//.b`), a value that folds to a dot only after NFC? Read fold/foldedSegments and isSafeRelativePath on this tree and reason from the code. (b) over-denial — does the class rule refuse anything legal it must not: the vault's own dot-files the dream must still read/write elsewhere, `.md` files whose NAME (not a directory segment) begins with a dot in a legitimate use, the `reports_dir` special case in inferLayout, the `.` reports_dir value the existing report-fallback test relied on (the spec drops three values from that test's case list — is that the right change and is the residual comment left honest)? (c) the held-out grading — extract V2 from the fenced block and run it as written on this untouched tree (expected `admit 0/312 | reader 0/234 | producer 1/7 | over 12/12 | handoff 36/87`, rc=1); reason about whether a matcher fitted to the generator's FAMILY (z-prefixed four-letter draws) passes the class grade while failing the class — the spec names this as a residual; is the residual honestly bounded? (d) Table D — 29 hand-transcribed paths as the independent oracle; does the sibling's shipped docs/instruction-file-inventory.md Table B (17 HANDOFF rows) map exactly onto them, and does each expected refusal reason ("dot" vs "unchanged") hold under the loop-LAST ordering (spot-check five with makeAdmit on this tree)? (e) the two RED proofs — read scripts/red-proofs.js validateProof/evaluateRed and tests/red-proofs/*.proofs.json; would C1 and C2 as specified validate, and would each RED be an own-body ERR_ASSERTION of the named test in tests/unit/dot-segment-denial.test.js and nothing else undeclared? (f) the adopt --yes round trip (criterion 4, tests/integration/adopt-e2e.test.js) — can it pass without proving that a dot-prefixed candidate is neither persisted nor silently discarded on re-read? (g) the Deliverables table as a permission boundary — is any file the implementer must touch missing (adopt.js? the sibling's inventory? GLOSSARY? CI?), and does the one allowed edit to tests/unit/dream-promote.test.js leave that test honest? (h) ADR-0004 and size M honest?
OUT OF SCOPE — file as scope objections in a separate routed section, do NOT count toward the verdict: the Dispatch-precondition item (silent fallback), the 2026-08-05 ruling's class shape, the basename list, any code change (nothing in src/ or tests/ changes on this branch), and every file other than the two in scope.
REPORT: state what you EXECUTED (each command with exit status), then findings with file:line and a materiality band (A: a criterion a wrong implementation passes, silent wrong behaviour, or an over-denial of legal input; B: caught downstream; C: hygiene), then the verdict. Review is READ-ONLY: git status --porcelain must be identical before and after; probes in memory where the sandbox denies temp writes.
